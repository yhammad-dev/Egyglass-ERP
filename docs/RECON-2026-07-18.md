# RECON — جرد استطلاعي شامل (قراءة فقط)
> التاريخ: 2026-07-18 · الوكيل: READ-ONLY reconnaissance · الملف تراكمي (يُلحق به عند نهاية كل Phase)
> **الحدود:** قارئ ساكن. لا ادعاء "يعمل/يشتغل end-to-end" — فقط تصنيف وجود الكود وتوصيله.
> **سلّم النضج:** `CODE-PRESENT` (المنطق موجود بدليل مقتبس) · `WIRED` (مسار متتبَّع: واجهة → action → Prisma، بدليل عند كل قفزة) · `NOT-WIRED` (كود بلا مدخل).
> **معيار الدليل:** كل ادعاء = `[VERIFIED path:line]` + اقتباس حرفي ≤3 أسطر. ما لم يُؤكَّد في الكود = `[UNVERIFIED source]`.

---

# PHASE 0 — التوجيه (Orientation)

## 0.1 — الـ Stack والبيئة (مُتحقَّق من الملفات لا من الذاكرة)

- **Next.js 15.3.4 / Prisma / Postgres / Auth.js v5 / next-intl / Tailwind RTL** — [VERIFIED AGENTS.md:19]
  ```
  | Framework | **Next.js 15.3.4** (App Router, Turbopack) | NOT 16. Verified via build/runtime repeatedly. |
  ```
- **المنافذ والحاويات — التطبيق 3100 والقاعدة 5433، والحاويتان تعملان** — [VERIFIED docker-compose.yml:49 + `docker compose ps` raw]
  ```
  egyglass-erp-app-1   node:20-alpine       Up 13 hours   0.0.0.0:3100->3000/tcp
  egyglass-erp-db-1    postgres:16-alpine   Up 2 days (healthy)   0.0.0.0:5433->5432/tcp
  ```
- **DB superuser = egyglass** — [VERIFIED docker-compose.yml:23] `POSTGRES_USER: egyglass`
- ⚠️ **إشارة أمنية (تفصيلها في SECTION D لاحقًا):** `docker-compose.yml:24` يحمل `POSTGRES_PASSWORD: egyglass_dev_pw` و`:45` يحمل `AUTH_SECRET: "dev-secret-change-me"` — dev-only بتعليق صريح، لكنه ملف مُتتبَّع في git.
- **الحاوية تعمل production (`npm run start`)** — [VERIFIED docker-compose.yml:40]
  ```
  command: sh -c "npm install --ignore-scripts && ... prisma migrate deploy 2>/dev/null; npm run start"
  ```
  (ملاحظة جانبية: الأمر يكتم stderr للـ `migrate deploy` بـ `2>/dev/null` — على خلاف درس "ممنوع كتم المخرجات" في CLAUDE.md؛ رصد فقط.)

## 0.2 — خريطة الشجرة (src/app + lib)

**صفحات الداشبورد (37) + auth (1) + طباعة (3) — [VERIFIED Glob `src/app/**/page.tsx` = 40 ملفًا]:**
- عملاء/مبيعات: `customers`, `customers/[id]`, `coverage-edits`
- تسعير/عروض: `quotations`, `quotations/[id]`, `quotations/[id]/edit`, `quotations/[id]/contract`, `quotations/new`, `quotations/new/[productType]`
- مكتب فني: `technical-office`, `technical-office/[id]`
- معاينات: `inspections`, `inspections/[id]`
- تصنيع/مراجعة: `manufacturing`, `manufacturing/[id]`, `review`, `review/[id]`, `investigations`, `investigations/[id]`, `factories`
- تركيبات: `installations`, `installations/[id]`
- مالية: `accounting`, `accounting/customer/[id]`, `statements`, `invoices`, `approvals`
- إدارة: `admin/pricing`, `admin/import`, `users`, `audit`, `executive`, `dashboard`, `hr`, `projects`
- طباعة `(print)`: `quotations/[id]/print`, `statements/[id]`, `invoices/[id]`
- auth: `login` + جذر `src/app/page.tsx`

**route handlers — [VERIFIED Glob]:** `src/app/api/notifications/route.ts` · `src/app/api/auth/[...nextauth]/route.ts` · `src/app/uploads/[...path]/route.ts` (الأخير موضوع BL-112).

**ملفات server actions (بـ `'use server'`) — [VERIFIED Grep] 31 ملف كود** (+3 ملفات توثيق مستبعدة): 13 في `lib/*/actions.ts` بالجذر · 5 في `src/lib/actions/` · 2 في `src/lib/finance/` · 10 في `src/app/(dashboard)|(auth)` · 1 صفحة (`quotations/new/page.tsx` تحمل action مضمّنًا). ملحوظة: `lib/executive/actions.ts` **لا** يحمل `'use server'`.

**الشجرتان المتوازيتان (BL-116 مؤكَّد شكلًا):** `lib/` بالجذر = 18 ملف ts [VERIFIED Glob `lib/**/*.ts`] مقابل `src/lib/` = 35 ملف [VERIFIED Glob `src/lib/**/*.ts`] — التفصيل المُلزِم في BACKLOG BL-116/BL-117/BL-118.

**مكوّنات مشتركة:** `src/components/ui/` (12 مكوّن shadcn) + `notifications-bell` + `intl-provider` + `company-settings-panel` + `document-upload` [VERIFIED Glob].

## 0.3 — جرد الـ Schema الفعلي (prisma/schema.prisma — قُرئ كاملًا، 1201 سطر)

- **Migrations: 29 مجلدًا، آخرها `20260714182016_scr018_inspection_gate`** — [VERIFIED `Get-ChildItem prisma\migrations` raw output = 29] — مطابق لـ CLAUDE.md §7.
- **42 موديل:** User, Customer, PostInstallReview, Interaction, Quotation, QuotationItem, DiscountRequest, InspectionRequest, Attachment, ActivityLog, Notification, ManufacturingOrder, Factory, InstallationOrder, InstallationPhoto, InstallationItem, Payment, Employee, LeaveRequest, Project, SystemSettings, CashbackTier, Referral, PriceListItem, ProductRecipe, ConfigType, ManualItem, Material, PricingFactor, ProductType, QuotationApproval, InspectionMeasurement, Document, Contract, PaymentMilestone, ProgressStatement, Invoice, InspectionPhoto, QuotationRequest, Drawing, FaultInvestigation, ExtraItem.
- **39 enum**، أبرزها:
  - `Role` = **14 قيمة بالضبط** [VERIFIED prisma/schema.prisma:620-635]:
    ```
    enum Role { ADMIN SALES_MANAGER SALES_REP INSPECTION_MANAGER INSPECTION_REP VIEWER REVIEW
    PROCUREMENT INSTALLATIONS ACCOUNTING HR PROJECTS TECHNICAL_OFFICE TEC_APPROVER }
    ```
  - `QuotationStatus` = `{DRAFT, SENT, PENDING_APPROVAL, APPROVED, EXPIRED}` [VERIFIED :681-687] — مطابق P-02.
  - `MfgStatus` = 6 قيم `{PENDING, UNDER_REVIEW, REJECTED, IN_PRODUCTION, READY, DELIVERED}` [VERIFIED :742-749].
  - `InspectionApprovalStatus` = `{DRAFT, PENDING_APPROVAL, APPROVED, RETURNED}` [VERIFIED :724-729] (SCR-018).
  - `TechnicalRoute` = `{PROJECTS, SOCIAL_MEDIA}` [VERIFIED :1175-1178] — المساران مؤكَّدان.
  - `DrawingStatus` يحمل قيمًا موثَّقة كميتة: `INS_VERIFIED`, `CEO_APPROVED`, `RELEASED_TO_FACTORY`, `REJECTED` [VERIFIED :1096-1104] — تُفحص في Phase 3.
- **`SystemSettings` singleton typed-columns** [VERIFIED :476-506]:
  ```
  model SystemSettings {
    id                 String    @id @default("singleton")
    discountBasePct    Decimal   @default(18) @db.Decimal(5, 2)
  ```
  الأعمدة الكاملة: discountBasePct, discountMaxReqPct, factorMinimum, vatPct, quotationValidDays, cashbackActive, cashbackStartDate, companyLogoUrl, companyName, updatedById, updatedAt, managerApprovalCeilingPct, ceoDrawingApprovalThreshold, satisfactionSurveyDelayDays, reviewGatePosition, warrantyTextProjects, warrantyTextSocialMedia, warrantyProjectsOnQuotation, warrantyProjectsOnContract, warrantySocialOnQuotation. (فحص "قارئ لكل عمود" = Phase 2.)
- **`Contract.totalValue` snapshot** [VERIFIED :899] `totalValue Decimal? @db.Decimal(14, 2)` مع تعليق "snapshot من Quotation.total وقت الإنشاء".
- **`QuotationRequest.quotationId` صار nullable** [VERIFIED :1038] `quotationId String? @unique` (دفعة هـ / W-01).
- **`ManufacturingOrder.quotationId` بلا `@unique`** [VERIFIED :285-288] مع تعليق رفع القيد (دفعة ج / W-06).
- **حقول قفل الحساب (SCR-016)** [VERIFIED :23-25] `failedLoginAttempts / lastFailedLoginAt / lockedUntil` — وجودها schema-level؛ من يكتبها app-layer يُفحص لاحقًا.

## 0.4 — انحراف عن كتلة التوجيه (Orientation drift)

| # | الادعاء في التوجيه | الواقع | الدليل |
|---|---|---|---|
| 1 | اقرأ `DESIGN-RATIONALE-AR.md` | **الملف غير موجود في الريبو إطلاقًا** | Read error + Glob `**/DESIGN-RATIONALE*` = صفر نتيجة |
| 2 | اقرأ `EXECUTION-LOG.md` كتوجيه | موجود لكنه **أرشيف مجمّد ذاتيًّا عند 2026-07-05** | [VERIFIED EXECUTION-LOG.md:1] `> 🔴 هذا الملف أرشيف تاريخي مجمّد عند 2026-07-05 — لا يُقرأ كحالة راهنة` |
| 3 | `AGENTS.md` كمصدر schema | أقسام الـschema فيه **مجمّدة عند Phase 1 بنصّه هو** | [VERIFIED AGENTS.md:89] `القائمة أدناه مجمّدة عند Phase 1 وتاريخية` |
| 4 | "Finance lives under lib/finance/" | صحيح جزئيًا — **شجرتان**: `lib/finance/` (scope/engine/actions/statements?) **و** `src/lib/finance/` (statements/invoices/document-number) | Glob outputs أعلاه + BL-116 |
| 5 | تقرير الأمان القائم (2026-07-06) يقول "No server-side file read/serve handler exists" (بند 12) | **صار قديمًا:** `src/app/uploads/[...path]/route.ts` موجود الآن وموضوع BL-112 (P0) | [VERIFIED knowledge/security/security-review.md:39] `| 12 | File Download Security | ✅ N/A | No server-side file read/serve handler exists |` مقابل [VERIFIED Glob] وجود الـroute |

## 0.5 — أوامر الـ slash (وُجدت الأربعة ونُفِّذت الثلاثة القرائية + preflight جزئيًا)

الملفات موجودة: `.claude/commands/{dead-schema,rbac-audit,money-audit,preflight}.md` [VERIFIED Glob]. الثلاثة الأولى تنص حرفيًا على "Use a dynamic workflow" بوكلاء `schema-guardian` / `rbac-auditor` / `finance-auditor` + تحقّق عدائي بـ `evidence-verifier` — **أُطلقت كذلك فعلًا** (3 workflows خلفية):

| الأمر | Run ID | الحالة عند كتابة هذا القسم |
|---|---|---|
| `/dead-schema` | `wf_1694e708-4bb` | يعمل — النتائج تُلحق أدناه عند الاكتمال |
| `/rbac-audit` | `wf_b022a600-b14` | يعمل — النتائج تُلحق أدناه عند الاكتمال |
| `/money-audit` | `wf_ff4a4116-91d` | يعمل — النتائج تُلحق أدناه عند الاكتمال |

> ملاحظة تشغيلية (شفافية): الإطلاق الأول للثلاثة فشل فورًا لعطل في تمرير `args` من أداة الـWorkflow (وصل string بدل object — عطل أداة لا عطل ريبو)، فأُعيد الإطلاق والبيانات مضمّنة داخل السكريبت. صفر أثر على الريبو.

### /preflight — تنفيذ جزئي مقيَّد بحدود recon (قراءة فقط)

| # | الفحص | النتيجة | الدليل الخام |
|---|---|---|---|
| 1 | Build | **SKIPPED-BY-RECON-BOUNDARY** | الحاوية تخدم production من `.next` — تشغيل build يدهسه ويستوجب restart (STD-16). recon قراءة-فقط لا يُحدث تغيير حالة. |
| 2 | Runtime (جلسة مُصادَقة) | **SKIPPED — بنيويًا مستحيل للوكيل** | STD-08: الوكيل لا يُسلَّم اعتمادًا حقيقيًا · BL-82: طريقة الإثبات المعتمدة = يوسف يسجّل الدخول. **المُلتقط بلا مصادقة:** `GET / : 307 -> http://localhost:3100/login` · `GET /customers : 307 -> http://localhost:3100/login` (السيرفر حي والحارس يعيد التوجيه — **لا يثبت أكثر من ذلك**). |
| 3 | الحسابات | **FAIL (بلوكر UAT قائم)** | استعلام قراءة: 12 صفًا فقط من 14 دورًا — **`HR` و`PROJECTS` صفر مستخدمين**. `ADMIN: total 4 / active 2` (ثاني نشط = BL-54) · `REVIEW: 1/1` (dev فقط — BL-37) · `INSPECTION_MANAGER: 2/1`. الناتج الخام محفوظ أدناه. |
| 4 | Secrets في git | **PASS مشروط** | `git ls-files` ~ `.env` غير مُتتبَّع؛ الموجود: `.env.example`, `.env.production.example`, `scripts/reset-password.mjs` (break-glass الموثّق BL-48). ⚠️ لكن `docker-compose.yml` المُتتبَّع يحمل dev creds (انظر 0.1). |
| 5 | Backups | **FAIL** | [UNVERIFIED live — VERIFIED doc: BACKLOG BL-56] `لا نسخ احتياطي — لا للـ DB volume ولا لـ egyglass_uploads ... بلوكر UAT` — لا دليل على restore مُختبَر. |
| 6 | Transport (UAT HTTP-only) | **FAIL بحسب التوثيق** | [UNVERIFIED live — VERIFIED doc: CLAUDE.md] `UAT (EC2, Elastic IP 13.63.54.138, port 3100)` بلا ذكر TLS. لم يُفحص المضيف (خارج نطاق recon). |
| 7 | /security-review | مُرحَّل لـ SECTION D | الموجود: شهادة 2026-07-06 (`knowledge/security/security-review.md` — CONDITIONAL PASS بـ P1 مفتوح BIZ-001) **وقد ثبت تقادمها جزئيًا** (بند 0.4 رقم 5). قراءة `knowledge/security/*` تمت التزامًا بـ BL-129. |

**الناتج الخام لفحص الحسابات (قراءة فقط):**
```
        role        | total | active
--------------------+-------+--------
 ADMIN              |     4 |      2
 SALES_MANAGER      |     2 |      2
 SALES_REP          |     4 |      4
 INSPECTION_MANAGER |     2 |      1
 VIEWER             |     4 |      2
 REVIEW             |     1 |      1
 PROCUREMENT        |     1 |      1
 INSTALLATIONS      |     2 |      2
 ACCOUNTING         |     1 |      1
 TECHNICAL_OFFICE   |     3 |      3
 TEC_APPROVER       |     1 |      1
 INSPECTION_REP     |     1 |      1
(12 rows)
```
> (محاولة استعلام تفصيلي لأسماء حسابات dev رُفضت من طبقة أذونات بيئة الوكيل — الاكتفاء بالمجمّع + توثيق BL-38/BL-54/BL-39.)

## 0.6 — حالة git لحظة الاستطلاع

[VERIFIED `git status --short` raw]: تعديلات غير مُرتكبة على `BACKLOG.md`, `lib/pricing/actions.ts`, `src/app/(dashboard)/quotations/[id]/edit/page.tsx`, `src/app/(dashboard)/quotations/new/[productType]/page.tsx`, `src/app/(dashboard)/quotations/new/page.tsx` + ملفات untracked (منها `docs/WORKFLOW-AUDIT.md`, `docs/priceing/`, `.claude/launch.json`). التعديلات الخمسة غالبًا دفعة B2/BL-125-128 غير المُرتكبة — تُفحص دلتاها في المراحل التالية.

---
*(نهاية Phase 0 حتى اكتمال الـworkflows الثلاثة — تُلحق خلاصاتها الخام هنا ثم CHECKPOINT 0.)*
