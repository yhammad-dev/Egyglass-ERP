---
artifact: QA Regression Record
task_id: SEC-002
priority: P0 (Security)
generated: 2026-07-06
author: Atlas QA Regression Engineer
verdict: PASS
---

# SEC-002 — QA Regression Summary

## Task
Fix `authorized()` in `src/lib/auth.config.ts` so all `(dashboard)` route groups require a valid
session at the middleware layer (previously only `/dashboard` was gated; every other path fell
through to `return true`).

## Change
- `src/lib/auth.config.ts` `authorized()` rewritten:
  - `/login` — public; logged-in users redirected to `/dashboard`.
  - `/` — public (it only `redirect()`s to `/login`).
  - **All other paths** — return `isLoggedIn`; unauthenticated requests redirect to `/login`.
- No other file modified. `proxy.ts` matcher already covers all non-`/api`, non-static routes.

> 🔴 **تصحيح مؤرَّخ — 2026-07-25 · المرجع: TO-14 / TO-17.**
> **الادعاء أعلاه غير صحيح**، وقد استُخدم مبرِّرًا لقرار «عدم تعديل أي ملف آخر».
> `src/proxy.ts` **لم يُحمَّل ولا مرة**: اسم `proxy.ts` اصطلاح **Next.js 16** والمشروع على
> **15.3.4** ⇒ لم يكن هناك matcher عامل ولا طبقة شبكة إطلاقًا، وبالتالي **`authorized()`
> المُعدَّل في هذه المهمة لم يُنفَّذ قط**. حُذف `proxy.ts` في TO-14.
>
> **الوضع الفعلي:** الحماية التي وصفتها هذه الوثيقة قائمة بالفعل، لكن مصدرها **طبقة الصفحات**
> لا الشبكة: `src/app/(dashboard)/layout.tsx:72-73` (`await auth()` ثم `redirect("/login")`)
> يغطي مجموعة `(dashboard)` كاملة، بالإضافة إلى `requireRole` في الصفحات والأكشنات.
> مُتحقَّق runtime (2026-07-25): 14 مسارًا محميًا يعيد **307 → /login** بلا جلسة.
> الحارس الشبكي الوحيد اليوم = `src/middleware.ts` ونطاقه **`/uploads` فقط** (TO-07، أُصلح في TO-13).
>
> ⚠️ **أثر على الحكم:** الثغرة مغطّاة فعلًا، لكنها **لم تُغلق بالتغيير المنسوب إليها هنا**.
> ولا يصح الاستشهاد بهذه الوثيقة كدليل على وجود حماية عند طبقة الـmiddleware.
> ⚠️ **سطر تحقق كاذب:** «Logged-in user on `/login` redirected to `/dashboard` — PASS (preserved)»
> **لا يحدث اليوم** — السلوك يعيش حصرًا في `authorized()` الميت ولا حارس ذاتي في
> `src/app/(auth)/login/page.tsx`؛ مُتحقَّق بجلسة حيّة: المسجَّل يبقى على `/login`.
> (خلل تجربة استخدام لا ثغرة أمنية — مسجَّل كتوصية، غير مُنفَّذ.)

## Verification

| Check | Result |
|---|---|
| `npx tsc --noEmit` typecheck | **PASS** (exit 0) |
| Public paths still reachable unauthenticated (`/login`, `/`) | **PASS** (explicit allow) |
| Logged-in user on `/login` redirected to `/dashboard` | **PASS** (preserved) |
| Protected route groups now require session (`/customers`, `/quotations`, `/users`, `/admin`, `/hr`, `/accounting`, `/inspections`, `/projects`, `/review`, `/manufacturing`, `/installations`, `/executive`, `/audit`) | **PASS** (fall-through now returns `isLoggedIn`) |
| Redirect loop risk | **None** — `/login` and `/` are excluded from the auth requirement |

## Not verified (and why)
| Item | Reason | Risk |
|---|---|---|
| Live `npm run lint` | Pre-existing ESLint circular-config crash (same as CRM-001 record); tooling defect, not code | Low — change introduces no new lint patterns |
| Live browser session walk | No dev server seeded in this session | Trace-based verdict; live smoke recommended before customer UAT |

## Verdict
**PASS.** Edge-layer authentication now covers the entire authenticated surface. One Critical
Broken-Access-Control finding closed. Combined with SEC-001, both unauthenticated-exposure
Criticals are resolved; Gate 5 still requires SEC-003 (documented security review) to lift.
