# BACKLOG — السجل الوحيد للبنود المفتوحة

> أي متطلب/فجوة/قرار **يولد في محادثة** يُقيَّد هنا **فورًا**.
> الفجوات ماضاعتش لنقص أدوار — ضاعت لأن **مفيش ليستة**.
> ملفات التحليل = المتطلبات **الأصلية**. هذا الملف = المتطلبات **المكتشَفة**.
> أي تدقيق قادم يقرأ **الاثنين معًا**.

---

## ⛔ قاعدتان لا تُخترقان

### 1. قاعدة الإغلاق
**الـ migration ليست دليل إغلاق. الكود ليس دليل إغلاق.**
بند يُغلق **فقط** بـ: البوابة **مُفعَّلة** فعليًا (لا NULL) **+** الفرع **الشغّال** مُختبَر بجلسة حقيقية.

> اكتشفنا دورين ناقصين → عملنالهم عمودين → سبناهم NULL → **الدورين فضلوا ناقصين.**

### 2. قاعدة منع الاختراع (STD-05)
**الجهل يُكتب، لا يُصمَّم.**

- أي فجوة معلومة → **تُقيَّد هنا كسؤال مفتوح**. **ممنوع** تتحول لـ `nullable` / `config` / `enum` / `flag`.
- أي مصطلح من لغة الشركة **ممنوع يدخل تصميمًا قبل تعريفه**. (كلمة "المدير التنفيذي" غير المعرَّفة أنتجت بوابة كاملة وهمية.)
- **ممنوع إعادة تفسير حقل قائم** لسد فجوة. الحقل يعني ما يعنيه في الكود — لا ما نتمناه.
- **اختبار الاختراع:** حقل/بوابة مقترَحة **ولا يمكن الإشارة إلى سطر في مستند أو قول صريح من صاحب العمل** → **اختراع. قف واسأل.**
- "القيادة عندك" = رتِّب واحسم بين بدائل عليها دليل. **ليست إذنًا بملء الفراغ.**

---

## 📌 كتلة إلزامية في كل برومبت وكيل

```
BACKLOG DISCIPLINE (إلزامي):
1. اقرأ BACKLOG.md قبل البدء — قسم "قرارات محسومة" مرجع مُلزِم، لا اجتهادك.
2. أي متطلب/فجوة/تناقض يظهر أثناء العمل → قيّده في BACKLOG.md فورًا "مفتوح"
   (حتى لو خارج نطاق مهمتك — خصوصًا لو خارجها).
3. ممنوع الاختراع. لو احتجت قرارًا غير موجود في "قرارات محسومة" → قف واسأل.
   ممنوع تحويل الجهل إلى nullable/config/flag، وممنوع إعادة تفسير حقل قائم.
4. ممنوع "تم" بلا: بوابة مُفعَّلة + الفرع الشغّال مُختبَر.
5. عند الإغلاق: حدّث BACKLOG.md بالدليل (ملف/سطر). CLAUDE.md/AGENTS.md → يوسف فقط.
```

---

# ✅ قرارات محسومة — المرجع المُلزِم
### (صاحب العمل · 2026-07-12)
## السلسلة الحقيقية

```
1. 1. INSPECTION_REP: مقاسات + صور + كروكي · INSPECTION_MANAGER يوزّع ثم يعتمد (D-37).
   ❗ لا تحقّق على رسمة المكتب الفني (بوابة الاعتماد على المعاينة نفسها — D-37).
   ❗ لا بوابة اعتماد. لا تحقّق على رسمة المكتب الفني.
2. مهندس المكتب الفني (TECHNICAL_OFFICE): رسم + تسعير
3. مدير المكتب الهندسي — TEC_APPROVER
      · يعتمد الرسم · يعتمد التسعير · يُصدر أمر التصنيع
4. عرض السعر → العميل → موافقة → التزام تعاقدي
5. REVIEW — قسم الجودة والمراجعة الفنية
      · مطابقة 3: طلب العميل + مقاسات/رسم المعاينات + رسم المكتب الهندسي
      · يمنع تضارب رسومات الإدارات
      · ❗ هو الذي يعتمد أمر التصنيع → بعده يتحوّل لـ PROCUREMENT
6. PROCUREMENT: فحص المخزون → تجهيز / طلب تصنيع من المصنع
7. تصنيع → استلام → تخزين → تسجيل التكاليف
8. تركيب → بنود إضافية → صور وتقرير → متابعة رضا العميل
```

## خريطة الأدوار الملزِمة

| الدور | الحقيقة |
|---|---|
| `ADMIN` (عمرو) | فواتير · خصومات · سياسات · كل الصلاحيات. **خارج سلسلة اعتماد الرسومات تمامًا.** |
| `TEC_APPROVER` | **المدير التنفيذي = مدير المكتب الهندسي.** يعتمد الرسم + التسعير + **يُصدر أمر التصنيع**. |
| `REVIEW_QC` | **رقابة وجودة فقط.** يطابق الـ3 و**يعتمد أمر التصنيع**. **لا علاقة له بالتسعير ولا عروض الأسعار.** |
| `INSPECTION_MANAGER` (حسن بهاء) | يوزّع المعاينات · **يعتمدها قبل المكتب الفني (D-37)** · لا يُنشئ. 🔴 **بلا بوابة على رسومات المكتب الفني** (شيء آخر). ||
| `PROCUREMENT_MANAGER` | ينفّذ الأمر **بعد** اعتماد REVIEW: مخزون · مصنع · تكاليف. |

## تصحيحات جوهرية



| # | التصحيح |
|---|---|
| **D-33** | **"خارج القاهرة" في المعاينة ≠ بند نقل إلزامي في التسعير — والانفصال مقصود.** المعاينة **مجانية** (لا رسوم). بند `OUT_OF_CAIRO_TRANSPORT` بند تسعير اختياري يضيفه `TECHNICAL_OFFICE` **حسب الحالة** — النقل لا يُحمَّل على العميل دائمًا. **لا تنبيه، لا حجب، لا اشتقاق آلي.** (يوسف، 2026-07-14 — يقفل BL-92) |
| **D-34** | **ADMIN يطلب خصمًا ويقرّره — مسموح، لكن مُوسَّم.** `DISCOUNT_ROLES` يشمل ADMIN، و`decideDiscountAction` بيده وحده → قرار ذاتي ممكن. **لا مَنع** (ADMIN صاحب القرار — D-19: "ADMIN يقرر النهائي على كل خصم"). **الضابط = الأثر والرؤية لا المنع** (D-11/D-24): حين `requestedById === actorId` يُوسَم `ActivityLog` صراحةً **"قرار ذاتي — ADMIN"** فلا يبدو السجل كأنه شخصان. **الاستثناء الوحيد لمنع اعتماد الذات يبقى SALES_MANAGER** (`cannotApproveSelf` — D-20، `discount.ts:163`). (يوسف، 2026-07-14) |
| **D-37** | 🔴 **مدير المعاينات يوزّع ويعتمد — لا يُنشئ.** (يوسف، 2026-07-14 — يُلغي "المدير للاستثناء" في D-31 ويقفل BL-104.) **السلسلة:** المبيعات تطلب → `INSPECTION_MANAGER` يوزّع → `INSPECTION_REP` يعاين ويحفظ → تنتقل تلقائيًا للمدير → يعتمد → المكتب الهندسي. **التنفيذ:** `CREATE_ROLES = ["SALES_REP","SALES_MANAGER","ADMIN"]` (`inspections/actions.ts:31`) — حُذف `INSPECTION_MANAGER`. **حارس شاشة العميل لم يُمَس** (`customers/[id]/page.tsx:15` — بلا INSPECTION_MANAGER، وهذا صار متسقًا لا فجوة). الجدولة/التعيين تبقى `MANAGER_ROLES`. **يُلغي D-05.** بيانات العميل الظاهرة للمدير في شاشة المعاينات (اسم/هاتف/عنوان/الطلب) كافية — لا يحتاج ملف العميل الكامل. بوابة الاعتماد تُبنى في المرحلة 2 (SCR-018 schema جاهز: approvalStatus/approvedById/approvedAt/returnReason). ⚠️ **بقية السلسلة (الانتقال التلقائي للمدير + اعتماده) غير مبنية** — انظر BL-106. |
| **D-01** | **"المدير التنفيذي" ≠ ADMIN (الرئيس التنفيذي).** في لغة الشركة = **مدير المكتب الهندسي** (TEC_APPROVER). |
| **D-02** | **ADMIN خارج سلسلة اعتماد الرسومات** — بوابة CEO كانت **اختراعًا**. |
| **D-03** | **REVIEW لا علاقة له بالتسعير ولا عروض الأسعار.** |
| **D-04** | **REVIEW يعتمد أمر التصنيع** — بعد إصداره من المكتب الهندسي (TEC_APPROVER)، وقبل تحويله لـ PROCUREMENT. |
| **D-05** | ~~INSPECTION_MANAGER بلا بوابة اعتماد~~ 🔴 **مُصحَّح بـD-37 (2026-07-14): المدير يعتمد فعلًا.** السلسلة: المبيعات تطلب → المدير يوزّع → المندوب يعاين ويحفظ → **تنتقل تلقائيًا للمدير → يعتمد** → المكتب الهندسي. القرار الأصلي بُني على فهم ناقص لدور المعاينات. |
| **D-06** | **التسعير مساران:** (A) داخل القائمة → Price List · (B) **خارج القائمة → طلب تسعير من المصنع** (والإكسسوارات نفس المعاملة). |
| **D-07** | **النظام يتتبع رصيد الكميات** (هدف). **مرحليًا:** تكاليف على المشاريع فقط. |
| **D-08** | **مسار السوشيال: عرض السعر بعد موافقة العميل = العقد.** لا سجل `Contract` منفصل. |
| **D-09** | **النظام لا يقارن الرسومات آليًا** — المقارنة **دور REVIEW الفعلي**. النظام: يعرض الـ3 · يُجبر التأكيد الصريح · يسجّل الأثر · يحجب الاعتماد قبل اكتمال التأكيدات. |
| **D-10** | **الدفع شرط قبل التصنيع — في المسارين.** النسبة تختلف حسب الطلب. **لا حد أدنى للمقدم:** خطة الدفعات حرة لكل عقد. |
| **D-11** | **الضابط = فصل الأدوار + الأثر + الرؤية، لا رقم.** المندوب يتفاوض · الحسابات تسجّل. |
| **D-12** | **اختيار المصنع + التاريخ المتوقع = PROCUREMENT، لا REVIEW.** REVIEW يطابق ويعتمد فقط. (يحسم BL-33) |
| **D-13** | **موافقة عميل السوشيال = تحويل الدفعة نفسها.** لا حقل موافقة منفصل. المبيعات تتلقى صورة التحويل (واتساب)، لكن **الفعل النظامي المُلزِم = تسجيل الحسابات للدفعة.** |
| **D-14** | **عقد السوشيال يُنشأ تلقائيًا عند تسجيل الحسابات لأول دفعة على العرض.** المبيعات لا تُنشئه. |
| **D-32** | **طلب المعاينة عبر-المندوب = تغطية مسموحة بحوكمة (يحسم BL-93، 2026-07-14).** لا حارس ملكية على `createInspectionAction`/`getSelectableRequests` — التغطية واقع تشغيلي (إجازة/غياب)، نموذج R-02 لا نموذج `changeCustomerStage`. **الضابط = الأثر والرؤية (D-11/D-24):** الفاعل الحقيقي في `ActivityLog` (INSPECTION_CREATED)، **+ إشعار لمالك العميل** (`notifications.inspectionByColleagueTitle`). 🔴 **نطاق الإشعار مُقرّ صراحةً (يوسف): أي فاعل غير المالك — لا يُضيَّق لـ`SALES_REP`.** السبب: **المالك يستحق أن يعرف أن أحدًا لمس عميله، أيًا كان** (ADMIN/مدير معاينات/مندوب مغطٍّ). الشرط في الكود = `ownerId && ownerId !== actorId` (`services/inspections.ts`). |
| **D-31** | **طلب المعاينة يدوي من شاشة العميل، والمندوب يختار `QuotationRequest` صراحةً — النظام لا يخمّن (يحسم BL-91، 2026-07-14).** الطلبات المعروضة: نفس العميل · غير مربوطة · غير DONE. المسار (SOCIAL/PROJECTS) غير مُقيِّد. الجدولة/التعيين تبقى بيد `INSPECTION_MANAGER`. الـ13 معاينة اليتيمة: رصد فقط، لا تُرمَّم (STD-14). |
| **D-30** | **`evidenceNotes` تُقفل بعد `JUDGED`.** الحُكم استند إليها — تعديلها بعده يُفرغ الأثر من معناه. نفس مبدأ تجميد العقد بعد الاعتماد. (2026-07-13) |
| **D-29** | **أمر التصنيع البديل: `TEC_APPROVER` يُصدره** (كأي أمر) · `REVIEW` يطابق ويعتمد. **لا استثناء من المطابقة** — أمر بلا مطابقة = أمر بلا أثر = تحقيق مستحيل. حكم `REVIEW` هو الإشارة التي تفتح الإصدار، لا الإصدار نفسه. |
| **D-28** | **تصحيح الرسمة الخاطئة = `TECHNICAL_OFFICE` دائمًا** (سواء أُعيدت المعاينة أو صُحِّح الرقم) → `TEC_APPROVER` يعتمد. |
| **D-27** | **الادعاء والحُكم حقلان منفصلان.** الحُكم لا يمسح ادعاء `INSTALLATIONS` — تشخيص خاطئ متكرر معلومة قائمة بذاتها. |
| **D-26** | **الحُكم = فئة العطل (`FaultType`) لا اسم شخص.** الأثر يقول مَن رسم ومَن اعتمد. الحُكم سجل — التكلفة والمسؤولية قرار منفصل، قد يتغيّر لاحقًا. |
| **D-25** | **التحقيق في البديل: `REVIEW` يجمّع الأثر · `ADMIN` يحكم.** REVIEW طرف في السلسلة (أكّدت المطابقة) فلا تحكم على نفسها. |
| **D-24** | **إلغاء الفاتورة: ACCOUNTING تلغي — بسبب إلزامي مكتوب + إشعار فوري لـ ADMIN.** الضابط = الأثر والرؤية لا المنع (D-11). الإلغاء لا يُرسل شيئًا للعميل — يسحب. منعه يولّد مسارًا موازيًا خارج النظام، وذلك أخطر من الغلط نفسه. |
| **D-23** | **الخصم له مدخلان مشروعان، وكلاهما يمر بنفس السلسلة (D-19):** • عند إنشاء العرض (خصم قائم/موسمي). • على عرض قائم مُرسَل — العميل يعترض فيطلب SALES_REP خصمًا (هذا المدخل غير موصول بأي واجهة — `requestDiscountAction` يتيم). |
| **D-22** | **خريطة الاعتماد (مقفلة):** • `TEC_APPROVER` (المدير التنفيذي — موظف): الرسم · التسعير · إصدار أمر التصنيع. • `REVIEW` (الجودة): المطابقة الثلاثية · اعتماد أمر التصنيع. • `ADMIN` (CEO): أي خصم · الفاتورة قبل إصدارها للعميل. "اعتماد العرض النهائي" = اعتماد الخصم عليه، ليست بوابة منفصلة. |
| **D-21** | **ACCOUNTING تجهّز الفاتورة (مسودة). ADMIN وحده يصدرها — الإصدار هو الاعتماد.** بعده: الرقم يتجمّد وتذهب للعميل. |
| **D-20** | **SALES_MANAGER لا يعتمد خصمًا طلبه هو** (نفس مبدأ منع اعتماد الذات في الرسومات). قد يشغل الشخص دورين. |
| **D-19** | **سلسلة الخصم:** • SALES_REP يطلب نسبة — لا يفرضها. لا خصم بلا سجل. • SALES_MANAGER يعتمد ويمرّر — لا يرفض لتجاوز سقفه، ولا ينهي السلسلة. • **ADMIN يقرر النهائي على كل خصم — أي نسبة، في المسارين.** • `discountBasePct` (18) = **سقف تفاوض SALES_MANAGER**؛ فوقه → الطلب موسوم "طلب خصم استثنائي". • `discountMaxReqPct` (25) = **سقف صلب مطلق**: لا يُنشأ عرض بخصم أعلى؛ **يبقى كما هو** (`pricing/actions.ts:261`). • بعد قرار ADMIN → إشعار SALES_REP ليبلّغ العميل. |
| **D-18** | **مسار البديل: لا أمر تصنيع تلقائي — في كل الحالات الأربع.** INSTALLATIONS (المدير) يسجّل الطلب + السبب → يبلّغ REVIEW → REVIEW يفتح تحقيقًا (السبب + المتسبب: فني/مصنع) → بعد تعديل الغلط، REVIEW يطلب من PROCUREMENT تنفيذ أمر تصنيع جديد. |
| **D-17** | **الجدولة (SCHEDULED للمعاينة/التركيب) = صنف ثالث: عمل حقيقي بتنسيق خارجي.** تبقى يدوية. **D-16 لا يشملها** (ليست اعتمادًا ولا واقعة مادية مُثبَتة). |
| **D-16** | **الاعتماد ينقل تلقائيًا.** زر الاعتماد = انتقال فوري للمرحلة التالية + إشعار صاحب الدور التالي. **لا خطوة "أرسل للتالي" يدوية بعد أي اعتماد.** الاستثناء: **تسجيل واقعة مادية** (وصل من المصنع · تم التركيب) يبقى يدويًا — إثبات حدث في الواقع، لا قرار. أتمتته = بيانات كاذبة. |
| **D-15** | **فرق تنظيم ذاتي: كل إدارة تملك فعلها ولا تنوب عن غيرها.** المندوب لا يُنشئ عقدًا بلا مال — شغل الحسابات. **ADMIN لا يُستثنى من قواعد سلامة البيانات:** الاستثناء يخصّ القرار الإداري، لا صحة السجل. عقد سوشيال بلا دفعة = مستحقات كاذبة، لا صلاحية. |
| **BL-148** | 🟡 **KPIs لأداء مندوبي المعاينة** (عدد المعاينات · الالتزام بالـSLA · التأخير). صاحب العمل: "مبدأ KPIs". غير حاجب — بند تقارير منفصل. | يوسف |
| **D-38** | **المقاسات تُقفل بعد اعتماد المدير (2026-07-14).** قبل `APPROVED`: `INSPECTION_REP` يضيف/يحذف/يصحّح بحرية — الحذف تصحيح لا خطر، **بلا إشعار** (الإشعار عند التسجيل فقط). بعد `APPROVED`: **صفر صلاحية حذف أو تعديل — لأي دور**. السبب: المدير اعتمد على الأرقام، والمكتب الفني سعّر عليها؛ تغييرها بعدها يُفرغ الاعتماد والتسعير من معناهما (نفس مبدأ D-30). **تفاصيل المعاينة والاعتماد تظهر في ملف العميل كبيان لحالة الطلب.** |
| **D-39** | **توحيد sendNotification: بالع + تسجيل، والإشعار دائمًا خارج الـtransaction (يحسم U-2، 2026-07-15).** النسخة النافذة = **بالعة** (فشل الإشعار لا يوقف عملية) **+ تسجيل الفشل في ActivityLog** بفعل `NOTIFICATION_FAILED` + systemActor (توسيع النمط القائم NOTIFICATION_ZERO_RECIPIENTS، send.ts:35-56) — فلا تختفي أعطال (6 إشعارات ACTION-REQUIRED تفشل اليوم صامتة بلا أثر). 🔴 **الاستثناء القاتل:** `contract-core.ts:106` (notifyRole ACCOUNTING) الوحيد داخل $transaction (addPayment→tx) — الرمي هناك يُلغي الدفعة+العقد. يُنقل **خارج** الـtx (بعد تأكيد العقد، في try/catch — نمط inspection-measurements). نسخة الجذر البالعة تُحذف، الكل يوجَّه لـsrc الموحّدة (بعد جعلها بالعة+تسجيل).✅ **مُنفَّذ (commit 23ebe91):** النسختان مُوحَّدتان على src (بالع+تسجيل)، إشعار العقد خارج الـtx (accounting/actions + contract-core)، نسخة الجذر محذوفة، الـ3 مستوردون موجَّهون لـsrc. مُثبَت بناقدين مستقلين: صفر إشعار داخل tx · rollback آمن (الإشعار داخل catch يُتخطّى) · صفر مستدعٍ يعتمد على الرمي. |
| **D-40** | **بوابة اعتماد المعاينة — التدفق الكامل (يحسم BL-109، 2026-07-16).** بُعدان منفصلان: (تتبّع الطلب) حفظ المقاس يحرّك حالة QuotationRequest تلقائيًا (recomputeLinkedRequest — قائم) ليرى الجميع أين وصل الطلب. (بوابة الاعتماد) approvalStatus منفصل: REP يسجّل بحرية (يبقى DRAFT/RETURNED)، ثم يضغط "قدّم للاعتماد" → PENDING_APPROVAL. لا انتقال تلقائي للبوابة عند الحفظ — يحمي من اعتماد مقاسات ناقصة (D-38 يقفلها كاملة). · (2) إشعار "للعلم" لمالك العميل + المبيعات فقط — **المكتب الفني لا يُخطَر بعد** (يُقفَل المساران المتخطّيان: addMeasurement:128 + updateInspectionStatus:431). المدير: **يعتمد** (`APPROVED` + approvedById/At → **عندها** يُخطَر TECHNICAL_OFFICE "جاهزة لإعادة التسعير" — D-37) **أو يُرجِع** (`RETURNED` + returnReason → إشعار REP بالسبب، المقاسات **تبقى مفتوحة** للتصحيح). بعد `APPROVED`: المقاسات **تُقفَل** لأي دور (D-38/BL-110). الإرجاع بعد الاعتماد غير مسموح (الاعتماد نهائي — أي تعديل بعده addendum مستقبلي). |
| **D-41** | **شاشة REVIEW لأوامر التصنيع — الوصول + إخفاء المال (يحسم BL-113، 2026-07-16).** REVIEW له بند nav خاص "مراجعة أوامر التصنيع" → قائمة **مفلترة على `UNDER_REVIEW` فقط** (شغله الفعلي — لا الـ14 أمرًا؛ توسيع النطاق قرار تيم لاحق). 🔴 **إلزامي (D-03):** عمود `unitCost` في ExtraItemsPanel **يُخفى عن REVIEW** — REVIEW جودة لا تسعير، لا يرى أي رقم مالي. IDOR غير قائم (أمر التصنيع بلا assigneeId — سلطة REVIEW على مستوى الدور بالحالة لا الملكية). التتابع: **B2 قبل BL-86** (B2 يفتح الوصول، BL-86 يقوّي البوابة داخلها — نفس الملفين: manufacturing/[id]/page.tsx + mfg-review.ts). |
| **D-42** | **قرار: دور "ADMIN" في الكود يحمل معنيين حسب السياق** — أحيانًا مدير نظام تقني، وأحيانًا "الإدارة العليا/CEO" في سياقات القرار النهائي (كما في D-19 اعتماد الخصومات). لا يوجد role مخصص باسم CEO حاليًا. "المدير التنفيذي" في Business_Process_Flowchart.xlsx = مدير المكتب الفني (TECHNICAL_OFFICE)، وليس الـCEO. الـCEO الفعلي (عمرو فاروق) منوط تحديدًا بالاعتمادات النهائية وموافقة الخصومات — مؤكَّد ومطابق لـD-19 الموجود بالفعل. قرار يوسف، 2026-07-24. | يوسف |
| **D-43** | **قرار: "المراجعة والتدقيق" (دور REVIEW) و"مكتب الجودة" كيان واحد**، ليسا قسمين منفصلين. النطاق: (1) المطابقة الفنية الثلاثية قبل التصنيع (رسمة + طلب عميل + رسمة معاينة) + (2) متابعة رضا العملاء بعد البيع والتركيب. قرار يوسف، 2026-07-24. | يوسف |
| **BL-149** | 🟡 **فجوة: ميزة "التفويض" غير موجودة في الكود إطلاقًا** (تأكيد بحث شامل). المواصفة من صاحب العمل: المدير (لا الموظف الغائب) يفوّض من يكمل العمل، عبر كل الأقسام (لا المبيعات فقط)، مع سجل تدقيق (history log) كامل للتفويض. يحتاج جلسة تصميم مخصصة، ليس إصلاحًا سريعًا. | يوسف |
| **BL-150** | 🟡 **فجوة: سلسلة اعتماد الرسومات (DrawingStatus schema) مصمَّمة لـ4 مراحل** (TEC_APPROVED→INS_VERIFIED→CEO_APPROVED→RELEASED_TO_FACTORY) لكن 3 منها بلا أي تنفيذ كودي مؤكَّد ببحث شامل (CEO_APPROVED وINS_VERIFIED لا تُكتَبان أبدًا في المشروع). أمر التصنيع حاليًا يُنشأ مباشرة بعد TEC_APPROVED فقط. هذا هو الجوهر الحقيقي خلف سؤال D-02 الأصلي — يحتاج تصميم كامل للترتيب ودور مكتب الجودة فيه، وليس قرارًا سريعًا. | يوسف |
| **BL-151** | 🟡 **`updateQuotation` يُفقد `costSnapshot` (TO-05/SCR-020).** الدالة تحذف بنود العرض وتعيد إنشاءها (`lib/pricing/actions.ts` — `deleteMany` ثم `create`) من `{description, quantity, unitPrice}` فقط، **بلا سياق وصفة** ⇒ البند المعدَّل يعود بـ`costSnapshot=null`. **هذا هو السلوك الصحيح لا عيبًا في التنفيذ:** الشاشة في وضع التعديل تحرّر السعر يدويًا بلا أبعاد ولا خامات، فلا توجد تكلفة كتالوج يمكن اشتقاقها — و`null` («غير معروفة») أصدق من ترحيل رقم قديم لا يخصّ السعر الجديد. **المطلوب لاحقًا:** إمّا مسار تعديل يحتفظ بسياق الوصفة، أو قرار صريح بأن العرض المعدَّل يدويًا خارج حساب الهامش. لا يُبنى الآن (L-14). | يوسف |
| **TO-45** | 🟡 **صلاحيات مرنة يضبطها الأدمن لمؤشرات الداشبورد (طلب المالك، رُصد في TO-08).** اليوم رؤية المالية (`costSnapshot` وكل اشتقاق منه: القيمة · التكلفة · الهامش) محكومة بـ**الدور وحده** — `ADMIN` فقط (`canSeeMoneyFor` في `src/lib/services/tec-dashboard.ts`)، والقصّ يتم **server-side قبل الإرسال** لا إخفاءً في الواجهة. المالك يريد لاحقًا أن يضبط بنفسه من يرى ماذا. 🔴 **لم يُبنَ عمدًا في TO-08** (أمر صريح: «لا تبنِ محرّك صلاحيات هنا»): محرّك صلاحيات حقيقي = نموذج تخزين + شاشة إدارة + نقطة فرض واحدة، وبناؤه ضمنًا داخل موجة داشبورد كان سينتج نصف محرّك يصعب اقتلاعه. **يحتاج تكليفًا منفصلًا وقرار تصميم** (هل الوحدة «مؤشر» أم «حقل» أم «شاشة»؟). | يوسف |
| **BL-155** | 🟡 **تنسيق جدول المكتب الفني — لم أجد الفرق المقصود، ورصدت خرقًا في ملف آخر (TO-28).** طُلب توحيد «تنسيق عرض النصوص» في جدول `/technical-office` مع باقي الجداول، وفحصتُ المحاور القابلة للقياس فوجدت `tec-client.tsx` **مطابقًا** لا مخالفًا: صفر خاصية فيزيائية (`ml-/mr-/pl-/pr-`)، وتنسيق تاريخ مطابق (`Intl.DateTimeFormat("ar-EG")` بنفس الخيارات كما في `quotations-client.tsx`). ⇒ الفرق المقصود بصري ولم أستطع تحديده من الكود، ويحتاج وصفًا محددًا من المالك (أي عمود؟ محاذاة/حجم خط/التفاف؟) — **لم أغيّر شيئًا بصريًا بالتخمين** (قاعدة «تحقّق بعيني قبل الـcommit»). 🔴 **لكن الفحص كشف خرقًا حقيقيًا في ملف آخر:** `src/app/(dashboard)/users/users-client.tsx` يستعمل خصائص فيزيائية بدل المنطقية — خرق صريح لقاعدة RTL في CLAUDE.md §6 / Definition of Done. **لم يُنفَّذ:** خارج ملف TO-28 (القيد الصريح في التكليف). ✅ **شقّ users-client أُغلق في TO-32** (`mr-2` → `ms-2` — الموضع الوحيد فيه، مُتحقَّق بإعادة المسح). الشقّ الأول (وصف الفرق البصري في جدول المكتب الفني) يبقى معلّقًا على وصف المالك. ⚠️ **رصد TO-32 خارج نطاقه** (للتسجيل لا التنفيذ): خصائص فيزيائية في ~12 ملفًا آخر أبرزها `components/ui/*` (badge · button · select · table · dropdown-menu) و`layout.tsx` و`review-detail` و`executive` — تستحق مسحة موحّدة منفصلة. | يوسف |
| **BL-154** | 🟡 **رسائل zod الخام في `roleEnum` و`departmentEnum` تصل المستخدم نصًا إنجليزيًا (سابقة لموجة TO-23، رُصدت أثناء TO-23-B).** `src/app/(dashboard)/users/actions.ts` — الحقلان `z.enum([...])` بلا `errorMap`، فقيمة غير صالحة تُنتج `"Invalid enum value. Expected 'ADMIN' \| 'SALES_MANAGER' \| … , received 'NOT_A_ROLE'"`. الواجهة (`users-client.tsx`) تمرّر رسالة الخطأ إلى `t()` ⇒ المفتاح غير موجود فيظهر النص الخام كما هو — يخرق قاعدة `t()` الإلزامية (CLAUDE.md §6) ويسرّب أسماء الأدوار الداخلية. **مُثبَت runtime:** حمولة بـ`role="NOT_A_ROLE"` أخرجت النص أعلاه حرفيًا. الإصلاح = `errorMap: () => ({ message: "errors.…" })` على الحقلين (نفس ما طُبِّق على `leadRouteEnum` في TO-23-B، `actions.ts:24`). **لم يُنفَّذ عمدًا — خارج نطاق TO-23-B/C (L-14).** ⚠️ الأرجح أن النمط نفسه يتكرر في كل `z.enum` بلا `errorMap` في المشروع — يستحق مسحًا شاملًا لا إصلاحًا موضعيًا. | يوسف |
| **BL-153** | 🟠 **`costSnapshot` قد يخرج «ناقصًا» بلا null وبلا استثناء (رُصد أثناء تحقق TO-21 runtime).** `selectRecipeLines` (`lib/pricing/recipe-selection.ts:57-63`) يتخطّى بصمت أي فئة اختيارها لا يطابق أي سطر — فلو تغيّرت خامة بين لحظة الحساب في الواجهة ولحظة الحفظ، تُحسب التكلفة من أسطر **أقل** من الأسطر التي بُني عليها `unitPrice` ⇒ **تكلفة أقل من الحقيقة وهامش مبالغ فيه، ورقم يبدو سليمًا تمامًا**. مُثبَت runtime: حمولة SHOWER باختيارات لخامات غير موجودة أعادت `190` (سطر السيليكون التلقائي وحده) بدل `null`. الفرع `NO_SELECTED_LINES` **لا يمسكها** لأنه يشترط صفر أسطر، وأي فئة بخيار واحد تُضم تلقائيًا. **TO-21 لم يغيّر السلوك** (قاعدة الاختيار مشتركة مع الواجهة والسعر يتبعها — تغييرها = تغيير تسعير، STD-05) بل **أنطقها فقط**: `COST_SNAPSHOT_PARTIAL … SELECTION_NOT_MATCHED` (`lib/pricing/actions.ts`). **القرار عند يوسف:** هل يصير هذا الفرع `null` (تكلفة غير معروفة) أم يبقى تحذيرًا؟ | يوسف |
| **BL-152** | 🟠 **`QuotationItem.unitPrice` مقبول من الكلاينت بلا إعادة اشتقاق (رُصد أثناء TO-05، خارج نطاقه).** الإجماليات تُعاد اشتقاقها server-side (RR-1) لكن **سعر البند نفسه** يصل من الواجهة كرقم جاهز (`quotation-builder.tsx` → `createQuotation`)، لأن اختيار الخامة لكل فئة قرار كلاينت لا يُحفظ في أي مكان. TO-05 لم يغيّر هذا (تسجيل فقط، بأمر صريح) لكنه **جعله قابلًا للكشف**: بعد نزول العمود يمكن مقارنة `unitPrice` بـ`costSnapshot` وضبط أي بند سعره لا يتّسق مع تكلفته. **القرار (هل يُعاد اشتقاق `unitPrice` server-side؟) عند يوسف** — تغييره = تغيير سلوك تسعير، لا يُجتهد فيه (STD-05). | يوسف |
| **BL-Y200** | — بذور بيانات اختبار قديمة تنتج حالات غير طبيعية
الوصف: صفوف من مراحل تطوير مبكرة (بلا عقود/عروض متسقة) تنتج مراحل
        عميل ونتائج اشتقاق صحيحة تقنيًا لكنها غير قابلة للتفسير عمليًا.
الأثر: يصعّب اختبار القبول (رقم صحيح، لكن السياق مربك).
الحل المقترح: عند الانتقال لبيئة UAT، تنظيف بيانات الاختبار قبل الاستيراد
        النهائي — لا إصلاح كود.
---

# 🚨 P0 — أمني

| # | البند | الدليل | دليل الإغلاق |
|---|---|---|---|
| **BL-47** | 🔴 **لا مسار break-glass للأدمن.** المستخدم العادي ينساها → ADMIN يعيد ضبطها من `/users`. لكن **ADMIN نفسه = طريق مسدود** (لا استرجاع، لا seed لـ `admin@egyglass.net`). أثبتته الواقعة. **شدّته معلّقة على BL-42:** لو شاشة المستخدمين مكسورة → لا استرجاع لأحد. **جزئيًا مُخفَّف بـ BL-48 (سكريبت break-glass).** | يوسف |
| **BL-01** | ✅ **مُغلق (PHASE 2، 2026-07-12).** حارس `requireRole(["TEC_APPROVER","ADMIN"])` مُضاف — `lib/manufacturing/actions.ts:45`. مُثبت: SALES_REP → `notAuthorized` بجلسة حقيقية. | مُغلق |
| **BL-112** | 🔴 **[P0-أمني] IDOR في تقديم الملفات:** `/uploads/[...path]/route.ts` يقدّم ملفات `public/` **بلا مصادقة** — أي شخص يفتح صورة معاينة/رسمة بالرابط المباشر بلا تسجيل دخول. **سابق لـ1ج** (نمط `uploadDrawingAction` القائم). الإصلاح النافذ: تخزين خارج web-root + handler مُصادَق + فحص `canWriteOnInspection` + Content-Disposition. مرتبط بـBL-40/BL-56. **تغيير معماري متعدد الوحدات — قرار يوسف (L-11).** | يوسف |

---

# 🔴 سلسلة الاعتماد — أدوار خاطئة وبوابات مخترَعة

> **السلسلة مش مكسورة — مسنودة لأدوار غلط. الإصلاح حذف وإعادة إسناد، مش إعادة بناء.**

| # | البند | الحالة في الكود | المطلوب |
|---|---|---|---|
| **BL-02** | ✅ **مُغلق (PHASE 1).** G3 مُعطَّلة — `ceoApproveDrawingAction` يرجع `gateRemoved` (`technical-office/actions.ts`) + حقلا العتبة أُزيلا من شاشة الإعدادات وzod. العمود/الـenum ميتان حتى حذف SCR (BL-20). | مُغلق |
| **BL-03** | ✅ **مُغلق (PHASE 1).** G2 مُعطَّلة — `verifyDrawingAction` يرجع `gateRemoved` + أزرار UI أُزيلت. مُثبت: الرسمة تقف عند `TEC_APPROVED`، INSPECTION_MANAGER بلا بوابة. | مُغلق |
| **BL-04** | ✅ **قائمة وصحيحة + تُوسّعت (PHASE 2).** G1 `TEC_APPROVER`، وشمل اعتماد التسعير عبر `/review` (نُقل إليه). | مُغلق |
| **BL-05** | ✅ **مُغلق (PHASE 2).** اعتماد العرض → `QUOTATION_APPROVAL_ROLES=["TEC_APPROVER","ADMIN"]` (`lib/review/actions.ts:10`) + نقل شاشة `/review`. مُثبت: REVIEW → `notAuthorized`، TEC_APPROVER → success. | مُغلق |
| **BL-06** | ✅ **مُغلق (PHASE 2).** التوليد التلقائي حُذف من `approveQuotationAction` (`lib/review/actions.ts:143`). مُثبت: الاعتماد `mo_count=0`. | مُغلق |
| **BL-07** | ✅ **مُغلق (PHASE 3).** البوابة → `["REVIEW","ADMIN"]` + عرض الأضلاع الثلاثة + 3 تأكيدات صريحة + حجب server-side (`errors.matchIncomplete`) + ActivityLog لكل تأكيد. `mfg-review.ts` + `manufacturing/[id]/review-panel.tsx`. مُثبت بالجلسات (D-09). | مُغلق |
| **BL-08** | ✅ **مُغلق (PHASE 2).** `createManufacturingOrder` يرفض بلا رسمة `TEC_APPROVED` على الطلب (`errors.noApprovedDrawing`) — `manufacturing/actions.ts:45`. مُثبت بجلسة. | مُغلق |
| **BL-09** | **`NULL` = fail-open في كل إعداد حوكمي** | نمط عام | القاعدة: `NULL` = **الضابط مُفعَّل**، لا مُتخطّى (البوابتان الحوكميتان NULL أُزيلتا في PHASE 1؛ المبدأ قائم لأي إعداد قادم) |

---
### ملاحظات إدارة المبيعات — سجل الرصد الميداني
### EgyGlass ERP · Sales Findings Log

**التاريخ:** 2026-07-25
**المصدر:** فحص بشري ميداني مباشر (UAT) — أكونت "مندوب مبيعات" + أكونت "مدير مبيعات"
**الترميز:** `SF-xx` (Sales Finding) — **تسلسل مستقل تمامًا عن ترميز الـ Backlog البرمجي `BL-xx`** الصادر عن تدقيق الكود الثابت، لتفادي تصادم الترقيم بين مصدرَي الاكتشاف (فحص بشري ↔ تدقيق كود).
**نطاق السجل:** ملاحظات إدارة المبيعات فقط. أي بند يتقاطع مع إدارة أخرى (مثل المشروعات) يُعلَّم صراحة في عمود "بوابة اعتماد" ولا يُغلَق فنيًا إلا عند اكتمال الطرفين.
**آخر تحديث حالة:** 2026-07-25 — Wave A وWave B مُقفلتان بالكامل (عدا SF-08 UTR وSF-14 كتوصية تنظيف لا تحجب الإغلاق). التفاصيل أدناه.

---

## 1) جدول ملاحظات إدارة المبيعات (Sales Findings Register)

| الكود | الدور المتأثر | الشاشة/المسار | الملاحظة كما وردت ميدانيًا | التصنيف | الخطورة | ربط بالكود (إن توفر) | الحالة |
|---|---|---|---|---|---|---|---|
| **SF-01** | مندوب مبيعات | `عروض الأسعار → إنشاء عرض سعر جديد` | الزر يفتح شاشة العملاء بدون تسجيل بيانات عرض السعر — المندوب لا يستطيع إنشاء/طلب عرض سعر فعليًا | UI Routing Defect | **حرجة** | `createQuotationRequestAction` موجود ومهيأ للمندوب [customers/actions.ts:100] لكن غير موصول بالزر | ✅ مُغلق — `930e109` (مكوّن NewQuotationRequestDialog، تسلسل عميل→طلب) |
| **SF-02** | مندوب مبيعات | شاشة العينات (غير موجودة) | مطلوب: تسجيل طلب عينة + متابعة رد إدارة العينات | Missing Feature | عالية | لا `model Sample` بالكود إطلاقًا (BL-57) | 🟡 مفتوح — Wave D (لم تبدأ) |
| **SF-03** | مندوب مبيعات | ملف العميل → تسجيل سبب الرفض | مطلوب: تسجيل سبب رفض العميل من المندوب نفسه + تقارير مجمّعة | RBAC Gap | عالية | `changeCustomerStage` مقفول على ADMIN فقط، فرع SALES_REP كود ميت | ✅ مُغلق — `dd9ce32`→`bab0221` (PR #3): REJECTED-only لغير ADMIN، SALES_MANAGER بلا قيد ownership، SALES_REP مقيّد بـownerId، تقرير مُجمّع `executive/actions.ts:196-211` |
| **SF-04** | مدير مبيعات | شاشة توزيع الطلبات | مطلوب: توزيع آلي على الموظفين + إمكانية التعديل في أي وقت + "توكيل لأقل الزملاء عملاء" | Missing Feature | عالية | لا يوجد محرك توزيع (`roundRobin/autoAssign`) بالمرة | 🟡 مفتوح — Wave D (لم تبدأ) |
| **SF-05** | مدير مبيعات | `/coverage-edits` (تعيين مسؤول التغطية) | تحويل عميل لموظف آخر بكامل الـ History — الشاشة موجودة لكن ناقصة | Functional Gap | متوسطة | `setCustomerCoverage` بلا إشعار للمستلم + لا تنقل الفواتير | ✅ مُغلق — `af84926`+`fff5973`→`78c2c6a` (PR #4): إشعار setCustomerCoverage + فواتير في getCustomerById |
| **SF-06** | مندوب + مدير | الشاشة الرئيسية (Dashboard) | تفتح على "عدد المعاينات المعلقة" بدل بيانات المبيعات المطلوبة لكل دور | IA / UX Defect | عالية | لم يُغطَّ بتدقيق الكود — يحتاج فحص منفصل لـ `/dashboard` | 🟡 مفتوح — Wave C (بوابة A أُغلقت؛ لم تبدأ) |
| **SF-07** | مدير مبيعات | الشاشة الرئيسية (تفصيل مطلوب) | مطلوب: عدد عقود منفَّذة/بانتظار، حالة عروض الأسعار، عدد عملاء لكل موظف، تصنيف عملاء (سوشيال/فردي/مشروعات) | Missing Aggregation | عالية | البيانات الخام موجودة (`Contract`, `Customer.ownerId/source`) — لا طبقة تجميع مبنية | 🟡 مفتوح — Wave C (لم تبدأ) |
| **SF-08** | مدير مبيعات | `/quotations/[id]/edit` — إضافة منتج | السعر يُحسب `0.0` بدل القيمة الصحيحة عند إضافة منتج لعرض قائم | **Data Integrity Bug** | **حرجة** | غير مغطى بتدقيق الكود الثابت (خطأ حسابي وقت التشغيل) — يغذي `Contract.totalValue` لاحقًا | 🔍 UTR (Unable To Reproduce) — **لم يُغلق كـ"محلول"**؛ تحقيق H1–H4 كامل بلا دليل قاطع على السبب الأصلي، والجذر المرجَّح عولج وقائيًا عبر SF-13 |
| **SF-09** | مدير مبيعات | `/coverage-edits` | تنسيق الأعمدة غير متطابق مع الصفوف | UI/CSS Defect | منخفضة | — | ✅ مُغلق — `1fb331b` (محاذاة coverage-edits) |
| **SF-10** | مدير مبيعات | الوصول العام للنظام | الوصول عبر IP داخلي (`192.168.8.22:3100`) بدون دليل TLS | Infrastructure/Security | تُحدَّد بعد التأكيد | خارج نطاق الكود — يتطلب تأكيد فريق البنية التحتية | 🟡 مفتوح — مسار بنية تحتية (فريق خارجي) |
| **SF-11** | مندوب مبيعات | السايدبار | تسمية "المبيعات" في السايدبار مضلِّلة — الشاشة فعليًا "العملاء" | Naming/UX | منخفضة | — | ✅ مُغلق — `f742968` (تسمية nav.customers) |
| **SF-12** | مدير/مندوب مبيعات | طلب عرض سعر (تدفّق W-01) | طلبات عروض الأسعار تذهب لـ`TECHNICAL_OFFICE` لا `SALES_MANAGER` — يخالف الملاحظة الميدانية عن "موافقة مدير المبيعات" | قرار عمل (يخص W-01) | — (قرار) | `notifyRole("TECHNICAL_OFFICE")` [quotation-requests.ts:52] | 📌 قرار عمل معلَّق — **ليس bug**؛ يحتاج قرار يوسف: مسار موافقة مزدوج أم التوجيه الفني الحالي صحيح والملاحظة غير دقيقة (مكتشَف أثناء SF-01) |
| **SF-13** | مدير/مندوب مبيعات | `/quotations/[id]/edit` | حارس `subtotal>0` مفقود في مسار edit (سمح بحفظ بند سعره 0) | Data Integrity Guard | حرجة | توحيد شرط edit مع create [quotation-builder.tsx] | ✅ مُغلق — `8563af9` (PR #1 `4f7b527`) |
| **SF-14** | — (سلامة بيانات) | `/quotations` (بيانات) | شذوذ: `QUO-2026-0020` إجمالي 17100 بصفر بنود — مصدره بذرة SQL لا مسار تطبيق | Data Anomaly (seed) | منخفضة (لا risk تشغيلي) | `seed_data.sql:261` | 🟡 مفتوح — توصية تنظيف بذرة، لم تُنفَّذ (الحُرّاس الحالية تمنع التكرار) |
| **SF-17** | كل الأدوار | `/customers` | محاذاة عمودي "الاسم"/"إجراءات" (حاوية `flex` لا تخضع لـ`text-center`) — كان مُرقَّمًا خطأً SF-16 على شاشة خاطئة | UI/CSS Defect | منخفضة | `customers-client.tsx:251,308` | ✅ مُغلق — `744df07` (PR #2 `3179a66`) |
| **SF-18** | مدير/مندوب مبيعات | ملف العميل → رفض العميل | أسباب الرفض تُخزَّن نصًا عربيًا مركّبًا (`"السعر مرتفع جداً"`) لا كودًا مُهيكلًا — يفسد دقة أي `groupBy` مستقبلي على تقرير SF-03 | Data Modeling | متوسطة | `stage-change-dialog.tsx:66-74` (موثَّق سلفًا `executive/actions.ts:177-181`) | 🟡 مفتوح — SCR مقترَح (عمود `rejectReasonCode`) — مكتشَف أثناء SF-03 |
| **SF-19** | كل الأدوار | جرس الإشعارات | الإشعارات تعرض مفتاح الترجمة الخام (`notification.title` بلا `t()`) — أثّر على SF-05 وكل الإشعارات القديمة دفعة واحدة | i18n Defect | متوسطة | `notifications-bell.tsx:84` | ✅ مُغلق — `b79cf97` (نقطة استهلاك واحدة) |

| **SF-20** | مندوب مبيعات | `/customers` + بروفايل العميل | تقييد `SALES_REP` على عملائه + مَن يغطّيهم — كان مرشَّحًا لفجوة "قراءة عملاء الزملاء" | RBAC (قرار) | — | `services/customers.ts:46-51` · `:275-282` | ✅ **مُغلق بقرار (يوسف، 2026-07-25) — بلا تعديل كود.** `ownerId OR coveredById` هو السلوك المطلوب فعليًا (عملاؤه + مَن يغطّيهم رسميًا، والباقي مقفول) — مؤكَّد ميدانيًا بعد استبعاد خطأ كاش. الإشعار عند تعيين التغطية قائم (SF-05). ⚠️ **إجراء متبقٍ على يوسف وحده (L-14):** تصحيح **CLAUDE.md §3** الذي يصف `SALES_REP` بأن له "قراءة عملاء الزملاء" — لا يطابق الكود ولا المطلوب |
| **SF-21** | كل الأدوار | بروفايل العميل | صف "آخر تعديل بواسطة" يعرض **اسم المالك/المغطّي** لا آخر مُعدِّل فعلي — تسمية مُضلِّلة. ✅ **التشخيص الأصلي مُتحقَّق مجددًا (C2-fix-2) وهو صحيح:** `customer-profile-client.tsx:242-243` يعرض حرفيًا `customer.ownerName \|\| customer.coveredByName`. 🔴 **والسبب أعمق مما بدا: `Customer` لا يملك عمود `updatedById` إطلاقًا** — لا في الschema ولا في القاعدة (`information_schema` أعاد `updatedAt` وحده؛ و`updatedById` في الschema يخصّ `Quotation:200` و`SystemSettings:540` و`PriceListItem:593`). ⇒ **العرض لم يكن يقرأ حقلًا خاطئًا، بل لا حقل أصلًا.** الإصلاح يحتاج SCR — [[BL-174]]. | Data/Honesty | منخفضة | `customer-profile-client.tsx:242-243` | 🟡 مفتوح — يحتاج عمودًا جديدًا (SCR) |
| **SF-22** | — (سلامة بيانات) | العقود / داشبورد المبيعات | 6 من 10 عقود بلا `signedAt` — بيانات بذرة سابقة لوجود الحقل في `contract-form`. **لا خلل تطبيقي** (الحقل يُكتب دائمًا الآن) | Data Hygiene | منخفضة | `services/sales-dashboard.ts` (تجميع العقود) · `contract-form.tsx:20-21` | 🟡 مفتوح — بعد تنظيف البذرة يصير التحويل إلى `signedAt IS NOT NULL` هو الأدق (تبديل سطر واحد). مكتشَف أثناء SF-07 |
| **SF-23** | مندوب + مدير | داشبورد المبيعات | البطاقات تعرض أرقامًا مجمَّعة فقط — لا نقر لعرض القائمة خلف الرقم (drill-through) | UX Enhancement | منخفضة | `dashboard/_components/sales-dashboard.tsx` | 🟡 مفتوح — تحسين لاحق لا يحجب شيئًا. 🔴 **شرطان يُفحصان قبل أي تنفيذ:** (1) هل فلاتر الشاشات الهدف (`/customers`, `/quotations`, `/inspections`) **URL-driven** أم حالة داخلية؟ الروابط العميقة لا تعمل مع الحالة الداخلية، وتحويلها شغل مستقل. (2) **إعادة فرض الحارس إلزامية على الشاشة الهدف** — الرقم المُجمَّع مُنطَّق، لكن القائمة التفصيلية تُنطَّق مستقلةً ولا تُبنى على افتراض تنطيق الرقم (**STD-15: الترشيح ليس حارسًا**). **التوصية:** روابط عميقة لشاشات قائمة بفلتر مُطبَّق، لا شاشات عرض جديدة تحتاج حراسًا جديدة تُختبر من الصفر |
| **SF-24** | مدير مبيعات | داشبورد المبيعات | بندان مترابطان: **(أ)** المؤشرات لحظية بلا اتجاه (أعلى/أقل من الفترة السابقة) — أقرب لحاجة طارق المعلَنة عن مراجعة التارجت · **(ب)** مؤشر **نسبة تحويل العروض إلى عقود** موجود في داشبورد ADMIN فقط وغائب عن داشبورد المبيعات، رغم أن مدير المبيعات أكثر مَن يحتاجه | Missing Aggregation | متوسطة | `lib/executive/actions.ts:87` (الحساب القائم) · `services/sales-dashboard.ts` | 🟡 مفتوح — تحسين لاحق. **(أ) أرخص من SF-23** (استعلام إضافي بلا مسألة حوكمة) ⇒ **أولوية أعلى** عند فتح جولة تحسين. **(ب)** يُضاف مع (أ) في نفس الجولة، بنفس تنطيق `getSalesDashboard` (فريق/عملاؤه) |
| **SF-25** | مكتب فني (المستقبِل) + مبيعات (المرسِل) | `/technical-office/[id]` | المكتب الفني يفتح الطلب فيرى اسم العميل · تسلسل الطلب · المسار · مسؤول المبيعات — **بلا بنود/تفاصيل ما يطلبه العميل**. على أي أساس يُسعِّر؟ | **Display Gap — بوابة مزدوجة** | **حرجة** | `services/tec.ts:112` (القائمة) و`:173` (التفصيل) **تُرجعان `summary`** · الشاشات الأربع (`tec-client.tsx` · `[id]/tec-detail-client.tsx` · صفحتاهما) **صفر عرض له** | 🔴 مفتوح — **التصنيف (ب) محسوم بالدليل: فجوة عرض بحتة لا فجوة تسجيل.** دليل رقمي: **20 من 21** طلبًا `summary` فيه غير فارغ (مثال حقيقي: «العميل محتاج كابينة شاور زجاج مصنفر عرض 1.5م × ارتفاع 2.5م»). ⚠️ **تحفّظ ثانوي:** `summary` **نص حر لا بنود مهيكلة** — التهيكل سؤال schema منفصل (SCR) **لا يحجب** إصلاح العرض الرخيص. الاحتمال (ج) مستبعد: مصدر التفاصيل هو الطلب لا المعاينة |
| **SF-26** | مبيعات + المكتب الفني | حالات عرض السعر | `QuotationStatus` **بلا أي حالة رفض** ⇒ النظام لا يفرّق بين عرض **رفضه العميل** وعرض **انتهت صلاحيته** (`EXPIRED`) — يفسد أي حساب لمعدل تحويل العروض (نفس مؤشر SF-24-ب) | **Data Modeling (SCR لاحق)** | عالية | `enum QuotationStatus {DRAFT,SENT,PENDING_APPROVAL,APPROVED,EXPIRED}` [schema.prisma:681-687] | 🔴 مفتوح — **القرار التصميمي (يوسف، 2026-07-25): حالتان لا واحدة.** `REVISION_REQUESTED` = يحتاج إعادة عمل، المسار **يبقى مفتوحًا** (أسبابه الأربعة: إعادة تسعير · خصم إضافي · خطأ في الطلبات · تعديل الرسم) · `REJECTED` = رفض نهائي من العميل، المسار **يُقفل**، مع نص حر إلزامي. 🔴 **درس SF-18 مُلزِم:** الأسباب **أكواد في enum مستقل** لا نصًّا عربيًا مركَّبًا (SF-18 أثبت أن المركَّب يفسد أي `groupBy`)، والنص الحر في **عمود منفصل** لا يُدمج مع الكود. 🔴 **سؤال يحجب البناء (STD-05):** `REVISION_REQUESTED` تعني إعادة عمل — إلى **مَن** يُوجَّه الطلب في كل سبب (إعادة تسعير/تعديل الرسم → المكتب الفني؟ · خصم إضافي → سلسلة D-19؟ · خطأ في الطلبات → المبيعات؟)، وهل يُعاد فتح العرض نفسه أم يُنشأ عرض جديد مرتبط به؟ **مسار عمل كامل يحتاج قرار يوسف/طارق قبل أي SCR.** ⚠️ **تقاطع يجب فحصه أولًا:** `reviewStatus` بُعد **الاعتماد الداخلي** وله `RETURNED` يُكتب في `lib/review/actions.ts:194` — بُعد مختلف عن رفض العميل، لكن يلزم فحص التقاطع قبل التصميم حتى لا يتضاعف مساران متشابهان |

> **ملاحظة ترقيم:** SF-12–SF-26 اكتُشفت أثناء تنفيذ الأمواج A/B/C (لا من الفحص الميداني الأصلي). **SF-15 غير مستخدم؛ SF-16 أُعيد ترقيمه إلى SF-17** (كان يشير لشاشة خاطئة — coverage-edits بدل customers).

---

## 2) خطة الإصلاح — تنفيذ متوازٍ حيث يسمح التصميم

### منهجية التسلسل

الترتيب أدناه **فني/معماري لا وظيفي فقط**: تم تجميع البنود في "أمواج تنفيذ" (Waves) بناءً على:
1. **استقلالية الملفات/الوحدات** (تفادي تعارض دمج الكود Merge Conflicts).
2. **تبعية سلامة البيانات** (لا يُبنى تجميع/تقرير على بيانات غير موثوقة).
3. **الحجم الهندسي** (ميزات جديدة كاملة تُفصل عن إصلاحات سريعة لتُدار بمسار تطوير مستقل دون حجب الإصلاحات العاجلة).

كل موجة تحوي بنودًا **قابلة للتنفيذ بالتوازي فيما بينها**. الانتقال بين الأمواج نفسها قد يتطلب تسلسلًا حيث مذكور صراحة.

---

### 🟢 الموجة A — إصلاحات عاجلة ومستقلة (توازي كامل، أولوية تنفيذ فورية)

| الكود | الإجراء | لماذا مستقلة |
|---|---|---|
| 🔍 **SF-08** | إصلاح منطق حساب الإجمالي عند إضافة منتج في `edit quotation` | ملف/منطق حسابي معزول، لا يتقاطع مع أي بند آخر |
| ✅ **SF-01** | إعادة توجيه زر "إنشاء عرض سعر" إلى `createQuotationRequestAction` | تعديل UI routing فقط، لا يمس الـ backend |
| ✅ **SF-09** | إصلاح تنسيق أعمدة `/coverage-edits` | CSS بحت |
| ✅ **SF-11** | تصحيح تسمية "المبيعات" → "العملاء" في السايدبار | نص واجهة فقط |

> **ملاحظة تسلسل حرجة:** **SF-08 يجب أن يُغلَق ويُختبر قبل بدء الموجة C**، لأن أي تجميع بيانات مالية في الداشبورد (SF-07) سيُبنى فوق قيم عروض الأسعار — تشغيل التجميع على بيانات مكسورة يُنتج KPI غير موثوق يتطلب إعادة بناء لاحقًا.

---

### 🟡 الموجة B — تعديلات RBAC وإشعارات على نفس الملف (توازي داخلي، تسلسل مع الملف المشترك)

| الكود | الإجراء | ملاحظة تنسيق |
|---|---|---|
| ✅ **SF-03** | فتح صلاحية `changeCustomerStage` لدور `SALES_REP` + تفعيل الفرع الميت + بناء تقرير رفض مُجمّع | يعمل على `src/lib/actions/customers.ts` |
| ✅ **SF-05** | إضافة `sendNotification` عند `setCustomerCoverage` + توسيع `getCustomerById` لتشمل الفواتير | نفس الملف `customers.ts` |

> **توصية تنفيذ:** نظرًا لأن SF-03 وSF-05 يعدّلان نفس الملف (`customers.ts`)، يُنفَّذان بواسطة نفس المطور/فرع Git (branch) تفاديًا لتعارض الدمج — لكنهما لا يعتمدان تقنيًا على أي موجة أخرى، ويمكن أن يبدآ فورًا بالتوازي مع الموجة A.

---

### 🟠 الموجة C — الداشبورد المخصص بالدور (تعتمد على إغلاق SF-08 من الموجة A)

| الكود | الإجراء |
|---|---|
| **SF-06** | إعادة تصميم الصفحة الرئيسية لتكون Role-aware (بيانات مختلفة لكل دور) بدل شاشة المعاينات المشتركة |
| **SF-07** | بناء طبقة تجميع (Aggregation Layer) جديدة: عقود منفَّذة/بانتظار، حالة عروض الأسعار، عدد عملاء/موظف، تصنيف العملاء |

> **بوابة الدخول:** لا يبدأ التنفيذ الفعلي لهذه الموجة قبل تأكيد إغلاق SF-08 (اختبار Regression على الأقل لعينة عروض أسعار). التصميم (Design/Wireframe) يمكن أن يبدأ بالتوازي مع الموجة A وB دون انتظار.

---

### 🔵 الموجة D — بناء ميزات جديدة كاملة (تصميم متوازٍ فورًا، تطوير مستقل طويل المدى)

| الكود | الإجراء |
|---|---|
| **SF-02** | وحدة العينات الكاملة: `Sample model` + Actions + شاشة طلب/متابعة |
| **SF-04** | محرك توزيع آلي (Round-robin أو Least-loaded) + شاشة توزيع قابلة للتعديل اليدوي |

> هاتان الوحدتان **مستقلتان تمامًا معماريًا** عن باقي الأمواج (schema جديد، لا تعديل على مسارات موجودة) — يمكن أن يبدأ فريق منفصل تصميمها وتطويرها **بالتوازي الكامل** مع الأمواج A/B/C منذ اليوم الأول دون أي حجب.

---

### ⚪ مسار مستقل — البنية التحتية (بالتوازي مع كل الأمواج، مالك مختلف)

| الكود | الإجراء |
|---|---|
| **SF-10** | تأكيد وجود TLS termination / عزل شبكي (VPN/VLAN) للوصول على `192.168.8.22:3100` |

> لا يتقاطع كوديًا مع أي موجة أعلاه — يُحال لفريق البنية التحتية فورًا وبالتوازي الكامل، دون انتظار أي بند تطبيقي.

---

## 3) مخطط الاعتمادية (Dependency Overview)

```
الموجة A (فوري) ──┬──> الموجة C (بعد إغلاق SF-08 فقط)
                   │
الموجة B (فوري، متوازٍ مع A) ─────────────┘ (لا تبعية تقنية على C، لكن تُغلق الصورة الكاملة معًا)

الموجة D (فوري، مسار مستقل تمامًا طوال الوقت)

مسار البنية التحتية SF-10 (فوري، مستقل، مالك خارجي)
```

---

## 4) بوابات الاعتماد على إدارات أخرى (Cross-Department Gates)

هذا السجل مقصور على نطاق **إدارة المبيعات وحدها**. إلا أن التقرير الفني السابق (تدقيق الكود) رصد نقاط انكسار handoff تتقاطع مع **إدارة المشروعات** (تحديدًا: غياب trigger تسليم Contract→Project، وحجب PROJECTS عن ملف العميل). هذه البنود **لا تُدرَج هنا لأنها ليست ملاحظة ميدانية من مستخدمي المبيعات**، لكن يجب تسجيلها كـ **بوابة اعتماد مزدوجة (Dual Gate)**:

- **الجاهزية من طرف المبيعات (هنا):** إرسال إشعار/حدث `PROJECT_HANDOFF` عند إنشاء العقد — هذا العمل يمكن تنفيذه ضمن نطاق هذه الإدارة وحدها.
- **الإغلاق الفني الكامل (هناك):** لن يكتمل الإغلاق 100% إلا عند وصول تدقيق **إدارة المشروعات**، وبناء منطق الاستقبال (إنشاء Project تلقائيًا أو inbox استقبال) على الطرف الآخر.

**لن تُعتبر إدارة المبيعات مُغلَقة فنيًا بنسبة 100%** إلا بعد اكتمال الأمواج A/B/C/D أعلاه **و** التأكد من أن جزء "الجاهزية" الخاص بالمبيعات في بوابة التسليم لإدارة المشروعات جاهز لاستقبال أي تكامل مستقبلي من الطرف الآخر.

### بوابة مزدوجة ثانية — SF-25: بنود طلب العميل → المكتب الفني (2026-07-25)

- **الجاهزية من طرف المبيعات (هنا):** تسجيل ما يطلبه العميل بما يصل للطرف الآخر. **متحقّقة جزئيًا:** `summary` يُملأ فعليًا (20 من 21 طلبًا) لكنه **نص حر لا بنود مهيكلة** — التهيكل سؤال schema منفصل (SCR)، لا يحجب.
- **الإغلاق الفني (هناك):** عرض البنود في شاشة المكتب الفني. **غير مُنفَّذ:** الخدمة تُرجع `summary` (`services/tec.ts:112,173`) والشاشات الأربع لا تعرضه إطلاقًا. **يُقفل عند تدقيق إدارة المكتب الفني** — خارج نطاق سيشن المبيعات.

---

## 5) ملخص الحالة (يُحدَّث عند كل إغلاق)

**الأمواج الثلاث A/B/C مُغلقة.** التصنيف أدناه بالحالة النهائية لا بالموجة.

### (أ) ✅ مُغلق ومُثبَّت بالدليل

| البند | الإنجاز | Commit |
|---|---|---|
| **SF-01** | تدفّق المندوب عميل→طلب عرض سعر + `NewQuotationRequestDialog` | `930e109` |
| **SF-03** | REJECTED-only لغير ADMIN · SALES_MANAGER بلا قيد ملكية · SALES_REP مقيَّد بـ`ownerId` · تقرير رفض مُجمَّع | `dd9ce32` → `bab0221` (PR #3) |
| **SF-05** | إشعار `setCustomerCoverage` + فواتير في `getCustomerById` | `af84926`+`fff5973` → `78c2c6a` (PR #4) |
| **SF-06** | داشبورد المبيعات Role-aware + تنطيق عدّاد المعاينات | `d239403` → `126fe71` (PR #6) |
| **SF-07** | طبقة التجميع الخمس (عقود D-44 · عروض · عملاء/موظف · نوع الطلب · تاج التوصية) | `a731388` → `126fe71` (PR #6) |
| **SF-09** | محاذاة أعمدة `/coverage-edits` | `1fb331b` |
| **SF-11** | تسمية «العملاء» بدل «المبيعات» في السايدبار | `f742968` |
| **SF-13** | حارس `subtotal>0` على مسار edit (توحيد مع create) | `8563af9` → `4f7b527` (PR #1) |
| **SF-17** | محاذاة عمودي الاسم/الإجراءات في `/customers` | `744df07` → `3179a66` (PR #2) |
| **SF-19** | إصلاح عرض مفتاح الترجمة الخام في الإشعارات | `b79cf97` → `07a80e3` (PR #5) |
| **SF-20** | تقييد `SALES_REP` على `ownerId OR coveredById` = السلوك المطلوب | ✅ **مُغلق بقرار — بلا كود** (يوسف، 2026-07-25) |
| **SCR-019** | `salesRequestType` + `isReferralTag` على `QuotationRequest` | `d361a06` |
| **D-44 · D-45** | حالة العقد (مصدر الحقيقة) + تصنيف الداشبورد (نوع الطلب + تاج التوصية) | `a90e599` |

### (ب) 🟡 مفتوح — تحسين لاحق لا يحجب شيئًا

| البند | الوصف | ملاحظة أولوية |
|---|---|---|
| **SF-23** | drill-through على بطاقات الداشبورد | يحتاج فحص الشرطين (URL-driven؟ · إعادة فرض الحارس STD-15) |
| **SF-24** | (أ) مقارنة زمنية للمؤشرات · (ب) نقل مؤشر نسبة تحويل العروض إلى داشبورد المبيعات | **(أ) أرخص من SF-23** ⇒ أولوية أعلى عند فتح جولة تحسين |

### (ج) 🔴 معلَّق على إدارة أخرى — بوابات مزدوجة

- **SF-25 — بنود طلب التسعير للمكتب الفني.**
  · *ما أنجزته المبيعات:* **الجاهزية قائمة بالفعل** — `summary` مُعبَّأ (20 من 21 طلبًا) ويصل للخدمة (`services/tec.ts:112,173`).
  · *ما ينتظره الطرف الآخر:* **عرضه في شاشات المكتب الفني** (صفر عرض حاليًا) ⇒ **الإغلاق كله عند المكتب الفني.**
- **SF-26 — حالة رفض/طلب تعديل على العرض.**
  · *ما أنجزته المبيعات:* رصد الفجوة + القرار التصميمي (حالتان: `REVISION_REQUESTED` / `REJECTED`) + إلزام أكواد enum لا نص مركَّب (درس SF-18).
  · *ما ينتظره الطرف الآخر:* **قرار مسار العمل** (إلى مَن يُوجَّه كل سبب؟) + **فحص التقاطع مع `reviewStatus.RETURNED`** (`lib/review/actions.ts:194`) قبل أي SCR — لا يُصمَّم قبله.
- **بوابة Contract→Project** (قائمة سلفًا).
  · *ما أنجزته المبيعات:* العقد يُنشأ ويُسجَّل، والبيانات كلها معلّقة على `customerId`.
  · *ما ينتظره الطرف الآخر:* منطق الاستقبال (إنشاء Project تلقائيًا أو inbox) — **بانتظار تدقيق إدارة المشروعات.**

### (د) 📐 معلَّق على جلسة تصميم مستقلة

| البند | الوصف |
|---|---|
| **BL-149** | التفويض عند الغياب (المدير يوكّل للأقل تاسكات + history) |
| **SF-02** | وحدة العينات الكاملة (`Sample` model + شاشة طلب/متابعة) |
| **SF-04** | محرك التوزيع الآلي (round-robin / least-loaded) |

### (هـ) 🧹 تنظيف / دين تقني

| البند | الوصف |
|---|---|
| **SF-14** | شذوذ بذرة `QUO-2026-0020` (إجمالي 17100 بصفر بنود) |
| **SF-18** | أسباب الرفض نص عربي مركَّب لا كود مُهيكل — SCR مقترَح (`rejectReasonCode`) |
| **SF-21** | صف `lastModifiedBy` مُضلِّل في بروفايل العميل |
| **SF-22** | 6 من 10 عقود بلا `signedAt` (بيانات بذرة) |
| **SF-10** | تأكيد TLS / عزل شبكي — **مالك خارجي** (فريق البنية التحتية) |

### 🔒 خلاصة الإغلاق

**حالة إدارة المبيعات (2026-07-25):** مُغلقة فنيًا لكل ما هو قابل للإغلاق داخل نطاقها وحدها. المتبقي: بوابتان معلَّقتان على المكتب الفني (SF-25/26) وواحدة على المشروعات · بندا تحسين (SF-23/24) · ثلاثة بنود تحتاج جلسة تصميم (BL-149/SF-02/04) · خمسة بنود تنظيف. **لا يُعاد فتح تدقيق المبيعات إلا عند إغلاق البوابات من الطرف الآخر.**

## 6) قرارات محسومة — Wave C (تحسم فجوات SF-07)

| الكود | القرار | صاحب القرار |
|---|---|---|
| **D-44** | **حالة العقد — مصدر الحقيقة (يحسم فجوة SF-07، Wave C).** "تم التعاقد/بانتظار التنفيذ" = وجود عقد موقَّع (دور المبيعات ينتهي هنا). "منفَّذ" = اكتمال `InstallationOrder` بالكامل (`COMPLETED`) على كل بنود العقد المرتبطة. قرار طارق (مدير المبيعات)، 2026-07-25. | طارق |
| **D-45** | **تصنيف الداشبورد: نوع الطلب + تاج توصية (يحسم فجوة SF-07، Wave C).** (1) **نوع الطلب** — قيمة واحدة إلزامية لكل طلب، تُختار من الموظف وقت تسجيل الطلب (لا وقت تسجيل العميل): فردي / سوشيال / مشروعات. صفة على الطلب لا العميل — نفس العميل قد يملك طلبات متعددة بأنواع مختلفة عبر الزمن. (2) **تاج "توصية"** — علامة إضافية اختيارية فوق أي نوع طلب (لا بديل رابع)، توثّق أن العميل معرفة/توصية شخصية قريبة من عمرو (CEO). **شكلي/بياني بالكامل — صفر أثر على مسار العمل، التسعير، الموافقات، أو أي حارس صلاحيات.** الأمر الفعلي بمعرفة العميل يصدر من عمرو مباشرة خارج النظام؛ أي مندوب مبيعات (SALES_REP) يوثّق التاج على الطلب بلا قيد صلاحية إضافي (لا موافقة، لا تصعيد). مصدر العميل التقليدي (CustomerSource: AD/REFERRAL/WHATSAPP/EXHIBITION/VISIT) يبقى كما هو، منفصل تمامًا عن هذا التاج — REFERRAL الحالية تخص "كيف عرفنا العميل أول مرة"، لا علاقة لها بتاج التوصية الجديد (علاقة عمرو الشخصية). قرار طارق (مدير المبيعات)، 2026-07-25. | طارق |

### SCR-019 — تصنيف نوع الطلب + تاج التوصية (ينفّذ D-45) · 🟢 مواصفات مكتملة بلا أسئلة مفتوحة — بانتظار تطبيق يوسف

> **L-02:** لم أعدّل `schema.prisma` ولا أنشأتُ migration — هذا **مخطط** فقط؛ المشغّل البشري (يوسف) يطبّقه بعد الاعتماد.

- **الموديل:** `QuotationRequest` (نفس موديل `technicalRoute`).
- **حقل 1 — نوع الطلب:** enum **جديد منفصل** `SalesRequestType { INDIVIDUAL · SOCIAL_MEDIA · PROJECTS }` + عمود `salesRequestType SalesRequestType`. **قرار مسنود: لا نُوسّع `TechnicalRoute`** — بالدليل: `technicalRoute` بقيمتين حِمل عمل ثقيل (تواقيع `route:"PROJECTS"|"SOCIAL_MEDIA"` في [document-number.ts:38,108,144] + أهلية العقد [contracts/actions.ts:37] + الطباعة [print/page.tsx:64-65] + شاشات TEC + الحسابات + المستخلصات/الفواتير) — قيمة ثالثة تكسر الأنواع والمنطق. الحقل المنفصل يعزل بُعد التقرير عن enum سير العمل.
  - **default/migration:** الطلبات القائمة تأخذ backfill مشتق من `technicalRoute` (PROJECTS→PROJECTS · SOCIAL_MEDIA→SOCIAL_MEDIA؛ "فردي" لا يظهر في البيانات القديمة). العمود إلزامي بعد الـbackfill.
- **حقل 2 — تاج التوصية:** `isReferralTag Boolean @default(false)` — **NOT NULL، default false صريح** (STD-05/BL-09: لا NULL كحالة صامتة). منفصل تمامًا عن enum النوع (تاج فوقي لا بديل رابع، نص D-45). صفر أثر على أي منطق/حارس/تسعير/موافقة.
- **✅ العلاقة بـ`technicalRoute` — محسومة (قرار طارق، 2026-07-25): الخيار (أ) — حقلان مستقلان تمامًا، بلا اشتقاق أحدهما من الآخر.** `technicalRoute` يبقى حرفيًا كما هو (PROJECTS/SOCIAL_MEDIA) ويحرّك سير العمل الفعلي (ترقيم المستندات · أهلية العقد · الطباعة · TEC · الحسابات) — صفر تغيير عليه أو على أي منطق يتفرّع عليه. `salesRequestType` للتقرير/الداشبورد فقط — صفر تأثير على سير العمل. **الموظف يختار الاثنين** عند تسجيل الطلب. في الحالة الشائعة تتطابق القيمتان منطقيًا (سوشيال/مشروعات) وهذا متوقَّع لا تكرار زائد؛ وعند طلب "فردي" يختار `technicalRoute` الفعلي (PROJECTS أو SOCIAL_MEDIA حسب طبيعة الطلب) **بالإضافة إلى** `salesRequestType=INDIVIDUAL` كتصنيف تقريري.
- **Blast radius — القراءة/التفرّع: صفر كسر (مُتحقَّق).** عمودان **مضافان فقط** (لا حذف/تعديل/إعادة تسمية) و`SalesRequestType` enum **جديد منفصل** ⇒ لا يمسّ أي تفرّع قائم على `technicalRoute`: [document-number.ts:38,108,144] · [contracts/actions.ts:37] · [print/page.tsx:64-65] · [tec-client.tsx:64,141] · [accounting/actions.ts:232] · [statements.ts:120] · [invoices.ts:104] — كلها تبقى تعمل بلا تعديل حرف واحد.
- **⚠️ Blast radius — الكتابة (تسلسل إلزامي، ليس كسرًا صامتًا):** `salesRequestType` **إلزامي بلا default** (عمدًا — D-45: قيمة يختارها الموظف؛ أي default = تصنيف صامت خاطئ يخالف STD-05). لذا **موقع الإنشاء الوحيد** `prisma.quotationRequest.create` ([services/quotation-requests.ts:28]) **يجب أن يُمرِّر القيمة** وإلا كسر البناء (TS) — سلسلة التحديث: الخدمة [quotation-requests.ts:13-39] ← الـaction [customers/actions.ts:100-113] ← الحواران [request-quotation-dialog.tsx:45] و[new-quotation-request-dialog.tsx:154]. **الكسر لو حدث = صريح وقت البناء، لا صامت وقت التشغيل** (مقبول ومقصود). التوصية: تطبيق الـmigration وتحديث مسار الكتابة **في نفس الجلسة**. `isReferralTag` بـ`@default(false)` ⇒ صفر أثر على مسار الكتابة.
- **Migration safety:** إضافة عمودين + enum جديد + backfill للصفوف القائمة (مشتق من `technicalRoute`) — بلا حذف أو تعديل بيانات، قابل للتراجع (drop columns + drop enum). اسم مقترح `scr019_sales_request_type_referral`.
- **⚠️ واقع البيانات (مُتحقَّق بـSELECT حيّ، 2026-07-25): 19 صفًا قائمًا في `quotation_requests` (8 PROJECTS · 11 SOCIAL_MEDIA).** ⇒ إضافة عمود **إلزامي بلا default** على جدول به صفوف **تُفشِل `prisma migrate dev` مباشرة** (لا يمكن ملء الصفوف القائمة). لذا التطبيق **ثلاثي الخطوات إلزاميًا**: (1) إضافته nullable + migrate · (2) backfill من `technicalRoute` · (3) تحويله إلزاميًا + migrate. **ممنوع `migrate reset`** (يمسح البيانات).
- **⚖️ استثناء موثَّق من L-02 — مُنح 2026-07-25، مانحه: يوسف (مالك القوانين).** **نطاقه هذه المهمة فقط:** كتابة تعديل `schema.prisma` + ملفات مسار الكتابة الأربعة + عناصر الواجهة + مفاتيح الترجمة. **سببه:** المشغّل البشري غير مبرمج وخطر الخطأ اليدوي أعلى. **روح L-02 محفوظة:** يوسف يراجع ويشغّل أمر الـmigration بنفسه — الوكيل **لا** يشغّل أي `prisma migrate`، و**L-03 (commit/push) ساري بلا استثناء**.
- **📌 نتيجة الاستثناء الفعلية:** الهوك `.claude/hooks/protect.py` **حجب تعديل `schema.prisma` تقنيًا** رغم الإذن (L-02 مصنّف [HARD])، **ولم يُلتفّ حوله بأي وسيلة بديلة** (الالتزام: "الحجب هو القانون يعمل"). ⇒ **تعديل `schema.prisma` يبقى بيد يوسف يدويًا** (المقطع الجاهز للّصق مُسلَّم في تقرير الجلسة). **طبقة التطبيق نُفِّذت بالكامل** (الخدمة + الـaction + الحواران + ar/en).

### SCR-020 — صورة تكلفة الكتالوج على بند العرض (TO-05) · 🟢 مواصفات مكتملة — بانتظار تطبيق يوسف

> **L-02:** لم أعدّل `schema.prisma` ولا أنشأتُ migration. الهوك حجب الملف (كسابقة SCR-019) ولم يُلتفّ حوله. **طبقة التطبيق نُفِّذت بالكامل** على فرع `fix/to-05-costsnapshot`، وتنتظر العمود.

- **الموديل:** `QuotationItem` (`prisma/schema.prisma:176-186`).
- **الحقل الوحيد:** `costSnapshot Decimal? @db.Decimal(12,2)` — **إضافة فقط**، صفر تعديل على أي عمود قائم.
- **السبب:** `unitPrice` يُحفظ اليوم، و**تكلفة الكتالوج وقت التسعير لا تُحفظ**. `Material.cost` متغيّر بمرور الوقت ⇒ أي حساب هامش لاحق يستخدم تكلفة اليوم لعرضٍ قديم = **رقم خاطئ**. هذا العمود هو ما يجعل مؤشر الهامش في الداشبورد قابلًا للبناء بصدق.
- **الدلالة (مُلزِمة لأي قارئ لاحق):** تكلفة **الوحدة الواحدة** — موازٍ لـ`unitPrice` ⇒ تكلفة البند = `quantity × costSnapshot`. تُحسب كـΣ(كمية الوصفة × `Material.cost`) للخامات المختارة فقط، **بلا معامل ربح**.
- **⚠️ nullable عمدًا — `null` ≠ صفر.** `null` = «التكلفة غير معروفة»، وتحدث في ثلاث حالات: (1) صفوف أُنشئت قبل TO-05 · (2) بند يدوي بلا وصفة · (3) بند مرّ على `updateQuotation` (يحذف البنود ويعيد إنشاءها من أسعار مكتوبة يدويًا بلا سياق وصفة — انظر BL-146). **أي قارئ يجب أن يستبعد صفوف `null` من البسط والمقام معًا ويعلن حجم المستبعَد** — معاملتها كتكلفة صفر تنتج «هامش 100%» كاذبًا. لا backfill: تلفيق تكلفة تاريخية غير معروفة يخالف L-01.
- **Blast radius — صفر كسر (مُتحقَّق):** عمود **مضاف nullable** ⇒ لا قارئ قائم يتأثر. الكاتب الوحيد لصفوف `QuotationItem` في الريبو كله هو `lib/pricing/actions.ts` (`createQuotation` + `updateQuotation`) — مُثبَت بـgrep على `quotationItem|QuotationItem` (المطابقة الأخرى الوحيدة: `lib/executive/actions.ts:79` وهي `groupBy` على `description` فقط، و`print/page.tsx:250` تعليق).
- **Migration safety:** إضافة عمود nullable واحد، بلا backfill وبلا تعديل بيانات، **قابل للتراجع بالكامل** (`DROP COLUMN`). لا يفشل على الصفوف القائمة (`Decimal?`). اسم مقترح: `to05_quotation_item_cost_snapshot`.
- **⚠️ تسلسل إلزامي:** `prisma migrate dev` **ثم** `prisma generate` **قبل** أي build. طبقة التطبيق تكتب `costSnapshot` بالفعل، فقبل الـmigration يرمي Prisma وقت التشغيل `Unknown argument costSnapshot` عند حفظ أي عرض.
- **🔴 مفاجأة منهجية مُثبَتة (تخصّ كل SCR لاحق):** `tsc --noEmit` **لا يكتشف** حقلًا غير موجود داخل `create` المتداخل في Prisma — بنية `XOR<>` تُعطّل فحص الخصائص الزائدة. **الدليل:** حُقن حقل وهمي `zzzBogusProbeField: 123` في نفس الكتلة و`tsc` أعطى **صفر خطأ** عليه (87 خطأ سابق فقط، لا واحد منها في الملفات المعدَّلة). ⇒ **بناء أخضر ليس دليلًا على أن العمود موجود.** الدليل الوحيد = صف حقيقي في القاعدة بعد حفظ عرض حقيقي.

### SCR-021 — دور `TEC_LEAD` + نطاق المسار + عمود آخر مُعدِّل (TO-23) · 🟢 مواصفات مكتملة — بانتظار تطبيق يوسف

> **L-02:** لم أعدّل `schema.prisma` ولا أنشأتُ migration. المقطع أدناه يُلصق كما هو. طبقة التطبيق منفّذة على فرع `fix/to-23-tec-lead` وتنتظر الأعمدة — **البناء أحمر حتى تنفيذ الـmigration** (مقصود، وموثّق).

**١) `enum Role` (`prisma/schema.prisma:623-638`) — إضافة قيمة خامسة عشرة:**
```prisma
enum Role {
  // … الأربع عشرة القائمة كما هي، بلا مساس …
  TEC_LEAD          // TO-23: تيم ليدر المكتب الفني — يوزّع على مهندسي مساره ويرى عملهم
}
```

**٢) `model User` — حقل نطاق المسار:**
```prisma
  /// TO-23: مسار تيم ليدر المكتب الفني. يُملأ لـTEC_LEAD **فقط** ويبقى null لبقية
  /// الأدوار الأربعة عشر. null لـTEC_LEAD = بلا مسار مُسنَد ⇒ **لا يرى أي عرض**
  /// (فشل مغلق: أوضح وأأمن من رؤية الكل بالخطأ). ليس قيدًا في القاعدة — يُفرض تطبيقيًا.
  leadRoute TechnicalRoute?
```

**٣) `model Quotation` (`prisma/schema.prisma:135-174`) — عمود آخر مُعدِّل:**
```prisma
  updatedById String?   // TO-23: آخر من عدّل العرض. التفاصيل الكاملة في ActivityLog — لا تُكرَّر هنا.
```

- 🔴 **انحراف مقصود عن نص التكليف — قرارك:** التكليف طلب «`updatedById` + العلاقة المناسبة مع User» و«اتبع النمط القائم ولا تخترع». **الشرطان متناقضان:** النمط القائم في الموضعين المُشار إليهما — `SystemSettings.updatedById` (`schema:490`) و`PriceListItem.updatedById` (`schema:543`) — **عمود scalar بلا علاقة**. ونفس النمط على `Quotation` نفسه: `approvedById` (`:149`) و`reviewedById` (`:159`) كلاهما `String?` بلا علاقة. اخترت **بلا علاقة** لسببين:
  1. **Blast radius:** `User.quotations Quotation[]` (`schema:31`) علاقة **بلا اسم** تخدم `Quotation.createdBy`. Prisma يوجب تسمية **كل** العلاقات بين نفس الموديلين متى تعدّدت ⇒ إضافة `updatedBy` تُجبر على **تسمية العلاقة القائمة وتعديلها**. أي: تعديل علاقة عاملة، لا إضافة فقط.
  2. **سابقة العرض قائمة:** اسم المعتمِد يُقرأ اليوم من عمود scalar باستعلام منفصل — `src/app/(print)/quotations/[id]/print/page.tsx:56-59` وفيه تعليق حرفي بذلك.
  - التكلفة المقبولة: استعلام واحد إضافي لتحويل المعرّفات إلى أسماء (استعلام واحد للقائمة كلها عبر `id: { in: [...] }`، لا استعلام لكل صف). **لو فضّلت العلاقة رغم ذلك، قل وأنفّذها — لكنها تلمس علاقة قائمة.**
- **Blast radius — صفر كسر:** عمودان مضافان nullable + قيمة enum مضافة. لا قارئ قائم يتأثر. قيمة الـenum الجديدة **مرفوضة افتراضيًا في كل الـ167 حارسًا** (نموذج allowlist: `roles.includes(session.user.role)` — `src/lib/rbac.ts:24`) ⇒ فشل مغلق: TEC_LEAD لا يصل لأي شاشة إلا التي أُضيف إليها صراحةً.
- **Migration safety:** إضافة بحتة، بلا backfill وبلا تعديل بيانات، قابلة للتراجع (`DROP COLUMN` + إزالة قيمة enum). ⚠️ **حذف قيمة enum في PostgreSQL غير مباشر** — التراجع يحتاج إعادة إنشاء النوع؛ لا يعني ذلك خطرًا على التقدّم للأمام. اسم مقترح: `to23_tec_lead_role_scope`.
- **الأمر الذي ينفّذه المالك (من جذر المشروع، داخل Docker):**
  ```
  docker compose exec app npx prisma migrate dev --name to23_tec_lead_role_scope
  docker compose exec app npx prisma generate
  docker compose exec app npm run build
  ```
- **⚠️ تسلسل إلزامي:** `migrate` ثم `generate` **قبل** أي build (سابقة SCR-020). وقبلها البناء يفشل على `leadRoute`/`updatedById`/`TEC_LEAD` — وهذا هو السلوك المتوقَّع لا عطل.
- **بعد الـmigration — خطوة بيانات إلزامية بيد المالك:** لا يوجد اليوم أي مستخدم بدور `TEC_LEAD`. لاختبار الموجة يلزم مستخدمان: واحد `TEC_LEAD` بـ`leadRoute=PROJECTS` وآخر `leadRoute=SOCIAL_MEDIA`، **ومستخدم ثالث `TEC_LEAD` بـ`leadRoute=null`** للتأكد من أنه لا يرى شيئًا. تُنشأ من شاشة `/users` (ADMIN) لا بـSQL مباشر (L-04).

### SCR-022 — ربط المهندس بتيم ليدره (TO-25) · 🟢 مواصفات مكتملة — بانتظار تطبيق يوسف

> **L-02:** لم أعدّل `schema.prisma` ولا أنشأتُ migration. المقطع يُلصق كما هو. طبقة التطبيق منفّذة على `fix/to-23-tec-lead` وتنتظر العمود — **البناء أحمر حتى الـmigration** (مقصود وموثّق، سابقة SCR-020/021).

**`model User` — أول علاقة ذاتية في هذا الموديل:**
```prisma
  /// TO-25: تيم ليدر المهندس. يُملأ لـTECHNICAL_OFFICE فقط، ويبقى null لبقية الأدوار.
  /// null = مهندس بلا تيم ليدر — **حالة صالحة** لا خطأ: لا يظهر في قائمة إسناد أي ليدر،
  /// ويبقى المدير (TEC_APPROVER/ADMIN) قادرًا على إسناده لأنه يرى كل المهندسين.
  teamLeadId  String?
  teamLead    User?   @relation("TecTeamLead", fields: [teamLeadId], references: [id], onDelete: SetNull)
  teamMembers User[]  @relation("TecTeamLead")
```

- **⚠️ لماذا التسمية إلزامية هنا (وعكس حالة TO-23):** العلاقة **ذاتية** (User↔User)، وPrisma يوجب اسمًا صريحًا لطرفيها. **الفرق الحاسم عن `updatedById` في SCR-021:** هناك كانت `User.quotations` علاقة قائمة **بلا اسم** فكانت إضافة علاقة ثانية تُجبر على تعديلها. هنا **لا توجد أي علاقة ذاتية على `User` إطلاقًا** — مُتحقَّق بمسح جسم الموديل كاملًا (`awk '/^model User \{/,/^\}/'` ⇒ صفر مطابقة لـ`User`/`User[]`/`User?` داخله). ⇒ التسمية `"TecTeamLead"` **جديدة تمامًا ولا تلمس أي علاقة عاملة**.
- **`onDelete: SetNull`** — متسق مع سياسة deactivate-not-delete (المستخدمون يُعطَّلون لا يُحذفون، وFK تمنع الحذف أصلًا). لا يغيّر سلوكًا قائمًا؛ صريح بدل الاعتماد على الافتراضي.
- **Blast radius — صفر كسر:** عمود مضاف nullable + علاقة بتسمية جديدة. لا قارئ قائم يتأثر، ولا `@unique` جديد، ولا تعديل على أي حقل قائم. مهندس بلا `teamLeadId` يسلك تمامًا كما اليوم.
- **Migration safety:** إضافة بحتة، بلا backfill وبلا تعديل بيانات، قابلة للتراجع بالكامل (`DROP COLUMN`). اسم مقترح: `to25_user_team_lead`.
- **الأمر الذي ينفّذه المالك (من جذر المشروع):**
  ```
  docker compose exec app npx prisma migrate dev --name to25_user_team_lead
  docker compose exec app npx prisma generate
  docker compose exec app npm run build
  ```
- **⚠️ تسلسل إلزامي:** `migrate` ثم `generate` **قبل** أي build. قبلها يفشل البناء على `teamLeadId`/`teamMembers` — سلوك متوقَّع لا عطل.
- **بعد الـmigration — خطوة بيانات بيد المالك:** ربط المهندسين القائمين بتيم ليدرهم من شاشة `/users` (تعديل كل مهندس `TECHNICAL_OFFICE` واختيار تيم ليدره). **قبل الربط، قائمة إسناد أي تيم ليدر تكون فارغة** — وهذا صحيح لا عطل: لا مهندس مُسنَد إليه بعد.

### SCR-023 — بوابة الاعتماد المبدئي من التيم ليدر + أزمنة KPI (TO-24) · 🟢 مواصفات مكتملة — 🔴 **قرار ترحيل مطلوب من يوسف قبل التنفيذ**

> **L-02:** لم أعدّل `schema.prisma` ولا أنشأتُ migration. طبقة التطبيق منفّذة على `fix/to-24-lead-approval` وتنتظر الأعمدة.

**١) enum جديد:**
```prisma
enum LeadApprovalStatus {
  NOT_SUBMITTED   // لم يقدّمه المهندس بعد
  PENDING_LEAD    // مُقدَّم، بانتظار قرار التيم ليدر
  LEAD_APPROVED   // اعتماد مبدئي — **بوابة وصول العميل**
  LEAD_RETURNED   // مُرجَع للمهندس بسبب مكتوب
}
```

**٢) `model Quotation` — خمسة أعمدة مضافة:**
```prisma
  /// TO-24: بوابة الاعتماد المبدئي. تنطبق **فقط** على العروض المرتبطة بطلب تسعير
  /// (عروض المكتب الفني)؛ العرض بلا طلب يمر بلا بوابة — مسار المبيعات المباشر
  /// لا يمر بالمكتب الفني. الفصل يتم في الكود لا في القاعدة.
  leadApprovalStatus LeadApprovalStatus @default(NOT_SUBMITTED)
  /// TO-24 (KPI): لحظة تقديم المهندس. أساس قياس «زمن انتظار قرار الليدر».
  submittedAt        DateTime?
  /// TO-24 (KPI): لحظة قرار الليدر — اعتمادًا كان أو إرجاعًا.
  leadDecidedAt      DateTime?
  /// TO-24: مُقرِّر الاعتماد المبدئي. **بلا علاقة** — نفس نمط `approvedById` (:149)
  /// و`reviewedById` (:159) على هذا الموديل نفسه. الاسم يُقرأ باستعلام منفصل.
  /// اعتماد الذات **لا عمود له**: يُشتق من `leadDecidedById === createdById` (قرار المالك).
  leadDecidedById    String?
  /// TO-24: سبب الإرجاع — إلزامي تطبيقيًا عند LEAD_RETURNED (`z.string().min(1)`).
  leadNote           String?
```

**٣) 🔴 قرار الترحيل التاريخي — الأرقام من قاعدتك الآن (32 عرضًا غير محذوف):**

| | العدد | أثر الافتراضي `NOT_SUBMITTED` |
|---|---|---|
| عروض **بلا** طلب تسعير | **20** | لا شيء — خارج البوابة أصلًا |
| عروض **مرتبطة بطلب** (داخل البوابة) | **12** | 🔴 **تفقد قابلية الطباعة فورًا** |
| منها: اجتازت الاعتماد **النهائي** بالفعل (`reviewStatus=APPROVED`) | **6** | 🔴 عروض مكتملة فنيًا تُمنع إعادة طباعتها |

- **الخيار (أ) — grandfathering (ترحيل):** سطر backfill بعد الـmigration يضع `LEAD_APPROVED` للصفوف القائمة.
  ```sql
  UPDATE "Quotation" SET "leadApprovalStatus" = 'LEAD_APPROVED'
  WHERE "createdAt" < '<تاريخ تنفيذ الـmigration>';
  ```
  **الأثر:** صفر عرض يفقد الطباعة · البوابة تسري على الجديد فقط. **الثمن:** الـ12 تُعلَّم «معتمدة مبدئيًا» دون أن يعتمدها أحد فعلًا — `leadDecidedById` يبقى `null` فيبقى الأثر صادقًا (لا مُقرِّر مُلفَّق)، لكن `submittedAt`/`leadDecidedAt` تبقى `null` ⇒ **تُستبعَد من حساب KPI** ولا تلوّثه.
- **الخيار (ب) — بلا backfill:** الـ12 تُحجَب حتى يقدّمها مهندس ويعتمدها ليدر. **الأثر:** توقف طباعة 12 عرضًا فورًا، منها 6 مكتملة فنيًا — عمل يدوي على كل واحد.

✅ **القرار: الخيار (أ) — grandfathering (يوسف، 2026-07-26).** السبب المعلَن: 6 عروض اجتازت الاعتماد النهائي فعلًا، وحجبها يكسر شغلًا قائمًا بلا مكسب. الأثر مقبول لأن `leadDecidedById` يبقى `null` (لا معتمِد مُلفَّق) و`submittedAt`/`leadDecidedAt` تبقيان `null` (الصفوف المرحَّلة تُستبعَد من KPI ولا تلوّثه).

- **Blast radius:** 5 أعمدة مضافة (أربعة nullable + واحد بـ`@default`) + enum جديد. لا قارئ قائم يتأثر · لا `@unique` · لا تعديل عمود قائم · لا علاقة جديدة (تجنّبًا لمشكلة تسمية العلاقات التي وثّقتها SCR-021).
- **Migration safety:** إضافة بحتة، قابلة للتراجع (`DROP COLUMN` + حذف النوع). اسم مقترح: `to24_lead_approval_gate`.
- **الأمر:**
  ```
  docker compose exec app npx prisma migrate dev --name to24_lead_approval_gate
  docker compose exec app npx prisma generate
  docker compose exec app npm run build
  docker compose restart app
  ```
  **ثم فورًا — سطر الترحيل المعتمَد (الخيار أ):**
  ```
  docker compose exec -T db psql -U egyglass -d egyglass -c "UPDATE \"Quotation\" SET \"leadApprovalStatus\"='LEAD_APPROVED' WHERE \"createdAt\" < now();"
  ```
  **التحقق بعده** (المتوقَّع: صفر صف مرتبط بطلب خارج `LEAD_APPROVED`):
  ```
  docker compose exec -T db psql -U egyglass -d egyglass -c "SELECT q.\"leadApprovalStatus\", count(*) FROM \"Quotation\" q JOIN quotation_requests qr ON qr.\"quotationId\"=q.id WHERE q.\"deletedAt\" IS NULL GROUP BY 1;"
  ```
  ⚠️ **الترتيب مقصود:** الترحيل **بعد** `migrate` (العمود يجب أن يوجد) و**قبل** أول استعمال فعلي — أي عرض يُنشأ بين الاثنين سيُرحَّل خطأً إلى `LEAD_APPROVED` لأن شرط `createdAt < now()` يلتقطه. لا تنشئ عروضًا بينهما.
- **⚠️ `next build` يمرّ أخضر كاذبًا قبل الـmigration** (يتخطّى فحص الأنواع — مُثبَت في TO-25). البوابة الحقيقية `tsc --noEmit` والـruntime.

### SCR-024 — حفظ مدخلات التسعير على بند العرض (TO-33) · 🟢 مواصفات مكتملة — بانتظار تطبيق يوسف

> **L-02:** لم أعدّل `schema.prisma` ولا أنشأتُ migration. طبقة التطبيق منفّذة على `fix/to-33-edit-engine` وتنتظر العمود.

**`model QuotationItem` (`prisma/schema.prisma:203-216`) — عمود واحد مضاف:**
```prisma
  /// TO-33 — مدخلات التسعير كما أُرسلت وقت الحفظ، بنفس شكل `ItemPricingInput`
  /// (`lib/pricing/recipe-selection.ts`): productTypeCode · height · width ·
  /// configTypeId? · pricingFactorId · selections.
  ///
  /// لماذا: البند اليوم يُخزَّن كأرقام نهائية فقط (وصف · كمية · سعر · إجمالي)،
  /// فإعادة بناء حالة محرك التسعير لبند قائم **مستحيلة** — ولهذا كانت شاشة
  /// التعديل حقولًا نصية، وكل حفظ يمسح `costSnapshot` إلى null (BL-151).
  ///
  /// ⚠️ nullable **دائم لا مؤقت**: null = بند يدوي بلا وصفة، أو بند سابق لـTO-33.
  /// حالة صالحة لا نقص — يُعرض للتعديل كحقول نصية لا في المحرك، وتكلفته تبقى null.
  pricingInput Json?
```

- **🔴 لا backfill إطلاقًا.** المدخلات لم تُسجَّل وقت الإنشاء، واستنتاجها من `unitPrice` عكسيًا تلفيق يخالف L-01. الأرقام: **44 بندًا قائمًا** (11 بتكلفة · 33 بلا) في **38 عرضًا** — كلها ستبقى `pricingInput = null` وتُعرض كبنود يدوية موسومة. هذا هو السلوك الصحيح لا نقصًا يُسدّ.
- **Blast radius — صفر كسر:** عمود مضاف nullable، لا قارئ قائم يتأثر، لا `@unique`، لا علاقة، ولا مساس بـ`costSnapshot` ولا بأي عمود آخر.
- **لماذا `Json` لا أعمدة منفصلة:** `selections` خريطة مفتوحة (فئة الوصفة ⇒ معرّف الخامة) تختلف بعدد مفاتيحها لكل نوع منتج — تفكيكها لأعمدة يعني جدولًا فرعيًا كاملًا لمكسب صفر، والحقل يُقرأ ككتلة واحدة دائمًا ولا يُستعلَم بداخله.
- **⚠️ الحقل بيانات لا عقد:** يُقرأ ويُحقَّق بـ`quotationItemPricingSchema` القائم قبل أي استعمال (نفس مسار TO-21-FIX) — لا يُوثق به لمجرد وجوده في القاعدة.
- **Migration safety:** إضافة بحتة، بلا backfill وبلا تعديل بيانات، قابلة للتراجع (`DROP COLUMN`). اسم مقترح: `to33_item_pricing_input`.
- **الأمر:**
  ```
  docker compose exec app npx prisma migrate dev --name to33_item_pricing_input
  docker compose exec app npx prisma generate
  docker compose exec app npm run build
  docker compose restart app
  ```
- **⚠️ `next build` يمرّ أخضر كاذبًا قبل الـmigration** (يتخطّى فحص الأنواع — مُثبَت في TO-25). البوابة الحقيقية `tsc --noEmit` والـruntime.

### SCR-025 — قاعدتا كمية بعدد الألواح (TO-40) · 🟢 مواصفات مكتملة — بانتظار تطبيق يوسف

> **L-02:** لم أعدّل `schema.prisma` ولا أنشأتُ migration. طبقة التطبيق منفّذة على `fix/to-40-panel-count` وتنتظر القيمتين.

**`enum QtyRule` (`prisma/schema.prisma:862-868`) — قيمتان مضافتان، صفر تعديل على القائم:**
```prisma
enum QtyRule {
  FIXED           // كما هي
  BY_AREA         // كما هي
  BY_LENGTH       // كما هي
  BY_CONFIG       // كما هي
  MANUAL          // كما هي
  /// TO-40: الكمية = defaultQty × عدد الألواح. (زوايا ×4 · بلتة أرضي ×2 · وسط ×1 · اسبايدر زوجي ×1)
  BY_PANEL
  /// TO-40: الكمية = defaultQty × (عدد الألواح + 1) — الخطوط الرأسية بين الألواح
  /// وعلى طرفيها، وعددها دائمًا أكثر من الألواح بواحد. (اسبايدر ثنائي ×2 · زعانف ×1)
  BY_PANEL_JOINT
}
```

- **السبب — من مقايسة حقيقية** (واجهة سبايدر `9.4 × 5.28` م · 6 ألواح): الزوايا 24 (`6×4`) · بلتة أرضي 12 (`6×2`) · بلتة وسط 6 (`6×1`) · اسبايدر زوجي 6 (`6×1`) — كلها `BY_PANEL`؛ و**اسبايدر ثنائي 14** (`(6+1)×2`) والزعانف 7 — كلها `BY_PANEL_JOINT`. الزجاج (49.632 = المساحة) والقطاع (29.36 = المحيط) يبقيان على `BY_AREA`/`BY_LENGTH` القائمتين بلا مساس.
- **لماذا قاعدتان لا واحدة:** الفرق بين «عدد الألواح» و«عدد الخطوط» فرق هندسي ثابت (n مقابل n+1)، لا مُعامل قابل للضبط. تمثيله بـ`defaultQty` وحدها كان يفرض كسورًا كاذبة (`14/6 = 2.333`) تنكسر عند أي عدد ألواح آخر.
- **Blast radius — صفر كسر:** إضافة قيمتين لـenum. `resolveQty` مبنية على `switch` شامل ⇒ القيم القائمة تسلك كما هي حرفيًا، ولا صف كتالوج واحد يستعملهما حتى يضبط المالك الوصفات. **التسعير القائم لكل المنتجات بلا أي تغيير رقمي.**
- **⚠️ TypeScript ينبّه لا ينكسر:** بعد `prisma generate` يصير `QtyRule` بسبع قيم، و`switch` غير الشامل يصير خطأ نوع — ولهذا تُضاف الحالتان في نفس الموجة (البند ب).
- **Migration safety:** إضافة قيم enum فقط، بلا عمود وبلا بيانات. ⚠️ **حذف قيمة enum في PostgreSQL غير مباشر** — التراجع يحتاج إعادة إنشاء النوع؛ التقدّم للأمام آمن تمامًا. اسم مقترح: `to40_qtyrule_by_panel`.
- **الأمر:**
  ```
  docker compose exec app npx prisma migrate dev --name to40_qtyrule_by_panel
  docker compose exec app npx prisma generate
  docker compose exec app npm run build
  docker compose restart app
  ```
- **🔴 بعد الـmigration — ضبط الكتالوج بيد المالك (لا كود):** أسطر وصفة الواجهات تُحوَّل إلى القاعدتين الجديدتين بـ`defaultQty` الصحيح (زوايا 4 · بلتة أرضي 2 · وسط 1 · اسبايدر زوجي 1 · اسبايدر ثنائي 2 · زعنفة 1). **قبل هذا الضبط لا يتغيّر أي رقم** — القاعدتان موجودتان بلا مستعمل.
- **⚠️ `next build` يمرّ أخضر كاذبًا قبل الـmigration** (يتخطّى فحص الأنواع — مُثبَت في TO-25). البوابة `tsc --noEmit` والـruntime.

### SCR-026 — قاعدة كمية بالمحيط (TO-44) · 🟢 مواصفات مكتملة — بانتظار تطبيق يوسف

> **L-02:** لم أعدّل `schema.prisma` ولا أنشأتُ migration. طبقة التطبيق منفّذة على `fix/to-39-discount-gate` وتنتظر القيمة.

**`enum QtyRule` — قيمة سادسة… سابعة (بعد TO-40)، صفر تعديل على القائم:**
```prisma
enum QtyRule {
  FIXED · BY_AREA · BY_LENGTH · BY_CONFIG · MANUAL · BY_PANEL · BY_PANEL_JOINT   // كلها كما هي
  /// TO-44: الكمية = defaultQty × محيط المستطيل = 2 × (الارتفاع + العرض).
  /// للمنتج **القائم بذاته** (واجهة) بأربعة أضلاع، مقابل `BY_LENGTH` التي تحسب
  /// ثلاثة أضلاع (h + 2w) للمنتج **المسنود على حائط** (شاور — الضلع الرابع حائط).
  BY_PERIMETER
}
```

- **السبب — من مقايسة حقيقية:** واجهة `9.4 × 5.28` ⇒ القطاع الصحيح **29.36** = `2×(9.4+5.28)`، بينما `BY_LENGTH` تعطي **24.08** = `5.28 + 9.4 + 9.4`. الفرق **5.28 م** (ضلع كامل) على كل واجهة.
- **🔴 لماذا قاعدة سطر لا خاصية منتج (قرار المالك بعد مراجعة):** «مسنود/قائم بذاته» ليس خاصية المنتج بل خاصية **السطر** — قد يحتاج منتج واحد قطاعًا محيطيًا لإطاره وقطاعًا ضلعيًا لتقسيمة داخلية في نفس الوصفة. عمود `isFreestanding` على `ProductType` كان سيغلق هذا الباب إلى الأبد.
- **البديل المرفوض:** الاشتقاق من كود المنتج (`FACADE_*`) — قائمة أكواد تُصان يدويًا وتُنسى عند أول منتج جديد.
- **`calcStrategy` لا يصلح:** فُحص أولًا لتجنّب أي migration — ثمانية من تسعة منتجات `RECIPE_FIXED`، و**صفر قارئ له في المشروع** (عمود ميت).
- **Blast radius — صفر كسر مضمون:** إضافة قيمة enum. كل الأسطر القائمة تبقى `BY_LENGTH` ⇒ **الشاور والكلادينج والأسقف والأرضيات والهاندريل بنفس أرقامها بالضبط**. لا صف كتالوج واحد يستعمل القيمة حتى يحوّلها المالك.
- **⚠️ `resolveQty` تُحدَّث في نفس الموجة** — `switch` غير الشامل يصير خطأ نوع بعد `prisma generate`.
- **Migration safety:** إضافة قيمة enum فقط، بلا عمود وبلا بيانات. اسم مقترح: `to44_qtyrule_by_perimeter`.
- **الأمر:**
  ```
  docker compose exec app npx prisma migrate dev --name to44_qtyrule_by_perimeter
  docker compose exec app npx prisma generate
  docker compose exec app npm run build
  docker compose restart app
  ```
- **🔴 بعده — ضبط الكتالوج بيد المالك (لا كود، بطلب صريح منه):** تحويل سطر قطاع الواجهات من `BY_LENGTH` إلى `BY_PERIMETER`. **قبل التحويل لا يتغيّر أي رقم.**

---

## موجة IN-C1 — قرار المطابقة والطوابع الزمنية (3 تغييرات schema)

> **✅ الحالة (2026-07-31): الهجرة طُبِّقت بيد يوسف، وطبقة التطبيق (المرحلة 2) مُنفَّذة.**
> مُتحقَّق على القاعدة: `dueDate` صار `is_nullable=YES` · الأعمدة السبعة موجودة ·
> `InspectionMatchResult` بثلاث قيم. المواصفات أدناه تبقى مرجعًا للقرارات لا خطةً معلَّقة.

> **L-02 [HARD]:** لم أعدّل `schema.prisma` ولم أُنشئ migration ولم أشغّل أي `prisma migrate`
> — الهوك حجب الثلاثة **ولم يُلتفّ حولها** (نفس سابقة SCR-019/SCR-020: «الحجب هو القانون يعمل»).
> ⚠️ **تنبيه على نصّ التكليف:** بروتوكوله يقرأ L-02 على أنه يمنع *تشغيل* `migrate` فقط؛ ونصّ
> القانون يمنع **تعديل الملف نفسه** أيضًا (`Never edit prisma/schema.prisma`)، و`L-03` يمنع
> **أي** أمر `prisma migrate` — بما فيه `--create-only`. المقاطع الجاهزة للّصق والـSQL كاملًا
> في تقرير الجلسة؛ يوسف يطبّقها بعد نسخة احتياطية مؤكَّدة.
>
> **واقع البيانات (SELECT حيّ، 2026-07-31):** 27 معاينة · كلها غير محذوفة · 7 `DONE` ·
> 4 `APPROVED` · 14 مُسنَدة. كل الأعمدة الجديدة **nullable بلا default** ⇒ الهجرة لا تفشل
> على أي صف قائم، وصفر backfill.

### SCR-INS-A — نتيجة المطابقة (ينفّذ IN-03 · D-IN-8 · D-IN-9) · 🟢 مواصفات مكتملة

- **الموديل:** `InspectionRequest`.
- **🔴 حقيقة تصميمية مُلزِمة لأي منفِّذ:** **النظام لا يقارن — النظام يسجّل حكم إنسان.** لا يوجد
  في القاعدة «مقاس مفترض» تُقارَن به مقاسات المعاينة (المبيعات لا تُدخل أبعادًا وقت الطلب —
  `summary` نص حر). المقارنة تحدث في رأس مدير المعاينات، والحقل يوثّق نتيجتها.
  **أي منطق مقارنة آلي = اختراع بلا مرجع (STD-05).**
- **enum جديد** — القيم الثلاث بنصّ حسن حرفيًا، لا رابعة ولا إعادة صياغة:
  `InspectionMatchResult { MATCHED · ACCEPTABLE_DEVIATION · REQUIRES_REPRICING }`
- **الأعمدة الأربعة (كلها nullable):**
  | العمود | النوع | الدلالة |
  |---|---|---|
  | `matchResult` | `InspectionMatchResult?` | `null` = **لم تُعلَن بعد** (ليست «مطابقة») |
  | `matchReason` | `String?` | إلزامي **في طبقة التطبيق** للقيمتين الأخيرتين — **لا قيد قاعدة بيانات** (D-IN-8) |
  | `matchDeclaredById` | `String?` | علاقة `User` — `onDelete: SetNull`، نفس نمط `approvedById` |
  | `matchDeclaredAt` | `DateTime?` | متى أُعلنت |
- **قواعد الطبقة التطبيقية (المرحلة 2):** (١) الإعلان **بعد الاعتماد حصرًا** — يُرفض إذا
  `approvalStatus !== "APPROVED"` (D-IN-9) · (٢) المُعلِن `INSPECTION_MANAGER` + `ADMIN`
  (متسق مع D-IN-1) · (٣) السبب إلزامي لـ`ACCEPTABLE_DEVIATION` و`REQUIRES_REPRICING`،
  اختياري لـ`MATCHED` · (٤) **يُكتب مرة واحدة** — يُرفض إذا `matchResult !== null` ·
  (٥) `ActivityLog` بالقيمة والسبب والمُعلِن · (٦) العرض في **ثلاث شاشات**: تفاصيل المعاينة ·
  عمود في قائمة المعاينات · **شاشة المكتب الفني** (المستهلك الحقيقي).
- **Blast radius — صفر كسر:** أربعة أعمدة **مضافة nullable** + enum **جديد منفصل** ⇒ لا قارئ
  قائم يتأثر، ولا تفرّع قائم يتغيّر. لا يمسّ `InspectionStatus` ولا `InspectionApprovalStatus`.
- **Migration safety:** إضافة enum + 4 أعمدة nullable + FK واحد. بلا backfill وبلا تعديل بيانات.
  **قابل للتراجع بالكامل** (`DROP COLUMN` ×4 + `DROP TYPE`). لا يفشل على الـ27 صفًا القائمة.
  اسم مقترح: `scr_ins_a_match_result`.

### SCR-INS-B — الطوابع الزمنية التشغيلية (ينفّذ IN-33) · 🟢 مواصفات مكتملة

- **الموديل:** `InspectionRequest`. **السابقة القائمة:** `Quotation.submittedAt`/`leadDecidedAt`
  (`schema.prisma:175,177`) — نفس النمط، لا اختراع.
- **الأعمدة الثلاثة (كلها `DateTime?`):**
  | العمود | يُكتب في | يفتح |
  |---|---|---|
  | `assignedAt` | `scheduleInspection` (`services/inspections.ts`) | حمل العمل · زمن التوزيع |
  | `submittedAt` | `submitInspectionForApproval` (`inspections/actions.ts`) | زمن التنفيذ الميداني |
  | `completedAt` | **موضعان** — انظر التحذير | `K2` زمن الدورة · `K3` نسبة SLA |
- **⚠️ `completedAt` له كاتبان لا واحد** (مُتحقَّق بالجرد): (١) `approveInspection` يكتب
  `status: "DONE"` حرفيًا منذ IN-50 (`inspections/actions.ts:945`) — وهو **المسار الأغلب**
  (4 من 7 صفوف DONE معتمدة)؛ (٢) `updateInspectionStatus` يكتب `status: parsed.data.status`
  **ديناميكيًا** فلا يظهر في grep على `"DONE"`. **إغفال أيٍّ منهما يُنتج عمودًا فارغًا صامتًا.**
  يُكتب في **نفس الـupdate** الذي يكتب `DONE`، وهي نفس النقطة التي تستدعي
  `recomputeAfterInspection` (IN-37).
- **🔴 الصفوف التاريخية تبقى `null` — ولا تُملأ من `ActivityLog`.** السبب مبدئي لا تقني:
  `ActivityLog` **أثر لا مصدر حقيقة** (BL-81)، وملء أعمدة تشغيلية منه يحوّله لمصدر حقيقة.
  **الأثر المقبول والمُصرَّح به:** مؤشرات الزمن تبدأ من تاريخ الهجرة، وما قبله **لا يُقاس**.
  **واجب على أي داشبورد لاحق:** يعرض «القياس يبدأ من [تاريخ]» صراحةً — لا متوسطًا محسوبًا
  على عيّنة ناقصة بصمت.
- **دلالة إعادة الكتابة — محسومة بسابقة المشروع لا باجتهاد:** `Quotation.submittedAt`
  **يُعاد ضبطه عند كل تقديم** بتعليق صريح (`lib/actions/lead-approval.ts:110-112`: «الدورة
  الجديدة تُقاس من تقديمها هي لا من الأول»). ⇒ **نفس الدلالة هنا:** `submittedAt` يُدهس عند
  كل إعادة تقديم بعد `RETURNED`، و`assignedAt` يُدهس عند كل إعادة إسناد — متسقًا مع
  `dueDate` الذي يُعاد حسابه عند كل جدولة (IN-09). الأثر الكامل للانتقالات محفوظ في
  `ActivityLog` (IN-28 يكتب `from/to`).
- **Blast radius — صفر كسر:** ثلاثة أعمدة مضافة nullable، صفر قارئ قائم.
- **Migration safety:** إضافة 3 أعمدة nullable. بلا backfill، قابل للتراجع بالكامل.
  اسم مقترح: `scr_ins_b_operational_timestamps`.

### SCR-INS-B2 — `dueDate` يصير nullable (يُغلق دَينًا مُصرَّحًا به) · 🟢 مواصفات مكتملة

- **⚠️ تصحيح مرجع:** التكليف ينسب هذا الدَين لـ**BL-160** — وBL-160 بند **جاهزية الموقع**
  (`SITE_READINESS_GATE_FROM`)، بند آخر تمامًا. الدَين الصحيح مُصرَّح به في الكود عند
  `services/inspections.ts:329-335` ضمن **IN-09**: «العمودان مدموجان في عمود واحد = دين
  مُصرَّح به، وحلّه SCR في تقرير الموجة». هذا هو الـSCR.
- **التغيير الوحيد:** `dueDate DateTime` ← `dueDate DateTime?`. **`null` = لم تُجدوَل بعد
  فلا مهلة تنفيذ.** الصفوف القائمة **تحتفظ بقيمتها** — لا تُفرَّغ.
- **لماذا:** العمود غير القابل للـnull أجبر B2 على كتابة قيمة عند الإنشاء **قبل أن يكون
  للمهلة معنى** (المعاينة لم تُجدوَل)، فصار يحمل دلالتين: «مهلة جدولة» ثم «مهلة تنفيذ».
- **🔴 جرد القرّاء الكامل (بحث شامل — 8 مواضع تحتاج معالجة `null` صراحةً):**
  | # | الموضع | المعالجة الإلزامية |
  |---|---|---|
  | 1 | `deriveEffectiveStatus` (`services/inspections.ts:73-77`) | **الأخطر.** `null` **لا يعني متأخرًا** — يعود `status` كما هو. بلا هذا تظهر كل معاينة جديدة «متأخرة» = عيب IN-09 نفسه عائدًا |
  | 2 | `daysRemaining` — `getInspections` (`:211`) | `null` ⇒ `null` لا `NaN` (طرح من `null` يُنتج NaN صامتًا) |
  | 3 | `daysRemaining` — `createInspection` (`:424`) | نفسه |
  | 4 | `daysRemaining` — `scheduleInspection` (`:615`) | نفسه |
  | 5 | أثر الجدولة (`:596`) | `current.dueDate.toISOString()` **يرمي على null** — يلزم `?.` |
  | 6 | DTO التفاصيل (`inspections/actions.ts:435`) | `.toISOString()` يرمي — يلزم `?? null` والنوع `string \| null` |
  | 7 | عمود القائمة (`inspections-client.tsx:197-203`) | `new Date(null)` ⇒ «1970» — يلزم فرع `—` |
  | 8 | ملف العميل (`services/customers.ts:261,404,409`) | النوع `Date` ← `Date \| null` + الاشتقاق |
  **ترك واحد بلا معالجة = معاينة جديدة تظهر «متأخرة».**
- **الفهرس `@@index([dueDate])` يبقى كما هو** — Postgres يفهرس `NULL` في B-tree بلا مشكلة،
  وصفر تغيير مطلوب عليه.
- **Blast radius:** تخفيف قيد (`NOT NULL` ← nullable) ⇒ **لا يفشل على أي صف قائم**، لكنه
  **يكسر البناء (TS) في المواضع الثمانية** — وهذا مقصود ومقبول: كسر صريح وقت البناء خير من
  عيب صامت وقت التشغيل (نفس منطق SCR-019).
- **⚠️ قابلية التراجع مشروطة:** العودة لـ`NOT NULL` تفشل **إن وُجد صف واحد بـ`null`**.
  التراجع ممكن **قبل** إنشاء أول معاينة غير مجدولة فقط، وبعده يلزم backfill أولًا.
  **هذا التغيير الوحيد في الموجة غير القابل للتراجع مجانًا** — يستحق تأنّي الدرس 12.
  اسم مقترح: `scr_ins_b2_duedate_nullable`.

### 🔴 ما لا يُبنى في C1 — مسجَّل بأمر صريح

| **BL-166** | 🟠 **التوجيه الشرطي المبني على نتيجة المطابقة — قيمة SCR-INS-A الحقيقية، ومؤجَّل عمدًا.**
المطلوب مستقبلًا: `REQUIRES_REPRICING` ← توجيه آلي لإعادة التسعير · `MATCHED` ← مسار التعاقد.
**لماذا لم يُبنَ في C1:** يمسّ **ثلاثة أقسام** (معاينات · مبيعات · مكتب فني)، ويحتاج قرارًا عن
آلية «إعادة التسعير»: هل هي زر `resubmitQuotation` القائم؟ حالة جديدة على `QuotationRequest`؟
إشعار فقط؟ بناؤه ضمنًا داخل موجة قسم كان سينتج نصف آلية يصعب اقتلاعها.
**بعد C1 يصير الحكم مسجَّلًا وظاهرًا، والمكتب الفني يقرأه ويتصرف يدويًا** — تحسّن حقيقي بذاته
(اليوم لا يوجد حكم أصلًا)، **لكنه ليس الأتمتة التي طلبها حسن**. 🔴 **يُبلَّغ حسن بهذه الصيغة
حتى لا ينتظر توجيهًا آليًا لم يُبنَ.** الحالة: مفتوح — قرار مطلوب قبل البناء.
---

| **BL-167** | 🟡 **«مهلة الجدولة» (D-IN-6) فقدت بيتها بعد SCR-INS-B2 — أثر معلَن لا مفاجأة.**
قبل C1 كان `dueDate` يُكتب عند الإنشاء بقيمة محسوبة، فأنتج **إشارة جانبية**: معاينة
تبقى بلا جدولة يومين تصير `OVERDUE` فتلفت المدير. بعد أن صار العمود `null` حتى الجدولة
(دلالة واحدة لعمود واحد — وهو المطلوب) **سقطت الإشارة**: `null` لا يتأخر.
**هذا ليس انحدارًا مستورًا:** الإشارة كانت أثرًا جانبيًا لاستعمال مزدوج لعمود واحد، لا
ميزة مقصودة. لكن **الحاجة التشغيلية قائمة** — طلب معاينة يُهمَل بلا توزيع يجب أن يظهر.
**الحل الصحيح:** «مهلة الجدولة» تحتاج بيتها الخاص لا عمودًا مستعارًا — مسجَّل أصلًا
كـ`SCR-INS-C` (مهلة SLA في `SystemSettings`، موجة C3). **الأثر بينهما:** لا تنبيه آليًا
على الطلبات غير الموزَّعة. **مخفَّف جزئيًا:** قسم «طلبات تحتاج إسنادًا» قائم في شاشة
المكتب الفني، وقائمة المعاينات تُظهر «لم تُجدوَل بعد» صراحةً. الحالة: مفتوح — يُغلق بـC3.
---

| **BL-168** | 🟡 **تصحيح نتيجة المطابقة بلا حدّ أقصى ولا قيد زمني** (منشؤه التحقق البصري لـC1).
`declareMatchResult` يسمح بالتصحيح بلا سقف عدد ولا نافذة زمنية — وهو **القرار الصحيح
اليوم** (قرار يوسف: تجنّب الصف المحبوس، سابقتا IN-07/IN-48؛ والضابط هو الأثر لا المنع).
**🔴 لماذا يصير خطرًا لاحقًا:** عند بناء التوجيه الآلي ([[BL-166]]) ستُحرّك النتيجةُ
عملًا في قسم آخر — إعادة تسعير أو مسار تعاقد. حينها **يكون المكتب الفني قد تصرّف على
قيمة سابقة**، فيغيّرها المدير بلا أن يعلم أحد أن الأساس تبدّل. اليوم الأثر محدود لأن
التصرّف يدوي وقارئه بشر.
**الخيارات (قرار مطلوب مع BL-166 لا قبله):** (أ) قفل التصحيح بعد أول قراءة/تصرّف من
المكتب الفني · (ب) نافذة زمنية · (ج) إبقاؤه مفتوحًا + **إشعار للمكتب الفني عند كل
تصحيح** (الأرخص والأقرب لنمط المشروع: الحوكمة بالإخطار لا بالمنع — R-02/D-32).
الحالة: مفتوح — **لا يُنفَّذ الآن**، يُحسم مع BL-166.
---

| **BL-169** | 🟠 **`QuotationRequest.inspectionOwnerId` عمود ميت — صفر كاتب في المستودع**
(اكتُشف في التحقق البصري لـC1). البحث الشامل على `src` و`scripts`: **صفر موضع كتابة**؛
وفي القاعدة **صفّ واحد من 27** يحمل قيمة (بقايا بذرة). شاشة المكتب الفني كانت تقرؤه
لعرض «مسؤول المعاينات» فتُظهر «—» دائمًا، بينما شاشة المعاينة تعرض المُسنَد إليه الحقيقي
من `InspectionRequest.assigneeId` ⇒ **شاشتان تتناقضان لنفس الطلب**.
**عولج app-layer في C1:** `getTecJobDetail` صار يقرأ `inspectionRequest.assignee.name`
ويُبقي العمود الميت **fallback** كي لا يُفقد الصفّ التاريخي الوحيد.
**المتبقي (schema، موجة هدم):** حذف العمود والعلاقة `TecInspectionOwner` بعد ترحيل
الصفّ الواحد أو إسقاطه بقرار. **⚠️ قارئ ثانٍ قائم:** `installations/[id]/page.tsx:38,75`
يقرأ نفس العمود الميت لعرض اسم مدير المعاينات — **لم يُعالَج في C1** (خارج نطاق الموجة)
وسيبقى «—» حتى الحذف أو التوصيل. يُضمّ لموجة C4 (الهدم) مع `InspectionPhoto`.
الحالة: مفتوح.
---

### ✅ السؤالان المفتوحان — **محسومان (قرار يوسف، 2026-07-31)**

1. **`completedAt` عند التراجع عن `DONE`: يتفضّى (`null`).**
   **القاعدة النافذة: `completedAt` مليان ⟺ الحالة `DONE`** — ثنائية واحدة قابلة للفحص
   بسطر SQL واحد، لا حالة ثالثة غامضة. مُنفَّذة في `updateInspectionStatus`
   (`completedAt: status === "DONE" ? new Date() : null`) وفي `approveInspection`.
2. **تصحيح إعلان مطابقة خاطئ: مسموح — لا «مرة واحدة».**
   لنفس الأدوار (`INSPECTION_MANAGER` + `ADMIN`)، والتصحيح يُسجَّل في `ActivityLog`
   بالانتقال **من←إلى + السبب** (فعل منفصل `MATCH_RESULT_CORRECTED`).
   **السبب:** تجنّب الصف المحبوس (سابقتا IN-07 و IN-48)، والإعلان **حكم بشري والبشر
   يخطئون**. الضابط هو الأثر لا المنع.
   ⚠️ **قيد مستقبلي مسجَّل:** عند بناء التوجيه الآلي ([[BL-166]]) يُعاد النظر في التصحيح
   **بعد أن يكون المكتب الفني قد تصرّف على النتيجة**.

---

## موجة IN-C2 — مسارات الانحراف (4 تغييرات schema) · 🟢 مواصفات مكتملة — بانتظار تطبيق يوسف

> **L-02 [HARD]:** لم أعدّل `schema.prisma` ولم أُنشئ migration ولم أشغّل أي `prisma migrate`.
> المقاطع الجاهزة للّصق والـSQL كاملًا في تقرير الجلسة (نفس سابقة SCR-019/020 وموجة C1).
>
> **الموضوع الواحد:** ماذا يحدث حين **لا** تمشي المعاينة في مسارها المستقيم.
> **المصدر:** إجابة حسن على Q5 حرفيًا + قرارات يوسف D-IN-17/18/19.
>
> **واقع البيانات (SELECT حيّ، 2026-07-31):** 27 معاينة — 12 `REQUESTED` · 8 `SCHEDULED` ·
> 7 `DONE` · صفر `OVERDUE` مخزَّنة. `siteReadiness`: 3 `true` · 1 `false` · **23 `NULL`**.
> بُعد الاعتماد: 21 `DRAFT` · 4 `APPROVED` · 2 `PENDING_APPROVAL`.

### 🔴 ثلاث حقائق من نصّ حسن — لا تُخلَط

| # | الحقيقة | الأثر التصميمي |
|---|---|---|
| 1 | التأجيل/الرفض/الإلغاء **مصدرها العميل** لا القسم | يلزم تمييز «من طلب» — وإلا حمّلت مؤشراتُ الأداء القسمَ قرارَ العميل |
| 2 | تنتهي بـ«حالة للمتابعة مع **الجودة**» | وموديول الجودة **غير موجود** ⇒ D-IN-3: حقل جاهز للربط، معطَّل |
| 3 | «لإعادة رفع المقاسات» سبب **مشروع** لإعادة الجدولة | **هذا مخرج IN-04** — الطريق المسدود بعد الاعتماد |

---

### SCR-INS-D — حالة «مؤجّلة» ومصدر قرار الانحراف

- **الموديل:** `InspectionRequest`.
- **قيمة جديدة على enum قائم:** `InspectionStatus` (اليوم 4 قيم، `schema.prisma:731-736`)
  تصير **5** بإضافة `POSTPONED`.
- **enums جديدة:**
  ```
  InspectionDeviationReason { CUSTOMER_POSTPONED · CUSTOMER_REJECTED · CUSTOMER_CANCELLED
                              UNSUITABLE_TIME · NO_STAFF_AVAILABLE · REMEASURE_REQUIRED · OTHER }
  DeviationSource          { CUSTOMER · INTERNAL }
  ```
  ✅ **الأسماء الأربعة حرة** — مُتحقَّق: صفر `enum SiteReadiness|DeviationSource|InspectionDeviationReason|InspectionReturnReason` في الschema.
- **⚠️ تضارب `POSTPONED` — مُتحقَّق ومُحتوى:** الاسم مستعمل بمعنى آخر (تأجيل العميل
  **قرارَ الشراء** لا المعاينة) في `REJECT_REASONS`
  (`customers/[id]/stage-change-dialog.tsx:40`). **لكنه ثابت عميل لا enum قاعدة بيانات**
  ⇒ صفر تضارب على مستوى Postgres. البادئة `CUSTOMER_` تمنع الالتباس الدلالي، والقيمة
  المضافة لـ`InspectionStatus` اسمها `POSTPONED` في نطاق enum مختلف تمامًا.
- **الأعمدة الستة (كلها nullable — لا `@default` يكذب على التاريخ):**
  | العمود | النوع | ملاحظة |
  |---|---|---|
  | `deviationReason` | `InspectionDeviationReason?` | السبب المقنَّن |
  | `deviationNote` | `String?` | نصّ حرّ **بجوار** المقنَّن لا بدلًا منه |
  | `deviationRequestedBy` | `DeviationSource?` | تنفيذ الحقيقة 1 |
  | `deviationAt` | `DateTime?` | متى |
  | `deviationById` | `String?` | **من سجّلها لا من طلبها** — علاقة `User`, `SetNull` |
  | `qualityFollowUp` | `Boolean?` | D-IN-3 — **جاهز للربط، معطَّل** |
- **⚠️ `qualityFollowUp`: `Boolean?` بلا `@default(false)`.** التكليف يقترح
  `Boolean? @default(false)` وهو **تناقض داخلي**: nullable + default يجعل `null` غير
  قابل للحدوث عمليًا فيضيع تمييز «لم يُقرَّر» عن «قُرِّر: لا». وقاعدة الموجة نصّها
  «كل الأعمدة الجديدة nullable بلا استثناء — لا `@default` يكذب على الصفوف التاريخية».
  ⇒ **`Boolean?` بلا default**، و`null` = لم يُقرَّر بعد. **قرار يوسف مطلوب إن أراد غير ذلك.**
- **قواعد الطبقة (المرحلة 2):** (١) `OTHER` تستلزم `deviationNote` (علّة IN-23) ·
  (٢) `CUSTOMER_*` **تفرض** `deviationRequestedBy = CUSTOMER` خادميًا لا تُترك للمستخدم ·
  (٣) إشعار المبيعات **إلزامي** لمالك العميل بـfallback لـ`SALES_MANAGER` (نمط B2) ·
  (٤) `ActivityLog` بالسبب والمصدر · (٥) `qualityFollowUp` **يُكتب ولا يُقرأ** حتى يوجد
  الموديول — يُوثَّق في الكود كي لا يُقرأ كعمود منسيّ.

#### 🔴 جرد قرّاء `InspectionStatus` — 9 مواضع تحتاج قرارًا صريحًا

| # | الموضع | القرار |
|---|---|---|
| 1 | `deriveEffectiveStatus` (`services/inspections.ts:84`) | **`POSTPONED` لا تتأخر.** الشرط اليوم `status !== "DONE"` ⇒ المؤجَّلة ستُحتسب `OVERDUE` وتُحمَّل على القسم قرارَ العميل (خرق الحقيقة 1). يصير الاستثناء قيمتين |
| 2 | `statusEnum` (`inspections/actions.ts:704`) | **`POSTPONED` لا تُضاف.** لها إجراؤها الخاص الذي يفرض السبب — نفس درس IN-07 حرفيًا |
| 3 | زر الجدولة (`inspections-client.tsx:257`) | الشرط `status === "DONE" \|\| approvalStatus === "APPROVED"` ⇒ **المؤجَّلة تبقى قابلة للجدولة تلقائيًا** ✅ لا تعديل — وهو المطلوب (منع الصف المحبوس) |
| 4 | `STATUSES` فلتر القائمة (`inspections-client.tsx:54`) | تُضاف `POSTPONED` وإلا صارت صفوف غير قابلة للفلترة |
| 5 | شارة الحالة + `InspectionStatus` type (`inspection-detail-client.tsx:41,47`) | يُضاف للنوع، و**لا** يُضاف لـ`STATUS_OPTIONS` (مرآة القرار 2) |
| 6 | `getPendingInspectionsCount` (`services/inspections.ts:211`) | `status: { not: "DONE" }` ⇒ المؤجَّلة **تُحتسب معلّقة**. متسق مع D-IN-19 (تظهر عاديًا) ✅ لا تعديل |
| 7 | `inspectionActive` (`status-derivation.ts:178`) | 🔴 **الأخطر.** نفس الشرط ⇒ المؤجَّلة **نشطة** ⇒ العميل يبقى `INSPECTION`. **القرار: نعم، هذا صحيح** (لم تنتهِ) ✅ لا تعديل — لكن يجب تأكيده بالقراءة لا افتراضه |
| 8 | داشبورد المبيعات (`sales-dashboard.ts:101`) | نفس الشرط ✅ لا تعديل |
| 9 | `getSelectableRequests` (`inspections/actions.ts:217`) | يقرأ `QuotationRequest.status` **لا** حالة المعاينة — **خارج النطاق** (تنبيه: الاسم يُضلِّل في البحث) |
| — | i18n | `inspections.status_POSTPONED` في `ar` و`en` معًا |

**الخلاصة: 4 مواضع تُعدَّل (1·2·4·5) · 4 صحيحة بلا تعديل (3·6·7·8) · 1 خارج النطاق (9).**

- **Migration safety:** إضافة قيمة enum + 6 أعمدة nullable + FK. ⚠️ **إضافة قيمة لـenum
  قائم في Postgres غير قابلة للتراجع مباشرةً** (`ALTER TYPE … DROP VALUE` غير مدعوم) —
  التراجع يستلزم إعادة بناء النوع. الأعمدة تتراجع بـ`DROP` نظيف.
  اسم مقترح: `scr_ins_d_postponed_deviation`.

---

### SCR-INS-E — أسباب الإرجاع مقنَّنة (يُنفّذ IN-23 · D-IN-17)

- **الموديل:** `InspectionRequest`. **عمود واحد + enum واحد.**
- `returnReasonCode InspectionReturnReason?` **بجوار** `returnReason String?` القائم —
  **لا يُحذف النصّي ولا تُهاجَر بياناته** (نصّ التكليف).
  ```
  InspectionReturnReason { MEASUREMENTS_INCOMPLETE · MEASUREMENTS_UNCLEAR
                           PHOTOS_MISSING · WRONG_LOCATION · OTHER }
  ```
- القيم **معتمدة (D-IN-17)** — نُفِّذت كما هي بلا إعادة عرض.
- **الحقل النصّي يبقى إلزاميًا عند الإرجاع** (`returnInspectionSchema` →
  `errors.returnReasonRequired`، مُتحقَّق ميدانيًا: «مع كتابة السبب إجباري»).
- **Blast radius — صفر كسر:** عمود مضاف nullable + enum جديد. القارئ الوحيد
  (`returnReason` في شاشة التفاصيل) يبقى يعمل بلا تعديل حرف.
- **Migration safety:** إضافة enum + عمود nullable. قابل للتراجع بالكامل.
  اسم مقترح: `scr_ins_e_return_reason_code`.

---

### SCR-INS-H — مسار إعادة القياس (يفتح IN-04 **جزئيًا**)

- **الطريق المسدود:** معاينة `APPROVED` لا تُعدَّل مقاساتها (IN-13) ولا تُربَط بمعاينة
  ثانية — `quotation_requests.inspectionRequestId String? @unique`
  (**`schema.prisma:1103`**؛ التكليف ذكر 1088 — انزياح مرجع، الصحيح 1103).
- **الأعمدة الثلاثة (nullable):** `remeasureRequestedAt DateTime?` ·
  `remeasureRequestedById String?` (`User`, `SetNull`) · `remeasureReason String?`.
- **الآلية — إجراء واحد `requestRemeasure` في معاملة واحدة:** يتحقق أن
  `approvalStatus = APPROVED` → يعيدها `PENDING_APPROVAL` (**فيُفتح القفل تلقائيًا بلا لمس
  حرّاس IN-13**) → يكتب الحقول الثلاثة → **يمسح `matchResult`/`matchReason`/
  `matchDeclaredById`/`matchDeclaredAt`** مع تسجيل القيمة القديمة في `ActivityLog` →
  `completedAt = null` و`status = SCHEDULED` (التزامًا بقاعدة **`completedAt` مليان ⟺ `DONE`**)
  → يُخطر المندوب والمبيعات **والمكتب الفني**.
- **🔴 الحدود الأربعة:** (١) **لا تُلمس `@unique`** — إعادة القياس **دورة داخل المعاينة**
  لا معاينة ثانية · (٢) الصلاحية `INSPECTION_MANAGER` + `ADMIN` فقط · (٣) **إخطار المكتب
  الفني بإبطال المقاسات إلزامي** — كان قد أُخطر بها وقد يكون سعّر عليها، وإغفاله = IN-37
  بالعكس · (٤) **D-IN-18: تُمنع بوجود عقد.**
- **سلسلة فحص العقد — مُتحقَّقة على القاعدة:** `InspectionRequest` ←
  `quotation_requests.inspectionRequestId` ← `.quotationId` ← `contracts.quotationId`
  (**`@unique`, `schema.prisma:952`**). يُرفض بمفتاح صريح `errors.remeasureBlockedByContract`
  **لا برسالة عامة** — المستخدم يجب أن يعرف أن المانع هو العقد لا عطل.
- **⚠️ واقع البيانات يقول إن الحارس لن يُختبَر اليوم:** المعاينات الأربع `APPROVED`
  **صفر منها له عقد**، والعقدان القائمان على معاينتين `DRAFT` (لا تصلان شرط `APPROVED`
  أصلًا). ⇒ الحارس صحيح لكن **غير قابل للإثبات ميدانيًا على البيانات الحالية** — يلزم
  تكوين حالة اختبار، ولا يُدَّعى أنه مُثبَت بلا ذلك.
- **Migration safety:** 3 أعمدة nullable + FK. قابل للتراجع بالكامل.
  اسم مقترح: `scr_ins_h_remeasure`.

| **BL-171** | ✅ **مُغلق (موجة C2-fix، 2026-07-31).** أُضيفت قيمة `CANCELLED` واحدة
تكفي للحالتين (رفض · إلغاء) — التفرقة محفوظة في `deviationReason`. والإغلاق يقع
**تتاليًا** من `changeCustomerStage` عند `REJECTED` (D-IN-20: القفل مسؤولية المبيعات).
`CANCELLED` نهائية مثل `DONE`: لا تُعدّ معلّقة · لا تُبقي العميل في «معاينة» · لا تُجدوَل ·
لا تتأخر. التصنيف في مصدر واحد `services/inspection-status.ts` يستهلكه سبعة قرّاء.
🔴 **يبقى مفتوحًا [[BL-172]]** (بعث المعاينة الملغاة عبر مسارات الكتابة). **النصّ الأصلي:**
| ~~BL-171 (الوصف الأصلي)~~ | 🟡 **الرفض والإلغاء لا حالة لهما في `InspectionStatus`** (رُصد أثناء تنفيذ SCR-INS-D).
`recordDeviation` يحوّل الحالة إلى `POSTPONED` لثلاثة أسباب فقط (`CUSTOMER_POSTPONED` ·
`UNSUITABLE_TIME` · `NO_STAFF_AVAILABLE`). أما **`CUSTOMER_REJECTED` و`CUSTOMER_CANCELLED`
فلا تُلمس لهما الحالة** — لأن `InspectionStatus` لا يحوي `REJECTED`/`CANCELLED`، وإضافتهما
لم تُطلب في C2 (نطاق الموجة: قيمة واحدة `POSTPONED`).
**الأثر اليوم محدود ومحتوى:** السبب المقنَّن `deviationReason` يحمل الحقيقة كاملةً وظاهرًا
في الشاشة والسجل، فلا معلومة ضائعة. **لكن** معاينة ألغاها العميل تبقى `REQUESTED`/`SCHEDULED`
⇒ تُحتسب «معلّقة» في العدّاد، وتبقى `inspectionActive` فيبقى العميل في مرحلة «معاينة».
**القرار المطلوب:** هل تُضاف قيمتان للenum (تغيير schema)، أم يكفي السبب المقنَّن، أم
تُعامَلان كـ`DONE` تشغيليًا؟ يمسّ العدّادات واشتقاق المرحلة ⇒ لا يُجتهد فيه. الحالة: مفتوح.
---

| **BL-170** | 🔴 **اختلاف المقاسات المكتشَف بعد التعاقد بلا مسار.**
إعادة القياس ممنوعة بقرار **D-IN-18** (قبل التعاقد فقط)، **ولا بديل مبني**. أي أن الحالة
الأصلية التي رصدها **IN-04** — اختلاف يُكتشف **بعد** التعاقد — **تبقى بلا مخرج في النظام**.
هذا **اختيار واعٍ لا سهو**: ما بعد التعاقد يمسّ **قيمة العقد**، وهو قرار تجاري لا تشغيلي
(ومتسق مع L-08: العقد الموقّع سند لا يُعدَّل، والتغيير = ملحق + عرض جديد).
**يُحسم مع حسن وعمرو معًا.** الحالة: مفتوح — **IN-04 مُغلق نصفه فقط**.
---

### BL-160 — جاهزية الموقع ثلاثية القيم (**يُغلق البند**)

- **المشكلة:** `siteReadiness Boolean?` (`schema.prisma:259`) لا تفرّق بين «سُئل العميل
  فقال لست متأكدًا» و«لم يُسأل أحد» — الاثنان `null`. ولذلك اضطرت B2 لحدّ زمني **صلب**
  (`SITE_READINESS_GATE_FROM = 2026-07-30`, `services/inspections.ts`) للتمييز بالتاريخ.
- **الحل:** `enum SiteReadiness { READY · NOT_READY · UNCONFIRMED }` وتحويل العمود.
- **الترحيل:** `true → READY` · `false → NOT_READY` · `null` **تبقى `null`** (لم يُسأل).
- **المكسب:** الحدّ الزمني الصلب **يسقط** — البوابة تفحص `UNCONFIRMED` صراحةً بدل
  تخمينها من `createdAt`، ويسقط معه العرَض المسجَّل في تحديث B3 (لافتة تعد بحجب لا يقع).
- **⚠️ أخطر ما في الموجة — تغيير نوع عمود قائم.** يلزم `USING` صريح. **الأثر على
  البيانات: 4 صفوف تتحوّل (3 `true` + 1 `false`) و23 تبقى `NULL`.**
- **🔴 جرد قرّاء `siteReadiness` — 7 مواضع:**
  | # | الموضع | التغيير |
  |---|---|---|
  | 1 | `createSchema` (`actions.ts:49`) | يستقبل الثلاثية **سلفًا** كنصّ ✅ يصير enum مباشرةً |
  | 2 | `toSiteReadiness` (`actions.ts`) | 🗑️ **تُحذف** — كانت جسر الثلاثية↔البوليان، وتسقط الحاجة إليها |
  | 3 | `siteReadinessSchema` (`actions.ts:788`) | `z.boolean().nullable()` ← enum nullable |
  | 4 | `updateSiteReadiness` details (`actions.ts:848-852`) | ثلاثية `true/false/null` ← 4 حالات مع `UNCONFIRMED` |
  | 5 | بوابة الجدولة (`services/inspections.ts`) | `=== null && createdAt >= GATE` ← `=== "UNCONFIRMED"`؛ **`SITE_READINESS_GATE_FROM` يُحذف** |
  | 6 | لافتتا حوار الجدولة (`inspections-client.tsx:472,481`) | `=== false` ← `=== "NOT_READY"` · `=== null` ← `=== "UNCONFIRMED"` |
  | 7 | أزرار التفاصيل + DTO (`inspection-detail-client.tsx`, `actions.ts:435`) | `boolean \| null` ← نوع الenum؛ والحوار (`request-inspection-dialog.tsx:34`) يستعمل نفس القيم الثلاث **سلفًا** ✅ |
- **⚠️ `null` تبقى قيمة رابعة ضمنية بعد التحويل** (23 صفًّا) — «لم يُسأل». هذا مقصود
  (لا اختراع قيمة للتاريخ)، لكن **الطبقة يجب أن تعرضها بنصّها** لا كـ`UNCONFIRMED`،
  وإلا ادّعينا سؤالًا لم يقع. الفرق بينهما هو **جوهر البند**.
- **Migration safety:** تحويل نوع بـ`USING` + `DROP DEFAULT` أولًا. **قابل للتراجع**
  بتحويل عكسي (`READY→true` · `NOT_READY→false` · `UNCONFIRMED→null`) **مع فقد التمييز**
  الذي أُنشئ البند لأجله. اسم مقترح: `bl160_site_readiness_enum`.

---

## موجة IN-C2-fix — إغلاق المعاينة عند رفض العميل · 🟢 مواصفات مكتملة — بانتظار تطبيق يوسف

> **L-02 [HARD]:** لم أعدّل `schema.prisma` ولم أُنشئ migration ولم أشغّل أي `prisma migrate`.
> المقطع والـSQL في تقرير الجلسة. **القاعدة:** `e6f28ac` (دمج C2، PR #33).
>
> **سبب الموجة:** C2 بنت **تسجيل** الانحراف ولم تبنِ **إغلاقه** — فمعاينة ألغاها العميل
> تبقى «نشطة» للأبد. **فجوة في مواصفة C2 نفسها:** حسن ذكر ثلاثة مخارج (تأجيل · رفض ·
> إلغاء) ونُمذج واحد. يُغلق [[BL-171]].
>
> **⚠️ دليل ميداني أن العيب حيّ الآن (SELECT حيّ، 2026-07-31):** عميل واحد في مرحلة
> `REJECTED` **وله معاينة مفتوحة غير منتهية** — الحالة التي يصفها البند، قائمة في القاعدة.

### D-IN-20 (قرار يوسف) — القفل مسؤولية المبيعات

رفض العميل أو إلغاؤه للمعاينة = **خروج من الصفقة**. القفل مسؤولية **المبيعات** لا المعاينات.
⇒ **لا إجراء جديد ولا شاشة.** الإجراء القائم `changeCustomerStage`
(`src/lib/actions/customers.ts`) هو نقطة الربط — يُغلق المعاينات المفتوحة **تتاليًا**.
«العميل المحتمل لاحقًا» يُستخرَج من التقارير لا من مسار منفصل.

### SCR-INS-I — قيمة `CANCELLED` + تتالي الإغلاق

- **قيمة واحدة على enum قائم:** `InspectionStatus` (5 قيم اليوم) تصير **6** بإضافة
  `CANCELLED`. **هجرة مستقلة بسطر واحد** — نفس نمط `20260731180000_c2_add_postponed_status`
  الذي نجح. صفر عمود جديد، صفر enum جديد.
- **🔴 لماذا قيمة جديدة ولا يُعاد استعمال `DONE`:** `DONE` تعني **أن المعاينة نُفِّذت**.
  استعمالها للإلغاء يفسد ثلاثة أشياء معًا: (١) `completedAt` يُكتب — والقاعدة النافذة
  «`completedAt` مليان ⟺ `DONE`» ⇒ **`K2` يقيس معاينات لم تحدث**؛ (٢)
  `recomputeAfterInspection` يعاملها إنجازًا؛ (٣) التقارير لا تفرّق «تمّت» عن «أُلغيت» —
  وهو بالضبط ما طُلب استخراجه لاحقًا.
- **قيمة واحدة تكفي للحالتين** (رفض · إلغاء): التفرقة محفوظة في `deviationReason`
  المبني في C2 (`CUSTOMER_REJECTED` / `CUSTOMER_CANCELLED`).
- **التتالي (طبقة التطبيق، المرحلة 2):** بعد نجاح `REJECTED` وفي **نفس المعاملة** —
  جلب معاينات العميل غير `DONE` وغير `CANCELLED` وغير المحذوفة → `status = "CANCELLED"` →
  **لا يُدهس انحراف مسجَّل سلفًا** (لو `deviationReason` موجودة تُترك؛ وإلا تُكتب
  `CUSTOMER_REJECTED` + `CUSTOMER` + الوقت + الفاعل + **سبب الرفض المُلزَم مُعادًا استخدامه**
  في `deviationNote` — لا سؤال ثانٍ) → `ActivityLog` لكل معاينة (من ← `CANCELLED`) →
  **إشعار للمندوب المُسنَد** (ذاهب لموقع لن يُعاين).
- **الحدود:** `DONE` لا تُمَس (تمّت فعلًا وهي جزء من `K2`) · `CANCELLED` سلفًا لا تُمَس
  (تشغيل مكرر) · **التتالي أحادي الاتجاه**: إعادة العميل من `REJECTED` **لا تُعيد فتح**
  المعاينات (الفتح بطلب جديد) — يُوثَّق في الكود · **لا تُلمس حرّاس `changeCustomerStage`
  ولا تُضاف صلاحيات** (أثر جانبي لإجراء قائم لا إجراء جديد) · **فشل التتالي لا يُبطل رفض
  العميل** لكنه `console.error` لا يُبتلع (نمط D-39).

#### 🔴 جرد قرّاء `DONE` — التكليف ذكر ثلاثة، والبحث أخرج **سبعة تحتاج تعديلًا**

| # | الموضع | النموذج | القرار |
|---|---|---|---|
| 1 | `NEVER_OVERDUE_STATUSES` (`services/inspections.ts:98`) | Inspection | **يُضاف** — `CANCELLED` لا تتأخر (نفس منطق `POSTPONED`: قرار العميل لا يُحمَّل على القسم) |
| 2 | `getPendingInspectionsCount` (`services/inspections.ts:226`) | Inspection | `notIn: ["DONE","CANCELLED"]` |
| 3 | حارس الجدولة (`services/inspections.ts:528`) | Inspection | 🔴 **لم يُذكر في التكليف.** `if (current.status === "DONE")` — بدونه **تُعاد جدولة معاينة ملغاة فتُبعث صامتةً** |
| 4 | داشبورد المبيعات (`services/sales-dashboard.ts:101`) | Inspection | 🔴 **لم يُذكر.** نفس العدّاد للمبيعات ⇒ رقمان متناقضان على شاشتين |
| 5 | `inspectionActive` (`status-derivation.ts:120`) | Inspection | `!== "DONE"` ⇒ يصير استثناءً لقيمتين — وإلا بقي العميل المرفوض في مرحلة «معاينة» |
| 6 | `activeInspection` (`status-derivation.ts:178`) | Inspection | نفسه |
| 7 | زر الجدولة (`inspections-client.tsx:265`) | واجهة | مرآة الحارس 3 (STD-15) — زر يظهر ثم يفشل |

**صحيحة بلا تعديل (2):** `completedAt` الثلاثي (`actions.ts:761`) — `CANCELLED` ⇒ `null`
تلقائيًا ✅ · مميِّز الطابع التاريخي (`inspection-detail-client.tsx:731`) ✅.

**⚠️ خارج النطاق — ثلاثة مواضع على `QuotationRequest` لا `InspectionRequest`:**
`actions.ts:215` · `services/inspections.ts:362` · `services/inspections.ts:418`.
🔴 **والثالث منها هو ما ذكره التكليف كبند «القارئ 2»** — إحالة غير دقيقة:
السطر `418` داخل `tx.quotationRequest.updateMany` (الربط الشرطي في `createInspection`)
ويخصّ حالة **الطلب**، وتعديله كان سيغيّر منطق ربط لا علاقة له بإلغاء المعاينة.

**العرض:** `inspections.status_CANCELLED` في `ar`+`en` · `STATUSES` في فلتر القائمة ·
`INSPECTION_STATUS_COLORS`.
⚠️ **وعيب مكتشَف من C2 نفسها:** `POSTPONED` أُضيفت للحالة **ولم تُضَف للألوان**
(`lib/status-colors.ts:12-17`) فتسقط على fallback رمادي. يُصلَح مع `CANCELLED` في نفس المرور.

- **`CANCELLED` لا تُضاف لـ`statusEnum`** (`actions.ts`) — تُكتب بالتتالي وحده،
  نفس قرار `POSTPONED` ونفس درس IN-07.
- **Migration safety:** إضافة قيمة enum واحدة. ⚠️ **غير قابلة للتراجع مباشرةً في
  Postgres** (`ALTER TYPE … DROP VALUE` غير مدعوم). صفر أثر على أي صف قائم.
  اسم مقترح: `c2fix_add_cancelled_status`.

| **BL-172** | 🟡 **مسارات الكتابة تحرس `approvalStatus` لا `status` — معاينة ملغاة تقبل الكتابة**
(رُصد أثناء جرد C2-fix). `addMeasurementAction` · `addInspectionAttachment` ·
`submitInspectionForApproval` كلها تفحص `approvalStatus === "APPROVED"` فقط
(`actions.ts:527,560,652,744,856`) **ولا تفحص `status` إطلاقًا**. ⇒ معاينة `CANCELLED`
بـ`approvalStatus = DRAFT` تقبل مقاسات ومرفقات، **ويمكن تقديمها ثم اعتمادها فتصير `DONE`
— أي تُبعَث بعد إلغائها**. الأثر اليوم قائم لـ`DONE` أيضًا (نفس الثغرة) لكنه نظري لأن
`DONE` تأتي عبر الاعتماد. **القرار المطلوب:** هل تُحجَب الكتابة على `CANCELLED`/`DONE`
بحارس `status`؟ **ليس بديهيًا:** مندوب قد يكون في الموقع لحظة رفض المبيعات، وحجب حفظ
عمله يُفقده إياه. الخيارات: (أ) حجب كامل · (ب) سماح بالحفظ ومنع التقديم للاعتماد ·
(ج) تركه وتنبيه في الواجهة. **لم يُنفَّذ في C2-fix — خارج نصّ التكليف (L-14).** الحالة: مفتوح.
---

| **BL-175** | 📌 **صفّان يحملان تناقض حالة/سبب من قبل D-IN-22 — لا يُصحَّحان تلقائيًا.**
`D-IN-22` (C2-fix-2) أصلح **المسار** فلا يتكرّر التناقض، لكنه **لا يمسّ الصفوف القائمة**
(لا backfill — تصحيح بيانات بلا فاعل بشري يخالف مبدأ الأثر). الصفّان المُتحقَّق منهما:

| المعرّف | الحالة | السبب | ما كانت ستصير لو سُجِّل الانحراف اليوم |
|---|---|---|---|
| `cmrh79ya9000imo3tffaiit4s` | `POSTPONED` | `CUSTOMER_CANCELLED` | `REQUESTED` (بلا `scheduledAt`) |
| `cmrzo0aoq0019mo3lasmkg6y6` | `POSTPONED` | `NO_STAFF_AVAILABLE` | ✅ **متسق** — التأجيل يستوجبه فعلًا |

⇒ **صفّ واحد فقط متناقض حقًّا** (`cmrh79ya9…`): «مؤجَّلة» تعني ستُستأنف، وسببها «إلغاء
العميل» يعني لن تُستأنف. **العلاج:** يوسف يسجّل عليه انحرافًا جديدًا من الشاشة فيصحّح
المسارُ الحالةَ تلقائيًا — أو يُترك موثَّقًا. الحالة: مفتوح — إجراء تشغيلي لا كود.
---

### SCR مقترَح — `Customer.updatedById` (يُغلق SF-21) · ⚠️ **مواصفة، لم تُطبَّق**

| **BL-174** | 🟠 **`Customer` بلا عمود «آخر مُعدِّل» — البند 2 في C2-fix-2 كان مبنيًا على
مرجع خاطئ ولم يُنفَّذ.**
🔴 **الحقيقة المُتحقَّقة (C2-fix-2):** التكليف نصّ على أن «`Customer.updatedById` موجود
(`schema.prisma:540`) والإجراء لا يكتبه». **الموضع 540 يخصّ `SystemSettings` لا `Customer`**،
و`Customer` **لا يملك العمود إطلاقًا**: `information_schema` أعاد `updatedAt` وحده، وكل
مطابقات `updatedById` في الschema ثلاث — `Quotation:200` · `SystemSettings:540` ·
`PriceListItem:593`. ⇒ الكتابة كانت **مستحيلة** لا متخطّاة، و`tsc` رفضها صراحةً
(`'updatedById' does not exist in type CustomerUpdateInput`). **التعديل رُوجِع بالكامل.**
⇒ [[SF-21]] يبقى بتشخيصه **الأصلي وهو الصحيح**: العرض يقرأ `ownerName || coveredByName`.

**المواصفة (تحتاج قرارك — L-02):** `Customer.updatedById String?` + علاقة `User` بـ`SetNull`
(نفس نمط `Quotation.updatedById` من TO-23 — عمود scalar بلا relation هناك؛ يُختار النمط).
**Migration safety:** عمود nullable مضاف، صفر backfill، تراجع نظيف بـ`DROP COLUMN`.

**⚠️ ومسارات الكتابة السبع تحتاج قرارًا لا ملءً ميكانيكيًا** (الجرد كامل):

| # | المسار | الفاعل |
|---|---|---|
| 1 | `lib/actions/customers.ts` `changeCustomerStage` | بشري 🔴 **الأولوية** — D-IN-21 فتحه للمندوبين |
| 2 | `lib/actions/customers.ts` `assignCustomer` | بشري |
| 3 | `lib/actions/customers.ts` `setCustomerCoverage` | بشري |
| 4 | `lib/services/customers.ts` `createCustomer` | بشري (يُناقَش: هل يُملأ عند الإنشاء؟) |
| 5 | `lib/services/customers.ts` `updateCustomer` | بشري — «التعديل» بالمعنى الحرفي |
| 6 | `lib/services/customers.ts` `setCustomerVip` | بشري |
| 7 | `lib/services/contract-core.ts` (`isRepeat`) · `status-derivation.ts` (المرحلة) | **آلي** ⚠️ |

🔴 **الاثنان الآليان (7) لا يُملآن باسم مستخدم** — «آخر تعديل بواسطة» سيكذب: لم يعدّله بشر.
الخياران: تركهما `null`، أو قيمة تدلّ على «النظام». أثرهما موجود في `ActivityLog`
(`STAGE_DERIVED`) لا هنا. **يُحسم قبل التنفيذ.** الحالة: مفتوح — بانتظار قرار SCR.
---

| **BL-173** | 📌 **`deviationReason` هو أساس تقارير «العميل المحتمل لاحقًا» — لا يُحذف ولا يُطوى.**
D-IN-20 حسم أن العميل الذي رفض اليوم وقد يعود لاحقًا **يُستخرَج من التقارير لا من مسار
منفصل**. الحقل الذي ستُبنى عليه تلك التقارير هو `InspectionRequest.deviationReason`
(بقيمه السبع) مع `deviationRequestedBy` و`deviationAt` — وهي المضافة في C2.
🔴 **تسجيل وقائي:** أي تنظيف مستقبلي (توحيد أعمدة · حذف «غير مستعمل» · طيّ الأسباب في
نصّ حرّ) **يجب أن يمرّ على هذا البند أولًا**. الحقل يبدو اليوم بلا قارئ تقريري — وهذا
مقصود ومؤقَّت، لا إهمال. الحالة: ملاحظة دائمة، لا عمل مطلوب.
---
# 🟠 مسارات عمل ناقصة

| # | البند | المصدر |
|---|---|---|
| **BL-57** | **عند نزول موديول المخازن (BL-21): "تجهيز من المخزون" = مسار مشروع بلا مصنع.** بوابة READY تتوسّع إلى: مصنع مُعيَّن **أو** تجهيز من المخزون. اليوم لا مخزون → المصنع إلزامي. | مرحلة 2 |
| **BL-58** | ❓ **اسم `IN_PRODUCTION` لم يعد دقيقًا** — يعني الآن "معتمد بانتظار توجيه PROCUREMENT". إعادة التسمية = تغيير enum = SCR. مؤجل. | يوسف (SCR) |
| **BL-55** | 🟠 **مسار إشعار التحويل غير مبني** — SALES ترفع صورة التحويل (واتساب) على ملف العميل + إشعار للحسابات. **ليس بلوكرًا:** ACCOUNTING تسجّل الدفعة والصورة تصل خارج النظام (D-13). دفعة منفصلة. | يوسف |
| **BL-65** | ✅ **مُغلق (2026-07-12، قرار يوسف: أي خصم>0=طلب).** `createQuotation`: `discountNeedsApproval = discountPct.gt(0)` (بدل `> base`) — **أي خصم يفتح DiscountRequest، صفر تطبيق مباشر**. الوسم "استثنائي" فوق `discountBasePct`، عادي تحته. السقف الصلب (25) باقٍ. مسار اليتيم `discount.ts` أُزيل سابقًا. مُثبت بالجلسات (10%/20%/0%). | مُغلق |
| **BL-66** | ✅ **مُغلق (الوسم كان قديمًا — verified 2026-07-14).** منع اعتماد الذات مُنفَّذ في `passDiscountAction` (`discount.ts:162-165` → `errors.cannotApproveSelf`، D-20). ~~قيد التنفيذ~~ | مُغلق |
| **BL-67** | ✅ **مُغلق (الوسم كان قديمًا — verified 2026-07-14).** إشعار SALES_REP بقرار الخصم مُنفَّذ في المسارين: الرفض (`discount.ts:270-277`) والقبول/التعديل (`:331-341`) — `DISCOUNT_DECIDED`. ~~صفر إشعار~~ | مُغلق |
| **BL-68** | ❓ **`managerApprovalCeilingPct` عمود ميت** (صفر منطق يقرأه). اسمه يطابق "سقف المدير" لكن الوظيفة يحملها `discountBasePct` بمعنى مقلوب. يُوحَّد أو يُحذف — SCR. | يوسف (SCR) |
| **BL-71** | ✅ **مُغلق (PHASE C، D-23).** زر "طلب خصم" في شاشة تفاصيل العرض (`quotation-detail.tsx` — SALES_REP/SALES_MANAGER/ADMIN، على DRAFT/SENT بلا طلب معلّق) يستدعي `requestDiscountAction` القائم بلا منطق جديد. مُثبت: 15% → طلب+SALES_MANAGER، السلسلة كاملة حتى إشعار SALES_REP، 30% مرفوض، طلب ثانٍ مرفوض. | مُغلق |
| **BL-73** | ✅ **مُغلق (PHASE D، D-24).** `cancelInvoiceAction` (ACCOUNTING/ADMIN): سبب إلزامي server-side (`cancelReasonRequired`) + إشعار فوري لـ ADMIN (رقم+مبلغ+سبب) + `INVOICE_CANCELLED` + CANCELLED لا محذوفة. مُثبت. **رصد (recon): لا FK بين `Invoice` و`Payment`** (`Payment.invoiceId` غير موجود — يرتبط بالعرض) → **إلغاء الفاتورة لا ييتّم أي دفعة**. الاسترجاع مسار منفصل (BL-75). | مُغلق |
| **BL-75** | 🟠 **لا مسار استرجاع مبالغ.** `Payment` يسجّل الوارد فقط. أول استرجاع فعلي سيُسجَّل خارج النظام أو برقم خاطئ. صاحب العمل: "طبيعي يحصل استرجاع". دفعة منفصلة. | يوسف |
| **BL-78** | ✅ **مُغلق (PHASE 4).** DrawingStatus.SUPERSEDED — عند اعتماد رسمة، كل رسمة TEC_APPROVED أخرى على نفس الطلب → SUPERSEDED (transaction + ActivityLog). الدليل: technical-office/actions.ts · commit 8e72113 | مُغلق |
| **BL-79** | 🟠 **دَين: `FaultType` و`InstallationItemType` enumان متوازيان** لنفس المفهوم (BREAKAGE↔BREAKAGE_REPLACEMENT · FACTORY_ERROR↔MFG_ERROR). التحقيق سيترجم بخريطة يدوية. التوحيد = SCR منفصل. ليس بلوكرًا. | يوسف (SCR) |
| **BL-80** | 🟠 **لا granularity على القطعة** — `InstallationItem` يصل لأمر التصنيع كاملًا (عبر `InstallationOrder.manufacturingOrderId` 1:1)، لا للقطعة المكسورة بعينها. مقبول حاليًا. | يوسف |
| **BL-81** | ✅ **مُغلق كاملًا — schema + app-layer (1ب، 2026-07-14).** schema: SCR-018/migration 29 (commit 4c055e1). **app-layer:** خدمة `src/lib/services/inspection-measurements.ts` (`addMeasurement`/`deleteMeasurement`/`listMeasurements`/`getMeasurementAssignee`) — الجدول **المصدر الوحيد**، وActivityLog أثر فقط (`MEASUREMENT_ADDED`/`MEASUREMENT_DELETED`). **المسار النصي حُذف بالكامل:** `addMeasurements` القديم أُزيل، والقرّاء الأربعة حُوّلوا (`status-derivation.ts:98` · `manufacturing/[id]/page.tsx:82` · `investigations/[id]/page.tsx:84` · `getInspectionDetail`) — **صفر قارئ متبقٍّ لـ`MEASUREMENTS_RECORDED` في الريبو كله** (ناقدان مستقلان، PHASE 2). الآثار الثلاثة منقولة، والاشتقاق صار **داخل الـ`$transaction`** والإشعارات **بعده ومحوَّطة** (فشل إشعار لا يُفسد حالة الطلب). ✅ **مُثبَت بجلسات حقيقية (STD-11/STD-12، طريقة BL-82):** REP أضاف صفّين بنقرات حقيقية → صفّان مهيكلان (`250.555`/SQM/كمية 2 · `90.25×220.5`/CBM) بفاعله، صفر سجل نصي · **الطلب TEC-PRJ-0007: ON_HOLD → IN_PROGRESS** (`TEC_STATUS_DERIVED`) · الإشعاران وصلا (TECHNICAL_OFFICE ×3 + **مالك العميل** dev-rep-e) · **`250.5555` (4 خانات) مرفوض** بـ`.multipleOf(0.001)` بصفر كتابة (لا تقريب صامت — BL-86) · الحذف بنقرتين → الصفّان اختفيا و**الحالة ارتدّت IN_PROGRESS → ON_HOLD** بصفر إشعار جديد (11→11، **D-38**) · **حارس BL-105 صمد على الجديد:** إضافة على معاينة غيره → `inspectionNotAssigned` · **حذف صفٍّ أنشأه INSPECTION_MANAGER على معاينة ليست للمندوب → `inspectionNotAssigned` والصف باقٍ** (استدعاء مباشر متخطٍّ للشاشة — STD-15) · **المدير بلا تضييق** (سجّل مقاسًا على معاينة غير مُسندة لأحد). | مُغلق |
| **BL-84** | ✅ **محسوم (قرار يوسف 2026-07-13): القاعدة سليمة UTF-8.** الدليل: "ادهم الشاذلي" (seed) يظهر سليمًا بجوار صفوف `????` في نفس العمود · والعربي المُدخل من الواجهة يُخزَّن سليمًا (بند الكسر الجديد + evidenceNotes). **العطب = ترميز ترمنال الوكيل.** → أنتج STD-13 وBL-85. | مُغلق |
| **BL-85** | 🔴 **صفوف بيانات اختبار بعربي مكسور `????` في القاعدة.** تُنظَّف مع بيانات الاختبار (SCR-017b). | يوسف (SCR-017b) |
| **BL-86** | 🔴 **مؤكَّد (2026-07-13): 4/4 عروض سوشيال بلا معاينة.** REVIEW تؤكد "مطابقة المقاسات" على ضلع غير موجود = **ختم مطاطي — البوابة الثلاثية تفقد ثلثها.** ❓ هل السوشيال بلا معاينة شرعًا، أم عيب بيانات اختبار؟ قرار يوسف. **في الحالتين: البوابة يجب أن تمنع تأكيد ضلع غير موجود.** **🔎 دليل الكود (تدقيق 2026-07-14، verified-in-code): الختم المطاطي بنيوي لا مجرد بيانات —** `confirmMatchItem` (`src/lib/services/mfg-review.ts:56-76`) يفحص فقط `status==="UNDER_REVIEW"` (سطر 63) ثم يكتب `MFG_MATCH_CONFIRMED` — **صفر فحص لمحتوى الضلع.** وزر التأكيد `disabled={busy \|\| done}` فقط (`manufacturing/[id]/review-panel.tsx:210`) — لا يُعطَّل حين `!hasInspection` (الذي يُعرَض كتحذير أصفر فقط، سطر 139-141). أي أن REVIEW تضغط "تأكيد مقاسات المعاينة" ويُسجَّل حتى بلا معاينة إطلاقًا. | يوسف ✅ **القرار حُسم (D-35/D-36، 2026-07-15):** حجب صلب server-side — لا تأكيد على ضلع غير موجود، بلا استثناء للسوشيال. ضلع المعاينة = المقاسات + صور الموقع + الكروكي (الثلاثة إلزامية، D-36). **البناء مؤجّل بعد بوابة اعتماد المعاينة (BL-109/المرحلة 2)** — لأن تعريف ضلع المعاينة يعتمد عليها. الـ4/4 بلا معاينة = بيانات اختبار (SCR-017b). | 
| **BL-86 تحديث** | بعد B1 (بوابة الاعتماد): ضلع المعاينة في المطابقة الثلاثية يجب أن يعني InspectionRequest.approvalStatus===APPROVED — لا مجرد وجود مقاسات. (كان: مقاسات موجودة. صار: معاينة معتمدة.) يُطبَّق عند بناء BL-86.|
| **BL-87** | 🟠 **Select الحُكم يعرض القيمة الخام بعد الاختيار بدل التسمية المترجمة.** تنظيف واجهة. | تنظيف |
| **BL-90** | 🟠 **`rejectQuotationAction` (`lib/review/actions.ts:180`) بلا حارس حالة** — يعيد عرضًا معتمدًا (وربما مدفوعًا/متعاقَدًا) إلى `RETURNED` بلا فحص. بعد حارس PHASE 3.5 يصير له أثر جانبي: أقساط تالية على عرض أُرجع تُرفض (`reviewNotApproved`) — وهو سلوك دفاعي مقبول غالبًا، لكن الانتقال نفسه (approved→returned بعد مال) غير مُصالَح (ACC-OBS-2). رُصد أثناء تدقيق PHASE 3.5. **القرار (منع الإرجاع بعد دفعة/عقد؟) عند يوسف.** | يوسف |
| **BL-89** | 🔴 **صفّا اختبار بالتناقض (Q-2026-00007 · Q-2026-00011): عرض غير معتمد وعليه عقد (والثاني + أمر تصنيع).** رصد فقط — يُنظَّفان مع بيانات الاختبار (SCR-017b/تنظيف UAT). **ممنوع ترميمهما** (STD-14). | يوسف (تنظيف) |
| **BL-91** | ✅ **محسوم بـ D-31 (PHASE 2، 2026-07-14): الربط باختيار صريح.** `createInspection` صار يتطلب `quotationRequestId` إلزاميًا مع حارس server-side (نفس العميل · غير مربوط · غير DONE، وإلا `errors.requestNotSelectable`) داخل transaction (ربط شرطي race-safe)؛ حُذف `findFirst` التخميني وحُذف التوليد التلقائي في `changeCustomerStage`. مُثبَت بجلسة واجهة (3a: SALES_REP اختار TEC-PRJ-0002 → ربط دقيق · 3b: عميل آخر → مرفوض · 3c: جدولة+تعيين · 3d: نقل لمرحلة INSPECTION → صفر معاينة). الـ13 اليتيمة: رصد فقط (STD-14). ~~انتهازي لا مضمون~~ | مُغلق (D-31) |
| **BL-92** | ✅ **مُغلق (D-33، 2026-07-14).** الانفصال مقصود لا عطل: المعاينة مجانية، وبند النقل اختياري بيد TECHNICAL_OFFICE حسب الحالة. صفر كود مطلوب. | مُغلق |
| **BL-95** | 🟡 **مفتوح، غير حاجب: لا مسار لنقل ملكية العميل بين المندوبين.** صاحب العمل: "المدير يغيّر المندوب عشان البيعة تتم". لا يُبنى ولا يُقترَح الآن (STD-05) — مُسجَّل فقط. | يوسف |
| **BL-93** | ✅ **محسوم بـ D-32 (2026-07-14): تغطية مسموحة بحوكمة — لا حارس ملكية.** الأصل (تدقيق RBAC عدائي لـPHASE 2): `createInspectionAction` و`getSelectableRequests` يقبلان `customerId` بحارس دور فقط بلا فحص ملكية، فيستطيع `SALES_REP` طلب/تعداد معاينة على عميل زميله (والطلب **يقدّم المرحلة المشتقّة**). التناقض: `changeCustomerStage:46` hard-scope مقابل R-02 soft-control. **القرار (يوسف): نموذج R-02** — التغطية واقع تشغيلي؛ الضابط = ActivityLog (الفاعل الحقيقي، INSPECTION_CREATED) + إشعار المالك (`notifications.inspectionByColleagueTitle`، `createInspection`). | مُغلق (D-32) |
| **BL-94** | 🟡 **مُضيَّق جزئيًا (1أ، 2026-07-14): `INSPECTION_REP` فقط.** `getInspections` (`src/lib/services/inspections.ts:74-80`) صار: `where.assigneeId = userId` حين `role === "INSPECTION_REP"` — لا غير. `ADMIN`/`INSPECTION_MANAGER` يريان الكل كما كانا (المدير يوزّع فيلزمه الكل). **يبقى مفتوحًا كمبدأ:** الدالة ما زالت **قائمة بيضاء بالاستثناء لا بالنطاق** — أي دور جديد يُضاف لحارس الصفحة سيرى كل المعاينات افتراضيًا. النموذج المطلوب مستقبلًا: نطاق صريح لكل دور (deny-by-default). | يوسف |
| **BL-104** | ✅ **مُغلق (نُفِّذ 2026-07-14).** قرار يوسف: يُحذف الزر (النموذج كان مكسورًا بنيويًا منذ D-31: لا يرسل `quotationRequestId` وzod يفرضه ⇒ كل حفظ يفشل). **المدخل الوحيد = شاشة العميل.** **التنفيذ:** حُذف من `inspections-client.tsx`: dialog الإنشاء · `formSchema` · `useForm` الخاص به · `open`/`submitting` · `openCreate`/`closeDialog`/`onSubmit` · الزر · استيرادات `Textarea`/`createInspectionAction`/`CustomerOption` · والمتغير الميت `isViewer`؛ `canCreate`→`canSchedule`. **التتالي:** `getCustomers()`+prop `customers` أُزيلا من `inspections/page.tsx`، و`getCustomers`/`CustomerOption` في `services/inspections.ts` صارا ميتين فحُذفا. `createInspectionAction` لم يُمَس. **اكتشاف أثناء الإثبات (الدرس 2):** الحذف كشف أن `INSPECTION_MANAGER` في `CREATE_ROLES` بلا شاشة تصله (`customers/[id]:15` يرفضه) → **حُسم بـD-37** فحُذف من `CREATE_ROLES`. ✅ **مُثبَت بجلستين (STD-11):** المدير على `/inspections` → `createButtonPresent:false` · 10 أزرار جدولة · 18 صفًا · الجدولة تعمل (`cmrkgcr9g…`→SCHEDULED/2026-07-22 + `INSPECTION_SCHEDULED` بفاعله). `SALES_REP` (dev-rep-e) على شاشة العميل → «لا طلبات مؤهَّلة — أنشئ طلب تسعير أولًا» (حارس D-31 حيّ) → أنشأ `TEC-PRJ-0008` ثم معاينة عليه → صف `cmrl31cyw…` REQUESTED + الطلب مربوط (`inspectionRequestId`) + الحالة اشتُقّت `ON_HOLD` + `INSPECTION_CREATED` بفاعل `dev-rep-e (SALES_REP)`. | مُغلق |
| **BL-106** | 🟠 **مفتوح — بقية سلسلة D-37 غير مبنية:** «المعاينة تنتقل تلقائيًا للمدير → يعتمد → المكتب الهندسي». الموجود اليوم: `INSPECTION_REP` يحفظ مقاسات → `addMeasurements` يُخطر `TECHNICAL_OFFICE` + مالك العميل **مباشرةً** — صفر انتقال للمدير وصفر بوابة اعتماد منه. ⚠️ **تعارض ظاهري مع D-05** («INSPECTION_MANAGER بلا بوابة اعتماد») — الحسم عند يوسف: بوابة جديدة أم إشعار فقط؟ لم يُخترع شيء (STD-05). | يوسف |
| **BL-107** | 🟡 **مفتاحان يتيمان بعد BL-104:** `inspections.created` · `inspections.selectCustomer` (صفر مرجع في `src/` — grep). لم يُحذفا (خارج نطاق الدفعة، L-14). | تنظيف |
| **BL-108** | 🟠 **تسريب قراءة: `INSPECTION_REP` يقرأ مقاسات أي معاينة عبر `/manufacturing/[id]`.** `ORDER_VIEW_ROLES` يضم `INSPECTION_REP` (`manufacturing/[id]/page.tsx:17`) والصفحة تحرس بالدور فقط بلا فحص ملكية على الأمر (`:26-27`)، ثم تقرأ `listMeasurements` (`:82`) — والدالة بلا نطاق (`inspection-measurements.ts:60-66`). **يلتف على تضييق `getInspectionDetail`** (REP يُمنع من معاينة ليست له، لكنه يرى مقاساتها من شاشة الأمر). 🔴 **سابق للدفعة 1ب — الدفعة غيّرت المصدر (ActivityLog→الجدول) لا الصلاحية؛** الشاشة كانت تقرأ نفس المقاسات بنفس الحارس. **الإصلاح الأدنى:** حذف `INSPECTION_REP` من `ORDER_VIEW_ROLES`، أو إخفاء ضلع المقاسات عنه. رصده RBAC-CRITIC (PHASE 2). | يوسف |
| **BL-109** | 🔴 **[BLOCKER] بوابة اعتماد المعاينة (D-37) معطّلة بنيويًا.** `approvalStatus`/`approvedById`/`approvedAt`/`returnReason` (SCR-018) بلا كاتب في أي مكان ⇒ المدير لا يعتمد، ولا انتقال تلقائي لـPENDING_APPROVAL بعد حفظ المقاسات؛ المقاسات تتخطّى الاعتماد للمكتب الفني مباشرة. **ليس "حقل بلا كاتب" بل بوابة عمل مكسورة.** لا schema — الحقول موجودة، الناقص writer + UI. | يوسف |
| **BL-110** | 🟡 **D-38: قفل المقاسات بعد الاعتماد — يُبنى مع البوابة (المرحلة 2).** اليوم `addMeasurementAction`/`deleteMeasurementAction` لا يفحصان `approvalStatus` (لا وجود لبوابة تفحصها — BL-109). بعد بناء البوابة: تُمنع الإضافة/الحذف على معاينة `APPROVED` (أو تُبطِل الاعتماد صراحةً). **لا يُبنى الآن بقرار يوسف.** | يوسف (مرحلة 2) |
| **BL-111** | 🟡 **الوثائق المرجعية صارت كاذبة بعد 1ب:** `CLAUDE.md` (بند «دين تقني مؤثّر») ما زال ينصّ «نموذج `InspectionMeasurement` غير مستخدم — المقاسات تُخزَّن نصًا في ActivityLog»، وكذلك `knowledge/uat/inspections.md:39` و`docs/UX-JOURNEY-AUDIT.md:60`. **الحقيقة الآن: الجدول هو المصدر الوحيد.** التحديث بيد يوسف وحده (STD-01). | يوسف |
| **BL-105** | ✅ **مُغلق (1أ، 2026-07-14 — قرار يوسف: يُقفل في نفس الدفعة، لا commit على تغيير يفتح ثغرة).** الأصل: `addMeasurements`/`addInspectionAttachment` حارسهما دور فقط (`ALLOWED_ROLES` يشمل `INSPECTION_REP`) بلا `assigneeId === userId` → REP يكتب على معاينة ليست له بتخطّي الشاشة (**جوهر STD-15: الترشيح ليس حارسًا**). **التنفيذ:** `canWriteOnInspection(role, userId, assigneeId)` (`inspections/actions.ts:36`) — `role !== "INSPECTION_REP" \|\| assigneeId === userId` — قبل أي كتابة في `addMeasurements` (`:207`) و`addInspectionAttachment` (`:289`) → `errors.inspectionNotAssigned` (ar+en). ADMIN/INSPECTION_MANAGER بلا تضييق. **منطق المقاسات نفسه لم يُمَس** (البنية النصية — بند 1ب). ✅ **مُثبَت بجلسة حقيقية (طريقة BL-82، الهوية من الترويسة أولًا — STD-11):** المندوب استدعى الأكشنين **من جلسته متخطّيًا الشاشة** (Next-Action مستخرج من الـbuild) على معاينة مُسندة لغيره وأخرى بلا مُسنَد → **`{"error":"errors.inspectionNotAssigned"}` × 3** · **صفر كتابة** (baseline 0/0 → بعد المحاولات 0/0، وصفر ActivityLog في آخر 10 دقائق على الضحيتين). **الفرع الشغّال حيّ:** نقرة حقيقية على معاينته هو → `MEASUREMENTS_RECORDED · INSPECTION_REP@egyglass.net · {"width":90,"height":120}`. | مُغلق |
| **BL-88** | 🔴 **عرض P5 (Q-2026-00011): `reviewStatus=PENDING_REVIEW` وعليه عقد + أمر تصنيع.** ❓ بيانات ما قبل الحارس، أم ثغرة في الحارس؟ **التشخيص (2026-07-13، قراءة فقط — STD-14):** **كلاهما.** (أ) **الصفوف نفسها أقدم من الحارسين مُثبتًا بالتوقيت:** عقد P5 أُنشئ 07-12 **02:56:51** والأمر **03:04:52** — حارس العقد على التصنيع دخل بـ`e13f8b4` الساعة **15:44** وبوابة الدفع بـ`cc98ce0` الساعة **20:13** من نفس اليوم (فرق ~12س). (ب) 🔴 **الثغرة قائمة اليوم:** لا `createContractCore`/`createContract` ولا `addPayment` (مسار المشروعات) ولا `createManufacturingOrder` يفحص `reviewStatus`/`status` للعرض إطلاقًا — سلسلة "عقد ثم دفعة ثم أمر تصنيع" قابلة لإعادة الإنتاج الآن على عرض غير معتمد (شرط APPROVED موجود فقط في العقد الآلي للسوشيال، وواجهة الحسابات تُخفي غير المعتمد لكن الـaction نفسه غير محروس). (ج) **الحصر:** عرضان فقط بالتناقض — Q-2026-00007 (عقد 07-11، بلا أمر) وQ-2026-00011. **القرار (فرض اعتماد العرض قبل العقد؟ أين بالضبط؟) عند يوسف — لا اجتهاد، وD-22 يُعرّف الاعتماد النهائي = اعتماد الخصم.** بيانات P5 لا تُمَس حتى الحسم. | يوسف |
| **BL-82** | ✅ **مُعاد توصيفه (قرار يوسف 2026-07-13): ترقية لطريقة الإثبات، لا عطل.** الوكيل لا يُدخل كلمات مرور (قاعدة أمان بيئته + STD-08). **الطريقة المعتمدة:** يوسف يسجّل الدخول في الـBrowser pane، الوكيل يقود الجلسة ويوثّق (لقطات + DOM + استعلامات قاعدة). دليل الواجهة أقوى من curl — يوسف في الحلقة عند كل جلسة إثبات. **أول تطبيق ناجح: دليل PHASE 1 للتحقيقات.** | مُعتمد كطريقة |
| **BL-83** | ✅ **مُغلق (قرار يوسف 2026-07-13): البيئة سليمة.** التحقق أثبت: ما يخدم 3100 = حاوية `app` نفسها (أمر preview = `docker compose up` — مسار الإقلاع القياسي كاملًا: install → generate → migrate deploy 28 → `next start` production)، لا node خارج Docker على المنفذ، `docker compose exec` يعمل. الفرق الوحيد: عملية compose مملوكة لأداة preview بدل terminal. | مُغلق |
| **BL-77** | ❓ **لا رابط `Invoice` ↔ `Payment`.** الدفعة على العرض لا على الفاتورة. النظام لا يعرف أي فاتورة سُدِّدت. مقصود أم فجوة؟ | يوسف |
| **BL-76** | ❓ **حارس "طلب معلّق يمنع الثاني" لم يُثبَت مستقلًا** — حارس الحالة (`!DRAFT/SENT`) سبقه. النتيجة صحيحة، الآلية غير مؤكدة. يُتحقَّق لاحقًا. | يوسف |
| **BL-74** | ✅ **مُغلق (PHASE E).** شاشة `/approvals` لـ ADMIN فقط تجمع: طلبات الخصم PENDING (المُمرَّرة من SALES_MANAGER) + الفواتير DRAFT، بالأقدم أولًا + عمر الانتظار، صفر → "لا شيء بانتظارك". **تستدعي `decideDiscountAction`/`issueInvoiceAction` القائمين بنفس الحرّاس — مدخل لا مصدر حقيقة ثانٍ.** مُثبت: نفس أثر الشاشة الأصلية · غير ADMIN مرفوض (307) · العنصر يختفي بعد الاعتماد. | مُغلق |
| **BL-70** | ✅ **مُغلق (PHASE C).** لم يعد يتيمًا — `requestDiscountAction` موصول بزر "طلب خصم" (مدخل العرض القائم، D-23). المساران مشروعان الآن (D-23): createQuotation عند الإنشاء · requestDiscountAction على عرض قائم. كلاهما نفس السلسلة. | مُغلق |
| **BL-69** | 🔴 **`QuotationStatus` قائمة حرة بلا حارس تسلسل.** EXPIRED يمكن الرجوع منها. SALES_REP يرى القائمة والأكشن يرفضه (عدم تطابق UI/server). | يوسف |
| **BL-60** | 🔴 **[verified-in-code] مسار البديل (W-06) ينشئ أمر تصنيع مباشرة `IN_PRODUCTION` — يتخطّى بوابة REVIEW والحارس كله.** `installation-extras.ts:121` **كتابة مباشرة** `prisma.manufacturingOrder.create` (لا `createManufacturingOrder`) → يتخطّى: `requireRole(TEC_APPROVER)` · العقد · الدفعة · فحص الرسمة المعتمدة · UNDER_REVIEW/مطابقة REVIEW. يُطلقه **INSTALLATIONS** (قائد الفريق) بإضافة بند تركيب من نوع خطأ/كسر (`addInstallationItemAction`). يرث `factoryId` من الأصلي (قد يكون null). القاعدة: **3 أوامر بديلة، كلها IN_PRODUCTION**. البديل **أخطر حالة** (لازم يطابق الأصل وإلا خسارة خامات) ويتخطّى المطابقة. **✅ الباب مُقفَل (2026-07-12، D-18):** حُذف الإنشاء التلقائي؛ البند يُسجَّل + إشعار INSTALLATIONS/REVIEW + `REPLACEMENT_REQUESTED`. مسار البديل معطّل حتى بناء موديول التحقيق (BL-63). الأوامر الثلاثة القائمة: رُصدت، لم تُهاجَر. | مُغلق (الباب) |
| **BL-63** | ✅ **مُغلق (SCR-017a PHASE 4، 2026-07-13).** موديول التحقيق كامل: PHASE 1 فتح · 2 الأثر · 3 الحكم · 3.5 حارس الاعتماد · 4 البديل + حارس W-06. الدليل: commit 8e72113 · tag scr017a-phase4-done | مُغلق |
| **BL-61** | ❓ **الفاتورة تصدر من ACCOUNTING بلا بوابة ADMIN** (`issueInvoice` — ACCOUNTING/ADMIN، لا اعتماد ADMIN منفصل) — وصاحب العمل يقول "ADMIN يعتمد الفواتير والخصم". الخصم له بوابة (`decideDiscount`)؛ الفاتورة لا. فجوة؟ | يوسف |
| **BL-62** | ❓ **`updateQuotationStatus` قائمة حالة حرة** (`pricing/actions.ts`) — `PRICING_ROLES` يضبط أي `QuotationStatus` بما فيها `APPROVED` بلا بوابة. هل `QuotationStatus` حاملة لأي قرار، أم عرض فقط؟ (منفصل عن `reviewStatus` الذي يحمل اعتماد التسعير.) | يوسف |
| **BL-59** | ✅ **مُغلق (PHASE B، D-16).** `createManufacturingOrder` يُنشئ `UNDER_REVIEW` مباشرة + إشعار REVIEW (`lib/manufacturing/actions.ts:94`). لا خطوة "أرسل للمراجعة" يدوية. `submitForReview` بقي لمسار REJECTED→إعادة الرفع. مُثبت بالجلسات. | مُغلق |
| **BL-64** | **[verified-in-code] `MfgStatus.PENDING` صارت حالة ميتة لأمر التصنيع** — بعد D-16 لا كاتب لها (كان `@default(PENDING)` عبر createManufacturingOrder فقط، والآن يحدد UNDER_REVIEW صراحةً). القرّاء (`submitForReview`, `executive`) يبقون. **حذف قيمة enum = SCR** (ممنوع schema هنا). لا تُحذف. | يوسف (SCR) |
| **BL-56** | 🔴 **لا نسخ احتياطي — لا للـ DB volume ولا لـ `egyglass_uploads`.** named volume ينجو من rebuild، **لا** من `docker volume rm` ولا فقد الـ host. **بلوكر UAT.** (يوسّع BL-40.) | يوسف (قبل UAT) |
| **BL-54** | 🟠 **`dev-scr016` = ADMIN ثانٍ نشط** — يُعطَّل مع كل حسابات dev قبل UAT (BL-38). | يوسف (قبل UAT) |
| **BL-49** | 🟠 **زر "حذف" في `/users` يُنفّذ تعطيلًا ناعمًا** (`deleteUser`: `deletedAt`+`isActive=false`). التسمية مضلِّلة — ADMIN لن يضغط "حذف" على موظف. **تُعاد التسمية إلى "تعطيل".** النموذج سليم. (دفعة تنظيف واجهة منفصلة.) | تنظيف UI |
| **BL-50** | 🟠 **حالة `isActive=false` + `deletedAt=null` بلا مسار واجهة.** إما تُمنع الحالة (توحيد التعطيل=حذف ناعم) أو يُضاف مسار استرداد لها. | تنظيف UI |
| **BL-51** | 🟠 **الشعار لا يظهر في كل الشاشات.** | تنظيف UI |
| **BL-52** | 🟠 **شاشة تفاصيل عرض السعر: قالب طباعة السوشيال** (بمساحة الرسم الهندسي) لا يظهر عند فتح عرض أفراد/سوشيال. يُتحقَّق: هل القالب مربوط بالمسار؟ | تنظيف UI |
| **BL-53** | 🟠 **لا زر رجوع في شاشة تفاصيل عرض السعر.** | تنظيف UI |
| **BL-10** | **تسعير خارج القائمة** — طلب تسعير من المصنع → التسعير على أساسه (+ الإكسسوارات) | D-06 |
| **BL-11** | **فحص توفر المخزون** قبل طلب التصنيع من المصنع | D-07 · الفلوشارت م2 |
| **BL-12** | **شاشة العينات** — طلب → المخازن تجهز وترسل → فيدباك للمبيعات | الفلوشارت م4 · SAL BRD-6 |
| **BL-13** | **طلب فحص (IR)** قبل التسليم النهائي — يبدأه المشروعات وبعده ينتهي دوره | الفلوشارت 4.5 · PRJ BRD-8 |
| **BL-14** | **تنبيهات التأخير** (قرب المدة / تجاوزها) لقسم المخازن | الفلوشارت م3 · PRC-R06 |
| **BL-15** | **`Drawing.REJECTED` قيمة enum ميتة** — لا يمكن رفض رسمة | تدقيق الكود |
| **BL-16** | ✅ **مُغلق (PHASE 2د).** `approvedById` يُكتب عند اعتماد العرض بيد `TEC_APPROVER` (`lib/review/actions.ts:105`) + المساران القديمان (`pricing/actions.ts:521`، `discount.ts:248`). مُثبت: الطباعة تُظهر "المدير التنفيذي — TEC Approver Test (DEV)" لا "—". | مُغلق |
| **BL-17** | **اعتماد تسعير المهندس من مديره** — "لو التسعير من مهندس، مدير المهندس يراجع ويعتمد" | الفلوشارت م3 |
| **BL-96** | ✅ **مُغلق (D-34، 2026-07-14).** الوسم مُطبَّق في مساري القرار (`discount.ts:244-246, 272, 332-333`). **مُثبَت بجلستين حقيقيتين:** ADMIN طلب 5% ورفضه بنفسه → السجل يحمل "⚠️ قرار ذاتي (D-34)" · ADMIN رفض طلب SALES_MANAGER (7%) → **بلا وسم** (الوسم يتتبّع `requestedById === decidedById` لا مجرد "ADMIN قرّر"). D-20 سليم: SALES_MANAGER حاول تمرير طلبه هو → `cannotApproveSelf`، صفر سجل، صفر إشعار. الأثر المالي صفر (Q-2026-00026: DRAFT · 0.00 · 1140.00). | مُغلق |
| **BL-97** | ✅ **الدَّين مُنظَّف (2026-07-14):** الحقل الميت `replacementOrderId` أُزيل من السلسلة (grep = صفر) + التعليق صُحِّح لـTEC_APPROVER (D-29). 🟠 **متبقٍّ:** `errors.cannotApproveSelf` نصه **"لا يمكن اعتماد رسم رفعته بنفسك"** (رسالة رسمة) ويُعرض لمدير المبيعات على **خصم**. الحارس صحيح — النص مضلِّل. (ليس مفتاحًا مكررًا — يوجد في namespace `errors` و`tec` منفصلين.) | تنظيف نص |
| **BL-116** | 🔴 **[الأخطر — أساس معماري] شجرتان حيّتان متشابكتان باتجاه واحد.** lib/ الجذر (19 ملف) → src/lib/ (35 ملف). src مستقل؛ الجذر يعتمد عليه (relative/dynamic imports، خارج @/lib alias). **مطمئن:** requireRole + getFinanceScope مصدر واحد (src) — تدقيقات RBAC/المال فحصت النسخة الصحيحة. **التكرار الحقيقي 3 نقاط فقط:** (1) ~~🔴 updateMfgStatus باب خلفي~~ ✅ مدحوض تشغيليًا (BL-117) — LEGAL_TRANSITIONS تحجب العبور. ليس بابًا خلفيًا. يبقى فقط: توحيد آليتَي الفرض (خريطة جذر · حُرّاس src) + STATUS_OPTIONS القديمة. (2) `sendNotification` نسختان (جذر يبلع الأخطاء، src يرمي) · (3) `createReplacementOrder` نسخة يدوية. **خطر مالي منفصل:** قرّاء الفواتير/المستخلصات في src لا يستوردون getFinanceScope (نصف القرّاء خارج R-03). **الاتجاه المقرّر مبدئيًا: C تدريجي، الأخطر أولًا (updateMfgStatus → send.ts → finance scope → replacement → نقل تدريجي).** ~54 ملف إجمالًا. **قرار يوسف — دماغ صافية.** | يوسف **خطر مالي منفصل:** ~~قرّاء الفواتير/المستخلصات لا يستوردون getFinanceScope~~ ✅ فُحص (U-3، BL-118): ليست ثغرة — الصفحات محروسة ACCOUNTING/ADMIN (RSC لا actions)، الـactions المالية تطبّق النطاق+فحص مورد. عدم اتساق معماري (قائمة أدوار مقابل scope) = فخ صيانة مسجّل، لا تسريب حالي.|
| **BL-117** | 🟠 **دَين U-1 (لا ثغرة):** `updateMfgStatus` ليس بابًا خلفيًا — `LEGAL_TRANSITIONS` (lib/manufacturing/actions.ts:153-163) تحجب UNDER_REVIEW→[] وتمنع كتابة IN_PRODUCTION (approveOrder وحده مصدرها). فرضية BL-116 عن الباب الخلفي دحضها الحارس الزمني. **يُصحّح BL-116 النقطة (1).** المتبقّي: STATUS_OPTIONS في manufacturing-client.tsx:24-31 قديمة (4/6 قيم) · آليتا فرض مختلفتان (تُوحَّد ضمن BL-116). | يوسف | ✅ مُثبَت تشغيليًا (جلسة ADMIN حقيقية، 4.txt): UNDER_REVIEW→IN_PRODUCTION مرفوض (illegalStatusTransition) · القفزة الأمامية مرفوضة · IN_PRODUCTION→READY يعبر الجدول ويتوقف عند D-12 (factoryNotAssigned) · صفر كتابة (14→14).
| **BL-113** | 🔴 **[BLOCKER] REVIEW بلا شاشة للوصول لمهمته الأساسية.** يعتمد أوامر التصنيع فقط عبر رابط /manufacturing/[id] مكتوب يدويًا — قائمة /manufacturing محروسة ["ADMIN","PROCUREMENT"] (page.tsx:8) وnav يُغفل REVIEW (nav.ts:31). لا قائمة = لا اكتشاف للأوامر المعلّقة. نمط "صلاحية بلا شاشة" (كـINSPECTION_REP سابقًا). | يوسف |
| **BL-114** | 🔴 **[BLOCKER] دورة الكاش باك مسدودة.** Referral.status يُضبط PENDING فقط (cashback.ts:100)؛ ELIGIBLE/PAID/CANCELLED بلا كاتب. أهلية الكاش باك (وعد مالي حقيقي) لا تتقدّم أبدًا. لا schema — الحقول موجودة، الناقص writer. | يوسف |
| **BL-115** | 🟠 ReviewStatus.RETURNED حالة-فخ (review/actions.ts:183 يضبطها، لا شيء يعيدها) · InstStatus بلا تسلسل مفروض (installations/actions.ts:99-143 — أي مستخدم يقفز لـCOMPLETED) · PROCUREMENT يعيد تقديم أمر مرفوض بلا فصل مهام (manufacturing/[id]/actions.ts:96). | يوسف |
| **BL-118** | 🟡 **فخّ صيانة مالي (لا ثغرة حالية — U-3، 2026-07-16):** الفواتير/المستخلصات تُقيَّد بـقائمة أدوار (`["ACCOUNTING","ADMIN"]`) — تُقصي PROJECTS/TECHNICAL_OFFICE كليًا. الأرصدة/الدفعات تُقيَّد بـ`getFinanceScope` (رؤية مفلترة). **آمن الآن** (الفواتير مقفولة على دورَي "all"، صفر تسريب — 7 فواتير/5 مستخلصات، قارئ بلا نطاق يُرجع صفر لمن لا يحقّه). 🔴 **الفخ:** أي توسعة لاحقة لحارس صفحات الفواتير لتشمل دورًا مُفلترًا **بلا إضافة getFinanceScope** → تسريب كامل فوري (PROJECTS يرى الـ7 كلها بينها 3 سوشيال). النمط القياسي للتعميم وقتها: getContractBalances/getPayments (requireRole → scope → قوائم تحقن quotationWhere · مورد مفرد يفحص findFirst). **مصدر واحد getFinanceScope (lib/finance/scope.ts) — سليم، تدقيقات RBAC/المال فحصت النسخة الصحيحة (يؤكد BL-116).** | يوسف — تحسين وقائي |
| **BL-125** | 🔴 **[D-03 — تسريب مالي خارج B2] `/quotations/new` مفتوح لـREVIEW ويمرّر معاملات التسعير الحقيقية + عتبة الخصم لمتصفحه** (quotations/new/page.tsx:13 الحارس · :44 value.toNumber() · :48 العتبة → QuotationBuilder "use client"). نفس فئة BL-123، ملف آخر. كشفه ناقد B2. **يُصلَح خادميًا (إسقاط الأرقام قبل التمرير لـREVIEW) أو إزالة REVIEW من حارس الصفحة — قرار يوسف.** | يوسف |
| **BL-126** | 🟡 خياران ميتان في STATUS_OPTIONS (`PENDING`/`IN_PRODUCTION`) — ليسا هدفًا لأي انتقال في LEGAL_TRANSITIONS (actions.ts:158-165) ⇒ مرفوضان دائمًا. عيب واجهة قديم (عائلة BL-117). تنظيف. | يوسف |
| **BL-130** | 🟠 محرك المستخلصات كان بلا حد أقصى — حاجز احترازي أُضيف، البند لم يُغلق كليًا: أُضيف تحقق في createStatement (src/lib/services/statements.ts) يرفض أي مستخلص جديد لو مجموع progressPct لمستخلصات العقد ISSUED+PAID + الجديد > 100%. دليل حي مزدوج: عقد dev_contract_s15 وُجد فعليًا بـ4 مستخلصات صادرة مجموعها 185% (استعلام قراءة فقط، مستقل عن Q-2026-00019) · الحاجز اختُبر ضد نفس العقد ورفض إضافة 5% (count before/after = 4/4، صفر كتابة) · اختبار وحدة يغطي >100% مرفوض، =100% مقبول، مجموعة فارغة مقبولة (tests/statement-progress-cap.test.ts). لا يُغلق البند (قاعدة الإغلاق): الحاجز على createStatement فقط — issueStatement غير محمي (مسودة سابقة قد تخترق السقف) · الـ185% القائمة على dev_contract_s15 لم تُرمَّم (رُصدت فقط، STD-14) · قرار PAID ضمن المجموع نُفِّذ باجتهاد الوكيل، غير معتمد منك. أسئلة مفتوحة (STD-05): 1. PAID يُحسب ضمن المجموع كما نُفِّذ، أم يُستبعد؟ 2. يُوسَّع الحاجز لـissueStatement؟ 3. الأصل: تراكمي يعوّض السابق، أم إضافي فوق الأخير؟ 🔹 **تحديث Q3 (2026-07-23، صاحب العمل):** القرار النهائي على «تراكمي مقابل إضافي» **غير معروف حاليًا** — صاحب العمل لا يتذكر المنطق الأصلي. النظام الحالي والبيانات التاريخية الحقيقية **كلاهما يتصرف كـ«إضافي»** (كل مستخلص قيمته المستقلة، بلا خصم من الذي قبله)، والحاجز الحالي مبني على هذا الافتراض ومتّسق معه. ⚠️ **يُحسم نهائيًا** عند مراجعة عقد ورقي قديم فيه مستخلصات متتالية حقيقية (مع العميل أو المحاسب). حتى ذلك الحين: يبقى السلوك الإضافي (الأكثر أمانًا الآن لأنه المطابق للواقع المُشغَّل فعليًا) — **لا تغيير في المعادلة الأساسية.** Q1 (PAID) وQ2 (issueStatement) يبقيان مفتوحين. ✅ **مُغلق بالكامل** (قاعدة الإغلاق): الأسئلة الثلاثة محسومة — (1) PAID
يُحسب ضمن المجموع (كان منفَّذًا بالفعل في createStatement) · (2) الحاجز
امتد لـissueStatement (مسودة قديمة سابقة للإصلاح تُرفض الآن عند التصدير،
مُختبَر بحقن transaction حقيقي بلا قاعدة بيانات: تجاوز 100% مرفوض بصفر
كتابة، الحد الفاصل 100% مقبول، الحالة العادية تُصدر بلا كسر) · (3) قرار
تراكمي/إضافي موثَّق كمعرفة ناقصة (تعليق كود يشير BL-130 Q3)، لا معادلة
تغيّرت. الحاجز الأصلي ثابت في مكانه.| يوسف |
| **BL-131** | 🔴 npm run build لا يفحص الأنواع — يتخطى type validation صراحةً ("Skipping validation of types"). "Build GREEN" لا يثبت سلامة الأنواع في هذا المشروع. 10-definition-of-done.md يعتمد على build وحده — فجوة في تعريف "تم"، تحتاج tsc --noEmit كخطوة منفصلة إلزامية. | يوسف |
| **BL-132** | 🟠 Linting معطوب أصلاً (ESLint: circular JSON، تعارض plugin react) — قائم من قبل هذا الإصلاح، غير مرتبط به. | يوسف |
| **BL-133** | 🟡 لا مُشغِّل اختبارات في المشروع (لا vitest/jest، لا مجلد tests/) — قرار مطلوب: إطار رسمي أم الاستمرار بسكربتات tsx مستقلة؟ | يوسف |
| **BL-134** | 🔴 المكتب الفني (TECHNICAL_OFFICE) يشوف شاشة الحسابات كاملة بكل تفاصيلها المالية — خطأ صلاحيات كامل، مؤكَّد من صاحب العمل (لا استثناء مقصود). الدليل: مطابقة كاملة في 3 مواضع (قرار متعمَّد وقت الكتابة، مش نسخ-لصق عرضي):lib/accounting/actions.ts:12 (ACCOUNTING_READ_ROLES) ·src/app/(dashboard)/accounting/page.tsx:8 (requireRole) ·src/lib/nav.ts:66 (روابط القائمة الجانبية) — الثلاثة متطابقون حرفيًا. تحقّق حي: تسجيل دخول tech-office@egyglass.net يعرض شاشة الحسابات كاملة. فحصت getFinanceScope (نفس الملف) — بيحدد الصفوف المرئية فقط، **صفر تصفية على  مستوى الحقول** (لا هامش ربح، لا تفاصيل دفع مخفية) لأي دور غير ACCOUNTING/ADMIN. يعني التسريب كامل، مش جزئي. الإصلاح المطلوب: حذف "TECHNICAL_OFFICE" من الثلاث مواضع. PROJECTS لم يُراجَع بعد — سؤال مفتوح منفصل، لا يُفترض صحيحًا ولا خاطئًا حتى تأكيد. | يوسف |
| **BL-135** | 🔗 مدموج في BL-144 — كان الوصف الأولي لعرَض bug التنقّل قبل
تحديد السبب الجذري. انظر BL-144 للتفصيل والإصلاح الفعلي. | يوسف |
| **BL-136** | 🟠 مفيش صفحة بروفايل أو تغيير باسورد للمستخدم الحالي في كل
النظام. | يوسف |
| **BL-137** | 🟠 أسماء عملاء بالعربي بتظهر "؟؟؟" في شاشات الحسابات/
المستخلصات/الفواتير — سبب مؤكَّد: بيانات اختبار مُدخَلة عبر ترمنال، مطابق
STD-13/BL-84. ليس عطلًا بالتطبيق — يحتاج إعادة إدخال بيانات من الواجهة لا
تعديل كود. | يوسف |
| **BL-138** | 🟠 رقم عرض السعر في شاشة الحسابات نص عادي، مش قابل للضغط
للرجوع للعرض. | يوسف |
| **BL-139** | 🟠 مفيش إشارة مرئية للمحاسب إن الفاتورة "بانتظار اعتماد
الأدمن" — مربوطة بعطل جرس الإشعارات المعروف (يعرض مفتاح خام بدل الترجمة).
| يوسف |
| **BL-140** | 🟠 نسبة الضريبة 14% مش ظاهرة على الفاتورة المطبوعة — مؤكَّدة
مرتين (فحص كود مستقل + استخدام حي مستقل، نفس النتيجة). | يوسف |
| **BL-141** | 🟡 لوجو الشركة في الـsidebar بس مش أعلى الشاشات، ونسبة
السداد (%) مش ظاهرة على الفاتورة (المبلغ فقط). تحسين واجهة. | يوسف |
| **BL-142** | 🟠 drawing-approval.ts كود ميت بالكامل (صفر استدعاء)،
وCLAUDE.md لسه بيشاور عليه كأنه الضابط الحي — مرجع مضلِّل في وثيقة نشطة.
| يوسف |
| **BL-143** | 🔴 مسار تسريب مالي رابع لـTECHNICAL_OFFICE — لم يغطّه BL-134:
FINANCE_READ_ROLES في lib/finance/actions.ts:13 لا يزال يضم TECHNICAL_OFFICE،
يحرس getContractBalances (أرصدة عقود حيّة، finance/actions.ts:133) — أكشن
"use server" قابل للاستدعاء المباشر بغض النظر عن إغلاق شاشة /accounting.
lib/finance/scope.ts:28 لا يزال يمنحه نطاقًا ماليًا مُصفّى أيضًا. اكتُشف أثناء
تنفيذ BL-134 بتحقيق الوكيل الذاتي — لم يُصلَح، خارج نطاق التكليف الأصلي.
نفس نموذج تهديد BL-127. ✅ **مُغلق** (قاعدة الإغلاق): تحقّق كودي شامل (صفر أثر لتوقيع التسريب في
كامل .next/server) + تحقّق حي (حساب tech-office@egyglass.net بعد
build+restart فعلي لا يشوف غير الداشبورد وشاشة المكتب الفني — صفر بيانات
مالية في أي شاشة متاحة له). | يوسف |
| **BL-144** | 🟠 4 روابط في القائمة الجانبية بلا تقييد صلاحيات — nav.ts:9-12
(dashboard، customers، quotations، inspections) بلا roles، بعكس
nav.accounting المصحَّحة (BL-134). النتيجة: أي دور غير مصرَّح يضغط الرابط
فتردّه الصفحة صامتًا لـ/dashboard — هذا هو السبب الجذري لعرَض BL-135 الأصلي
عبر 3 أدوار (ACCOUNTING، SALES_REP، TECHNICAL_OFFICE).

الصلاحيات الفعلية: /inspections → ["ADMIN","INSPECTION_MANAGER",
"INSPECTION_REP"] · /quotations و/customers → ["ADMIN","SALES_MANAGER",
"SALES_REP","VIEWER"] · /dashboard → بلا قيد.

✅ الإصلاح منفَّذ بالكود (لم يُبنَ/يُنشَر بعد): roles: أُضيفت للثلاثة ·
مكوّن Unauthorized مشترك (lucide-react) بدل الـredirect الصامت · مفتاحا
i18n (ar+en) · tsc --noEmit صفر خطأ · إثبات حتمي لكل 14 دورًا. **لا يُغلق**
حتى: (1) build+restart فعلي، (2) جلسة دور غير مصرَّح حقيقية تؤكّد الرسالة.

| **BL-145** |🟡 اكتشاف جانبي: خط material-symbols غير محمَّل في المشروع — أيقونات
القائمة الجانبية كلها نص خام عبر التطبيق كله. مرشَّح لـBL-145 (تأكد
الرقم فاضي أولًا).✅ **مجزئيًا** (قاعدة الإغلاق): تحقّق حي بجلسة حقيقية (حساب ACCOUNTING،
بعد build+restart فعلي) — الروابط الصح (الحسابات/المستخلصات/الفواتير) ظهرت
بدل تكرار الداشبورد. النطاق: ظهور الروابط فقط، لا تقييم شامل لبيانات كل
شاشة. **الباقي مفتوح:** تحقّق رسالة "غير مصرح" لدور غير مسموح لم يتم بعد. | يوسف |

| **BL-146** | ✅ **مُغلق** — إصلاح اكتشاف BL-145 (أيقونات القائمة الجانبية
كانت تُعرض كنص خام لعدم تحميل خط material-symbols في المشروع). استُبدلت
بأيقونات lucide-react (SVG حقيقية، أسماء مُتحقَّقة من الحزمة المثبَّتة فعليًا
لا مخمَّنة). لوجو الشركة أُضيف أعلى الشاشات أيضًا (كان بالـsidebar فقط).
tsc --noEmit صفر أخطاء. | يوسف |


| **BL-147** | 🟠 فاتورة ملغاة تُطبع كأنها عادية — بلا أي ختم/علامة "ملغاة"
على الورقة، حالة الإلغاء موجودة بس في خلية صغيرة بالترويسة. خطر: مستند
مطبوع ممكن يتلخبط بفاتورة سارية فعليًا. | يوسف |

| **BL-156** | 🟠 غياب نمط تنقّل موحّد (زر رجوع / مسار تنقّل)
الوصف: معظم شاشات التفاصيل في النظام بلا أي وسيلة رجوع داخل التطبيق.
النطاق: عام (كل الإدارات) — لا يُعالَج داخل موجة قسم واحد.
الأعراض المسجَّلة: IN-42 (تناقض سلوك المنع) · IN-43 (المعاينة نص لا رابط في ملف العميل)
الحل المقترح: مكوّن رجوع موحّد برابط صريح لصفحة الأب — لا router.back().
الأثر: مرتفع على المستخدمين الميدانيين (موبايل/تابلت).
الحالة: مفتوح — يحتاج جرد للشاشات المتأثرة قبل التقدير.

| **BL-157** | 🟡 تشديد صلاحيات مجلد الرفع — **مؤجَّل لنشر UAT، وغير قابل للتنفيذ في dev**
المنشأ: بند IN-41 في موجة IN-A. البند طُلب على أساس أن المجلد `drwxrwxrwx` وملكية
`root` وأن فيه ملفَّين يتيمين. **الأساسان صُحِّحا بالدليل:**
(١) `/app` مُركَّب 9p/drvfs من `E:\` بخيارات `uid=0;gid=0` مثبَّتة في الـmount نفسه،
والتطبيق يعمل بـ`uid=0(root)` ⇒ بتات الصلاحيات **لا تفرض شيئًا** على root، و777
على `var` و`var/uploads` أثر إنشائهما من ناحية ويندوز (بدليل أن `uploads/inspections`
يظهر 755 لأن node أنشأه داخل الحاوية بـumask). فتشديدها في dev **مسرحية أمنية**.
(٢) **لا ملفات يتيمة:** 4 ملفات على القرص ↔ 4 صفوف `Attachment`، كلها تشير إلى
معاينات قائمة غير محذوفة (`parentId` مُتحقَّق بـJOIN). لا حاجة لبند فحص صفوف معلّقة.
الضبط النافذ فعلًا على الملفات = حارس `src/app/uploads/[...path]/route.ts` (جلسة
إلزامية) + `src/middleware.ts` — لا نظام الملفات.
العمل الحقيقي المتبقّي (production فقط، حيث `egyglass_uploads` مجلد ext4 حقيقي):
تشغيل الحاوية بمستخدم غير root وتمليكه مجلد الـvolume. يمسّ `Dockerfile`/
`docker-compose.prod.yml` ⇒ يخرج عن نطاق أي موجة تطبيقية، ويُنفَّذ مع رفعة UAT.
الحالة: مفتوح — لا يُنفَّذ الآن بقرار نطاق (CLAUDE.md: لا تلمس إعدادات UAT).

| **BL-158** | 🔴 **سرّ في مستودع متتبَّع** — `docker-compose.prod.yml:8` فيه
`POSTGRES_PASSWORD: EgyGlass2026Prod` نصًّا صريحًا، والملف متتبَّع في git
(`git ls-files` مُتحقَّق؛ أُضيف في `e9978b3` ثم `07e9db7`). خرق L-09 مباشر.
اللازم قبل أي رفعة UAT: نقل القيمة إلى متغيّر بيئة (نفس نمط `DATABASE_URL`/
`AUTH_SECRET` في نفس الملف)، وتوليد كلمة جديدة وقت النشر لأن القيمة الحالية
باقية في تاريخ git. الحالة: مفتوح — أُبلغ في تقرير بوابة P0 لموجة IN-A.

| **BL-160** | ✅ **مُغلق (موجة C2، 2026-07-31).** العمود صار `SiteReadiness` enum ثلاثي
(`READY` · `NOT_READY` · `UNCONFIRMED`)، فصار التمييز **في البيانات** بدل التاريخ:
البوابة تفحص `siteReadiness === "UNCONFIRMED"` صراحةً، و**`SITE_READINESS_GATE_FROM`
حُذف من الكود** (مُتحقَّق: صفر استعمال حيّ، والمطابقات المتبقية نصّ تعليق).
الترحيل مُتحقَّق على القاعدة: `READY=3` · `NOT_READY=1` · `NULL=23` — مطابق للحالة قبله.
**و`null` بقيت قيمة رابعة ذات معنى** («لم يُسأل أصلًا») ولها نصّها المنفصل في الواجهة،
فلا تُطوى في `UNCONFIRMED` — وهذا هو جوهر البند. **النصّ الأصلي أدناه للأثر:**
| ~~BL-160 (الوصف الأصلي)~~ | 🟠 **`SITE_READINESS_GATE_FROM` يعتمد على تاريخ صلب يُضبط يوم الدمج (IN-48، موجة B2).**
بوابة جاهزية الموقع تحجب الجدولة عند `siteReadiness = null`، لكن **كل** معاينة قائمة
قبل الموجة قيمتها `null` (الحقل لم يكن يُكتب من أي واجهة) ⇒ تطبيق البوابة على الكل
كان سيحبس 18 صفًّا (12 `REQUESTED` + 6 `SCHEDULED` — مُتحقَّق على القاعدة). فالتمييز
بـ`createdAt >= SITE_READINESS_GATE_FROM` في `src/lib/services/inspections.ts`.
**المشكلة:** `null` غامضة بين «صف قديم» و«جديد اختار: لم يؤكّد العميل»، فلا يوجد
تمييز بالبيانات، والحدّ الزمني يتسع كل يوم يتأخر فيه الدمج. **مرفوض بديلًا:** استخدام
`ActivityLog` كمميِّز — يخالف قاعدة المشروع «ActivityLog أثر لا مصدر حقيقة» (BL-81).
**الخطر مُحتوى الآن لا مُلغى:** أضفنا مسار تصحيح داخل المنتج (المبيعات تعدّل الجاهزية
من شاشة التفاصيل) ⇒ الصف المحصور يحتاج **نقرة من المالك** لا تعديل كود ونشرًا.
مُتحقَّق: صفر صفوف محصورة اليوم (أحدث معاينة 2026-07-30 وهي `DONE`).
**المطلوب:** (أ) يوسف يضبط التاريخ ليوم الدمج قبل الدفع؛ (ب) الحلّ الجذري = عمود
tri-state حقيقي (`SiteReadiness` enum بقيمة `UNCONFIRMED` صريحة) عبر SCR، فيسقط
الغموض والحدّ الزمني معًا.
**🔴 تحديث موجة B3 — عرَض ثانٍ لنفس الجذر (رُصد أثناء تصحيح النص):** لافتة التحذير
في حوار الجدولة (`inspections-client.tsx`) تظهر لكل صف `siteReadiness === null`
**بصرف النظر عن `createdAt`**، بينما البوابة الخادمية لا تسري إلا على ما بعد الحدّ.
فعلى الصفوف الأقدم اللافتة تقول «لا يمكن جدولتها» وهي **تُجدوَل فعلًا**، وبعد B3 تقول
أيضًا «تم إخطار المبيعات تلقائيًا» ولا إشعار يُرسَل لها. اللافتة لا تملك ما تميّز به
(الحدّ ثابت خادمي، و`null` غامضة أصلًا) — وهو **نفس الغموض** الذي يصفه هذا البند، لا
عيب جديد. يسقط تلقائيًا مع عمود tri-state في (ب). **الأثر محدود** بصفوف ما قبل الحدّ
وحدها، وهي في طريقها للاستهلاك. الحالة: مفتوح.
---
| **BL-159** | 🟡 **سؤال معلَّق لحسن** — هل الحقائق الأقوى تكسر المراحل البشرية؟
المنشأ: IN-37 (موجة B1). أضفنا `HUMAN_OWNED_STAGES` في
`src/lib/services/status-derivation.ts` = {`REJECTED` · `FOLLOW_UP` ·
`RE_INSPECTION_FOLLOWUP`} — ثلاث مراحل لا يستطيع الاشتقاق إنتاجها (الـenum ثماني
قيم والاشتقاق يُنتج خمسًا)، فصارت **محصّنة تمامًا** ضد إعادة الاشتقاق في كل
مستدعييها الخمسة (العقود · التركيبات · التسعير · إنشاء المعاينة · أحداث المعاينة).
**السؤال:** التحصين مطلق اليوم. فلو عميل في `FOLLOW_UP` **وُقّع له عقد** أو
**اكتمل تركيبه**، تبقى مرحلته `FOLLOW_UP` ولا تصير `CONTRACT`/`EXECUTION` — أي أن
حقيقة أقوى وموثّقة (عقد موقّع) لا تكسر متابعة بشرية. هل هذا مقصود؟
الاحتمالات: (أ) التحصين المطلق يبقى — المتابعة قرار بشري لا يُدهس بحال؛
(ب) تدرّج: `hasContract` أو `installationCompleted` يكسران `FOLLOW_UP` و
`RE_INSPECTION_FOLLOWUP` ويبقى `REJECTED` وحده محصّنًا مطلقًا.
الأثر الحالي: صفر — لا يوجد في القاعدة عميل في مرحلة بشرية وله عقد أو تركيب
مكتمل (مُتحقَّق). فالقرار غير عاجل، ويجب أن يُحسم قبل أول حالة كهذه.
الحالة: مفتوح — قرار عمل مؤجَّل لحسن، لا يُخترع في الكود.
---

| **BL-161** | 🟡 **سجل الأحداث يعرض معرّفات خامًا لا أسماء** (منشؤه IN-45، موجة B3).
`ActivityLog.details` للجدولة/إعادة الجدولة JSON بشكل `{assigneeId:{from,to}, …}`
يحمل **cuid المستخدم** لا اسمه، فيظهر في الشاشة كسلسلة عشوائية. العرض صحيح وأمين
(يعرض ما كُتب حرفيًا) لكنه غير مقروء للمستخدم الميداني.
**لم يُعالَج في B3 عمدًا:** الحلّ يحتاج استعلامًا إضافيًا لتحويل المعرّفات لأسماء،
وبند IN-45 نصّ صراحةً على «قراءة فقط بلا استعلام جديد ولا توسيع نطاق».
الحل المقترح: جمع معرّفات المستخدمين من الأثر ثم `findMany` واحد لترجمتها — أو
(أنظف) توحيد كتّاب الأثر على تخزين الاسم بجوار المعرّف وقت الكتابة.
⚠️ **لا يُشتق من الأثر منطق** (BL-81) — هذا تحسين عرض بحت. الحالة: مفتوح.
---

| **BL-162** | 🟡 **`addMeasurementAction` يطوي كل أخطاء zod في مفتاح عام واحد**
(رُصد أثناء IN-22، موجة B3). `inspections/actions.ts` → `if (!parsed.success)
return { error: "errors.invalidInput" }` — فرسائل الحقول المُصاغة في
`addMeasurementSchema` (`errors.required` للبيان · `multipleOf(0.001)` للدقة)
**لا تصل المستخدم أبدًا**. IN-22 عالج ما يراه المندوب فعلًا (فحص دقة في الواجهة
برسالة `errors.measurementPrecision` قبل النداء)، لكن **نداءً خادميًا مباشرًا بأربع
خانات عشرية ما زال يعود بـ«بيانات غير صالحة» العامة**.
النمط الصحيح قائم في **نفس الملف**: `addInspectionAttachment` يُصعّد أول رسالة حقل
(`parsed.error.errors[0]?.message`). لم يُطبَّق هنا في B3 لأنه يغيّر مفتاح الخطأ
العائد لكل حقل في هذا المسار — تحسين مرغوب لكنه خارج نصّ البند (L-14).
⚠️ الأرجح أن النمط يتكرر في أكشنات أخرى — يستحق مسحًا لا إصلاحًا موضعيًا
(نفس ملاحظة BL-154). الحالة: مفتوح.
---

| **BL-163** | 🟡 **لا سجل أحداث عابر للكيانات لدورة الطلب** (قرار IN-25، موجة B3).
سجل أحداث المعاينة (IN-45) يقرأ `entity = "InspectionRequest"` **وحده**. وسجلات
اشتقاق الحالة تُكتب على `entity = "QuotationRequest"` (`status-derivation.ts`)
فلا تظهر في الشاشتين معًا كخيط واحد.
**القرار المتَّخذ صراحةً في B3: تُستثنى** — موثَّق في الكود عند الاستعلام
(`inspections/actions.ts`, `getInspectionDetail`). السببان: (١) حدّ الكيان — تلك
أحداث الطلب ولها شاشته، وضمّها ينسب حدث الطلب للمعاينة؛ (٢) **توسيع تصريح**: مَن
يرى المعاينة لا يرى الطلب بالضرورة والعكس، فالضمّ يفتح سؤال نطاق ثانيًا يمنعه نصّ
البند («لا حارس جديد ولا توسيع نطاق»).
**البند المفتوح:** إن طُلب خيط زمني موحّد للطلب (طلب تسعير → معاينة → عرض → عقد)
فهو **ميزة مستقلة** لها قرار نطاق خاص بها، لا توسيع لسجل المعاينة. الحالة: مفتوح.
---

| **BL-164** | ⚪ **بنود المعاينات المؤجَّلة خارج الموجات A · B1 · B2 · B3** — مسجَّلة
كي لا تضيع بإغلاق القسم. لا يُنفَّذ أيٌّ منها داخل موجة قسم.

| البند | لماذا أُجِّل | يُحسم عند |
|---|---|---|
| `IN-44` طباعة/تصدير المعاينة | **ميزة جديدة لا إصلاح** — تحتاج قرار تصميم: ماذا يُطبَع (مقاسات؟ صور؟ كروكي؟) ولمن | يوسف — قرار تصميم |
| `IN-42` توحيد سلوك المنع | **عابر للأقسام** — يُحسم مرة واحدة للنظام كله لا داخل المعاينات. عرَض من أعراض [[BL-156]] | قرار نظام |
| `IN-19` نمط التغطية للمعاينات | مرتبط ببند التفويض ([[BL-149]] سابقًا) الذي يحتاج جلسة تصميم مخصّصة | جلسة تصميم |
| `D-IN-7` منح `PROJECTS` حق طلب المعاينة | يحتاج تحديد الشاشة أولًا — سؤال مفتوح | يوسف |

الحالة: مفتوحة كلها — مسجَّلة لا مُنفَّذة.
---

| **BL-165** | 🟠 **لا تمثيل موحّد لـ«من المسؤول الآن» عبر مراحل الطلب** (بند عابر
للأقسام — حُسم تسجيله في موجة B3).
«المسؤول» يعني **ثلاثة حقول مختلفة حسب المرحلة**: `ownerId` على العميل ·
`assigneeId` على المعاينة · `assignedTo` على الطلب — **ولا شاشة تعرضها موحّدة**،
فالمستخدم يفتح ثلاث شاشات ليعرف عند مَن الكرة الآن.
**الحل المقترح الأرخص:** بطاقة «المسؤول الحالي» **محسوبة في الواجهة** تُفرّع حسب
المرحلة — بلا schema وبلا عمود جديد. ولو تكرّرت الحاجة عبر شاشات متعددة، **يُعاد
النظر في حقل موحّد بـSCR** حينها لا قبلها.
**النطاق عابر للأقسام** (مبيعات · معاينات · مكتب فني)، فلا يُنفَّذ داخل موجة قسم
واحد. **مرشَّح لموجة C** مع بند «نتيجة المطابقة». الحالة: مفتوح.
---

### 🔒 خلاصة إغلاق المعاينات

**حالة إدارة المعاينات (2026-07-31):** الموجات A · B1 · B2 · B3 منفَّذة ومدموجة (B3 عند `cb1e006` — PR #30، commit `cfba8ed`). المتبقي: موجة C (تغييرات schema — نتيجة المطابقة · الطوابع الزمنية · تأكيد استلام المكتب الفني · «مؤجّلة» · عمود tri-state لجاهزية الموقع) معلَّقة على إجابات حسن · وبنود عابرة للأقسام (زر الرجوع BL-156 · طبقة الإشعارات · توحيد الداشبورد · تنسيق التواريخ · «المسؤول الحالي» BL-165). **لا يُعاد فتح تدقيق المعاينات إلا عند إغلاق موجة C أو ظهور أدلة ميدانية جديدة.**

---

# 🟡 قرارات معلّقة — **ممنوع الاختراع فيها**

| # | السؤال | القرار عند |
|---|---|---|
| **BL-18** | **تحقَّق (2026-07-12، grep كامل): لا يوجد أي حقل يسجّل موافقة العميل.** كتّاب `Quotation.status="APPROVED"` اثنان فقط وكلاهما داخلي: (1) `lib/pricing/actions.ts:501 updateQuotationStatus` — قائمة حالات عامة بيد `PRICING_ROLES` من شاشة تفاصيل العرض (`quotation-detail.tsx:115`)، (2) `src/lib/actions/discount.ts:247` — قرار خصم بيد SALES_MANAGER/ADMIN. تسمية APPROVED في الواجهة = "معتمد" (عامة). "موافقة العميل" تظهر في الكود **فقط** كعنوان مربع توقيع ورقي في قالب الطباعة (`(print)/quotations/[id]/print/page.tsx:376`) — ليست حقل بيانات. **الخلاصة: `APPROVED` = اعتماد داخلي، وموافقة عميل السوشيال غير مسجَّلة في النظام إطلاقًا. لم يُبنَ حارس التزام للسوشيال في إصلاح السلسلة (التزامًا بـ STD-05) — القرار عند يوسف: حقل جديد (SCR) أم آلية أخرى.** | **يوسف — مفتوح** |
| **BL-19** | **الفلوشارت قديم** — يضع المراجعة قبل إرسال عرض السعر، وصاحب العمل يقول بعد التعاقد. **يُحدَّث أو يُعلَّم "مرجع تاريخي".** | يوسف |
| **BL-20** | **مصير `RELEASED_TO_FACTORY` على الرسمة** — الإفراج صار خاصية **أمر التصنيع** بعد اعتماد REVIEW. **يُقسَّم:** SCR-017a (إضافة `DrawingStatus.SUPERSEDED`، صفر خطر) · SCR-017b (حذف الأعمدة والحالات الميتة — بعد مسح بيانات الاختبار). | يوسف (SCR) |
| **BL-32** | ✅ **مُغلق (2026-07-12، قرار يوسف: /technical-office/[id]).** زر "إصدار أمر تصنيع" في شاشة تفاصيل الطلب (بيت TEC_APPROVER) — `tec-detail-client.tsx` (TEC_APPROVER/ADMIN فقط، معطّل بسبب صريح: "الرسمة غير معتمدة"/"لا يوجد عقد"/"أمر التصنيع صادر بالفعل"). الحارس server-side كما هو. مُثبت بالجلسات (proofs 3/4/5 + حارس العقد). ~~[verified-in-code] createManufacturingOrder صار يتيمًا بلا مدخل~~ | مُغلق |
| **BL-33** | ✅ **مُنفَّذ ومُغلق (2026-07-12، D-12).** `approveOrder` (REVIEW) = مطابقة ثلاثية فقط، بلا مصنع/تاريخ → IN_PRODUCTION بـ factory/date=null. أكشن جديد `assignFactory` (PROCUREMENT) يعيّنهما (`mfg-review.ts` + `manufacturing/[id]/actions.ts`). **بوابة READY** (قرار ب): `updateMfgStatus` يرفض IN_PRODUCTION→READY بلا مصنع (`errors.factoryNotAssigned`). الحقول نُقلت في `review-panel.tsx` لـ PROCUREMENT. مُثبت بالجلسات: REVIEW يعتمد بلا مصنع✅ · READY بلا مصنع مرفوض✅ · REVIEW على assignFactory مرفوض✅ · PRC يعيّن✅ · READY بعده✅ · لا جمود. | مُغلق |
| **BL-31** | ✅ **محسوم بـ D-10 (2026-07-12).** الدفع شرط قبل التصنيع في المسارين، بنسبة متغيرة لكل طلب بلا حد أدنى. (كان ادعاء "100%" في commit `6f0908c` غير مؤكَّد — القرار النهائي: نسبة حرة لا رقم ثابت.) التنفيذ يحتاج BL-34 (Contract للسوشيال) + بوابة الدفع قبل التصنيع. | محسوم — بانتظار تنفيذ |
| **BL-34** | ✅ **مُحلّ app-layer (PHASE 1) — بلا schema.** السوشيال يحصل على `Contract` عام (النموذج عام أصلًا، لا حقول مشروعات إلزامية) يُنشأ آليًا عند أول دفعة → `totalValue` مجمّد → محرك الدفعات وحارس الالتزام يعملان. **`contractType` غير مطلوب** (المسار مشتق من `quotation.technicalRoute`، العقد 1:1). **BL-18 (حقل موافقة العميل) يبقى مفتوحًا** لكنه غير حاجب (D-13: الموافقة = الدفعة). | مُغلق (app-layer) |
| **BL-35** | ✅ **verified-in-code (2026-07-12).** `savePaymentPlan` (`lib/finance/actions.ts:14,34`) و`addPayment` (`lib/accounting/actions.ts:200,211`) كلاهما `ACCOUNTING/ADMIN`. **مسار كتابة وحيد على `Payment`، صفر `update`/`delete`/`raw`.** الخطر المفترض (SALES_REP يسجّل دفعة وهمية) **لا يتحقق.** | مُغلق |
| **BL-40** | 🟡 **تشخيص (2026-07-12، قراءة `docker-compose.yml`) — لا volume مخصّص لـ `public/uploads`، لكن bind mount `- .:/app` (السطر 51) يغطّي الجذر كاملًا** → محليًا الرسومات تُكتب على host (`./public/uploads`) وتنجو من rebuild/down-up. **الخطر الحقيقي في UAT/production:** إن لم يُكرَّر هذا الـ bind (نشر بصورة image فقط) → الرسومات في طبقة الحاوية العابرة = تُفقد. **التوصية المسجّلة:** named volume/bind مخصّص لـ `public/uploads` قبل UAT. | يوسف (قبل UAT) |
| **BL-42** | ✅ **محسوم (2026-07-12) — شاشة الأدمن سليمة.** النموذج: زر "حذف" = **تعطيل ناعم** (`deletedAt`+`isActive=false`) ↔ "إعادة تفعيل" (مشروط بـ `deletedAt`). العطل الظاهر (لا زر تفعيل) نتج عن **كتابة مباشرة سابقة للوكيل** (`isActive=false` بلا `deletedAt`) أنتجت حالة خارج النموذج — **نفس ما تحذّر منه STD-07.** BL-47 يهدأ تبعًا: ADMIN عامل يستطيع استعادة أي مستخدم من الشاشة. | مُغلق |
| **BL-45** | ✅ **مُغلق (PHASE 1، 2026-07-12).** `createContract` يرفض `SOCIAL_MEDIA` **لكل الأدوار بما فيها ADMIN** (`lib/contracts/actions.ts` → `errors.socialContractAutoOnly`). مُثبت بالدليل الخام: SALES_REP + ADMIN مرفوضان · مشروعات ينجح. المسار الوحيد لعقد السوشيال = `addPayment` عبر `createContractCore`. | مُغلق |
| **BL-43** | ✅ **مُغلق (PHASE 1، D-14).** عقد السوشيال يُنشأ آليًا في `addPayment` عند أول دفعة على عرض معتمد (`reviewStatus=APPROVED`) بلا عقد، **في transaction واحدة**، `createdById=المحاسب`، `totalValue` snapshot من العرض (`lib/accounting/actions.ts` + `src/lib/services/contract-core.ts`). مُثبت: `getContractBalances` يعمل فورًا · createdBy=ACCOUNTING. | مُغلق |
| **BL-44** | ✅ **مُغلق (PHASE 2).** حارس `createManufacturingOrder` موحّد: **عقد إلزامي للمسارين** (حُذف شرط `route===PROJECTS`) + **بوابة الدفع** (دفعة واحدة على الأقل، D-10 — لا عتبة). مُثبت بالدليل الخام: سوشيال بلا عقد→مرفوض · مشروعات بلا دفعة→مرفوض · النجاح في المسارين. | مُغلق |
| **BL-46** | ✅ **رُصد (2026-07-12): صفر** عرض `SOCIAL_MEDIA` عليه دفعات وبلا عقد (استعلام مباشر). لا بيانات يتيمة ستُحجب بعد توحيد الحارس (BL-44). **رصد فقط — لم أُهاجر.** | مُغلق (رصد) |
| **BL-48** | **سكريبت break-glass أُضيف** (`scripts/reset-password.mjs`). الحل الدائم = مسار إعادة تعيين داخل التطبيق (بعد UAT). السكريبت **لا يمنح قدرة جديدة** — من يملك الحاوية يملك القاعدة أصلًا — لكنه **يجعل الفعل مسجَّلًا** (`ActivityLog: PASSWORD_RESET_BREAKGLASS`) بدل كتابة صامتة. | ✅ مُضاف — الدائم بعد UAT |
| **BL-41** | ✅ **مُغلق (PHASE 2).** D-10 مطبّق — `createManufacturingOrder` يرفض بلا دفعة مسجّلة (`errors.noPaymentForManufacturing`). دفعة واحدة تكفي (لا عتبة). مُثبت بالجلسات. | مُغلق |
| **BL-36** | عمود **"نسبة المقدم"** في لوحة ADMIN — مشتق من `PaymentMilestone[0]`. **غير حاجب:** يُظهر النمط، وADMIN يضع سياسة بالبيانات لا بالتخمين. | يوسف |
| **BL-37** | 🔴 **حسابات UAT غير مُنشأة — REVIEW بلا حساب إطلاقًا.** (حساب dev-review-a للاختبار فقط.) **بلوكر UAT.** | يوسف |
| **BL-38** | **حسابات dev + بيانات دورة P6** تُعطَّل/تُنظَّف قبل UAT. **+ إضافات جلسة D-31/D-32 (2026-07-14):** (أ) **3 معاينات اختبار** أُنشئت أثناء إثبات PHASE 2 — على الطلبات `TEC-PRJ-0002` · `TEC-SOC-0001` · `TEC-PRJ-0007` (صفوف صحيحة لا يتيمة، لكنها بيانات اختبار؛ ملحوظة: معاينة `TEC-SOC-0001` أُنشئت قبل تفعيل بناء D-32 فلا إشعار مالك لها). (ب) **حساب `INSPECTION_MANAGER` الجديد** الذي أُنشئ للإثبات (القديم `inspection@egyglass.com` يبقى معطّلًا — BL-39). | يوسف |
| **BL-39** | 🚨 **كلمة مرور `inspection@egyglass.com` غُيّرت أثناء الاختبار** — تُدوَّر قبل UAT. | يوسف |

---

# ⚪ مؤجل بقرار موثّق

| # | البند | ملاحظة |
|---|---|---|
| **BL-21** | **موديول المخازن (Inventory)** — النموذج المعتمد: **سجل حركات append-only** (`StockMovement`: IN/OUT/ADJUST + كمية + سبب + مرجع) · **الرصيد مشتق** (لا عمود رصيد قابل للتعديل) · **حجز عند إصدار أمر التصنيع** (المتاح = الموجود − المحجوز) | **ليس بلوكر UAT** — PROCUREMENT يعمل بدونه. SCR منفصل، مرحلة 2 |
| **BL-22** | آلية الملحق (Addendum) الكاملة | ما بعد UAT |
| **BL-23** | تعدد الأدوار (مثال: ACCOUNTING + HR لنفس الموظف) | تدقيق قراءة أولًا |
| **BL-24** | إرفاق رسمة بعرض السعر (رفع صورة مباشر) | لا ربط Drawing↔Quotation |
| **BL-25** | فصل قسمين فرعيين داخل `TECHNICAL_OFFICE` (مشروعات/سوشيال، R-03) | يحتاج حقل قسم فرعي |
| **BL-26** | شاشات المستخلصات/الفواتير (SCR-015) | schema مطبّق · التطبيق معلّق |
| **BL-27** | `getSettings()` مكرر — لا accessor مركزي (`lib/config.ts`) | دَين تقني |
| **BL-28** | تنظيف مراجع `NODE_ENV` القديمة (`AGENTS.md` · `CLAUDE.md` · `START-HERE.md`) | توثيق |
| **BL-29** | `calculateRecipe` غير مُهاجَر لـ Decimal (~1e-12) | تحسين |
| **BL-30** | قوائم أسعار لكل مُصنِّع | **ملغى كأولوية** |
| **BL-98** | hook يمنع الوكيل من تعديل schema.prisma / CLAUDE.md | من atlas-kit (ملغي) — غير عاجل |
| **BL-99** | خطأ ESLint "circular structure" في next build (لا يكسر البناء) | تنظيف |
| **BL-103** | 🟡 **`protect.py` إنذار كاذب: يحجب أي أمر يحوي `>` + مسارًا محميًا — حتى لو كان `>` داخل نص عرض** (`echo "copied -> ..."`). حجب أمر `cp` آمن (قراءة فقط) في جلسة SCR-018. **الحارس يفشل مقفولًا — وهذا مقصود** (الدرس: hook يفشل مفتوحًا ليس ضابطًا). **لا يُعدَّل الآن** — تعديل حارس = خطر بلا مقابل. الحل التشغيلي: الوكيل يتجنّب `>` في نصوص العرض. | يوسف — مؤجل |
| **BL-119** | 🟡 **فورمات المقاس/الحذف/المرفق لا تُعطَّل استباقيًا عند `APPROVED`** (D-40/B1، 2026-07-16 — أنتجه FLOW-CRITIC). الرفض **صريح** server-side (`errors.inspectionApprovedLocked`) + رسالة مترجمة تظهر بعد النقر — **ليس صامتًا، BL-32 ✅** — لكنه **تفاعلي لا استباقي**، بخلاف بوابة الاعتماد نفسها (تُخفى/تُعطَّل حسب الحالة). تحسين اتساق واجهة. | تحسين |
| **BL-120** | 🟡 **مفتاح يتيم `inspectionCompletedTitle` + نوع `INSPECTION_COMPLETED`** (`messages/ar.json:156` / `en.json:157`) بلا كاتب بعد حذف كتلة DONE→TECHNICAL_OFFICE في `updateInspectionStatus` (D-40). يتيم **كودًا لا لغةً** — بقايا تنظيف (نمط BL-107). | تنظيف |
| **BL-121** | 🟡 **over-fetch مالي في `manufacturing/[id]/page.tsx`:** `quotation.total` (:37) و`items.unitPrice` (:39) يُجلبان من القاعدة لكن **لا يُعرَضان** (`threeWay` يُسقطهما — يمرّر description+quantity فقط). صفر تسريب حالي لـREVIEW، لكن **تدنية بيانات ناقصة** — لو عُرِضا لاحقًا = كشف مالي. رُصد في B2/D-41 (2026-07-16)، خارج نطاق إصلاح `unitCost` المحصور — لم يُلمَس. | تنظيف/يوسف |
| **BL-122** | 🔴 **[منهجي] `next.config.ts` ignoreBuildErrors:true** — البناء لا يفحص أنواع TypeScript. كل "build GREEN" يثبت إنتاج مخرجات لا سلامة أنواع. قرار يوسف: إزالته وإصلاح ما يظهر، أم فحص tsc منفصل؟ أولوية عالية. | يوسف |
| **BL-123** | 🔴 **D-03 لم يتحقّق في B2:** إخفاء unitCost عن REVIEW client-side فقط (ExtraItemsPanel:64)، لكن page.tsx:180 يمرّره بلا شرط ⇒ يصل متصفح REVIEW (DevTools). إخفاء عرض لا حماية (STD-15). يُصلَح خادميًا قبل commit B2. | يوسف |
| **BL-124** | 🟡 فجوات B2 ثانوية: أداة تغيير الحالة تظهر لـREVIEW (رفض خادمي، أداة ظاهرة) · نوع العميل قديم بلا UNDER_REVIEW · canViewCost prop ميت · ADMIN يرى مدخلَي nav متطابقين. تنظيف. | يوسف |
---

## 🧭 بنود دائمة

| # | البند |
|---|---|
| **STD-01** | **بعد كل قرار:** تحديث `CLAUDE.md` + `AGENTS.md` — **يوسف فقط**، مرة واحدة بعد الحسم. |
| **STD-02** | **مصدر الحقيقة = الكود.** ليس تعليقات الـ schema (تعليق `ceoDrawingApprovalThreshold` قال `null = كل المشروعات`، والسلوك `null = تخطّي`). |
| **STD-03** | أي متطلب يولد في محادثة **يُقيَّد هنا في نفس اللحظة** — قبل ما يتحول لكود أو migration. |
| **STD-04** | **الفلوشارت قديم** — قسم "قرارات محسومة" **يعلو عليه**. |
| **STD-05** | **الجهل يُكتب، لا يُصمَّم.** (القاعدة الكاملة أعلى الملف.) |
| **STD-06** | **كل بند في هذا الملف يُوسَم `verified-in-code` (بملف/سطر) أو `unverified`.** ادعاء بلا سند سطري = `unverified` صراحةً — لا يُبنى عليه. (نشأت من رسالة commit وصفت تعديلات وقاعدة دفع لم تكن في الملف ولا في الكود.) |
| **DBW-01** | **كتابة مباشرة مأذونة (STD-07، إذن يوسف الصريح · 2026-07-12).** `UPDATE "users" SET "isActive"=true WHERE email LIKE 'dev-%' AND "deletedAt" IS NULL AND "isActive"=false;` — **السبب:** كتابة مباشرة سابقة للوكيل أنتجت حالة خارج النموذج (isActive=false بلا deletedAt) لا مسار واجهة يعيدها؛ التصحيح يتطلب كتابة واحدة. **الحسابات المتأثرة:** dev-rep-e · dev-tecm-e · dev-tec-a · dev-appr-b · dev-acc-d · dev-review-a · dev-lead-c · dev-tech-c · dev-prc-a (كل dev-* المعطّلة غير المحذوفة). **مستثنى صراحةً:** `inspection@egyglass.com` (يبقى معطّلًا — يخرق سياسة @egyglass.net). |
| **STD-10** | **نتعامل بالمناصب (roles) لا بالأشخاص.** الاسم = شاغل المنصب اليوم؛ المنصب هو الثابت. كل قرار في BACKLOG يُكتب بالدور، والاسم بين قوسين كإشارة. (جذر كارثة بوابة CEO: قُرئت "المدير التنفيذي" كشخص لا كمنصب.) **الاستثناء الوحيد: منع اعتماد الذات — يُبنى على الشخص لا المنصب.** ✅ **تحقّق 2026-07-12:** صفر منطق يستهدف شخصًا/userId صلبًا في الكود؛ كل مقارنات userId = ملكية/ذات ديناميكية؛ الأسماء في التعليقات فقط؛ `assertNotSelf` = الاستثناء المسموح. |
| **STD-09** | **ممنوع تخزين أي كلمة مرور في ملف نصي داخل شجرة العمل — حتى لحساب dev.** النمط ينتقل إلى UAT. الاعتماد يُمرَّر عبر stdin أو env، لا ملف. (الدرس 3: سر يدخل git = يبقى في التاريخ للأبد.) **تحقّق 2026-07-12:** ملفات كلمات dev كانت في scratchpad الجلسة (خارج الريبو) — **صفر منها دخل git**؛ `scr016-pw.txt` حُذف. |
| **STD-08** | **ممنوع تسليم الوكيل أي بيانات اعتماد حقيقية.** الوكيل يعمل بحسابات dev فقط. أي فعل يتطلب ADMIN حقيقيًا → **يوسف ينفّذه بنفسه.** (نشأت من: كلمة مرور `admin@egyglass.net` مجهولة، والوكيل لا يُسلَّم ولا يُنشئ اعتمادًا حقيقيًا.) |
| **STD-15** | 🔴 **فحص الواجهة ليس حارسًا. أي قاعدة عمل تُفرَض server-side أو لا تُفرَض.** (نشأت من: `contract/page.tsx:25` حجب العروض غير المعتمدة من الشاشة فقُرئ كقاعدة نظام — والأكشن كان مفتوحًا. BL-88، 2026-07-13) |
| **STD-14** | 🔴 **ممنوع تعديل بيانات تكشف تناقضًا قبل تشخيصه.** "بيانات اختبار قديمة" ليست تفسيرًا — تُثبَت بالتواريخ أو تُرفَض. إصلاح العَرَض يمحو الدليل. (نشأت من BL-88: اعتماد عرض P5 "لإكمال الاختبار" كان سيمسح دليل الثغرة، 2026-07-13) |
| **STD-13** | 🔴 **ممنوع إدخال نص عربي عبر الترمنال/curl — يُخزَّن `????`.** بيانات الاختبار: إنجليزية، أو من الواجهة. الملفات (ar.json/BACKLOG) سليمة لأنها تُكتب بأدوات الملفات لا بالـshell. (نشأت من BL-84، 2026-07-13) |
| **STD-11** | **كل دليل واجهة يبدأ بإثبات هوية الجلسة (الاسم + الدور في الترويسة) قبل أي نقرة.** كوكي قديم (ADMIN عالق في الـpane) كاد يفسد دليل PHASE 1 للتحقيقات لولا الانتباه — الفاعل الخطأ = دليل باطل. (2026-07-13) |
| **STD-12** | **النقر البرمجي (JS) ليس دليل واجهة. الدليل = نقرة حقيقية في الشاشة.** JS يُستخدم للقراءة/الالتقاط فقط (نص toast مثلًا)، لا لإثبات سلوك مستخدم. (2026-07-13) |
| **STD-07** | **ممنوع الكتابة المباشرة على قاعدة البيانات كالتفاف على أكشن مكسور.** الأكشن المكسور = بند في BACKLOG، لا عقبة تُلَفّ. أي كتابة مباشرة تتطلب **إذن يوسف الصريح** وتُسجَّل في BACKLOG. (نشأت من واقعة: 4 حسابات عُطّلت بكتابة مباشرة بصفر `ActivityLog`، والالتفاف أخفى عطلًا محتملًا في شاشة الأدمن — BL-42.) |
| **STD-16** | **أي `next build` أثناء تشغيل السيرفر يُتبَع بـ restart إلزامي.** الـbuild يدهس `.next` فيشير manifest الذاكرة لملفات محذوفة → `Loading chunk failed` في المتصفح، ويبدو كعطل في الكود وهو عطل بيئة. (نشأت من جلسة D-34، 2026-07-14) |
| **STD-18** | **الوكلاء الفرعيون: للقراءة فقط. الكتابة بوكيل واحد دائمًا.** **مسموح** (قراءة · صفر كتابة · صفر git · صفر build): مرحلة **الاستطلاع** (قبل التنفيذ) · مرحلة **النقد** (بعد التنفيذ، قبل الـcommit). **ممنوع قطعًا:** كتابة كود · تعديل ملفات · migration · git · **جلسات الإثبات** (تتطلب دخول يوسف — لا تُوازى، STD-11/BL-82). **السقف: 3 وكلاء**، لكلٍّ دور مُسمّى ومخرَج مكتوب: **RECON** (الواقع بالدليل، صفر اقتراح) · **RBAC-CRITIC** (يهاجم الحارس · يفحص الملكية/منع الذات/التسريب/فحص واجهة قُرئ كحارس — STD-15) · **EVIDENCE-CRITIC** (يقرأ تقرير الإثبات ويقول ما ادُّعي ولم يُثبَت — مخرَج خام مفقود · هوية جلسة · نقر برمجي STD-12). كل تقرير: `verified-in-code` (ملف:سطر) أو "غير موجود" صراحةً. الوكيل الرئيسي يجمّع ولا يُخفي خلافًا. **كل برومبت تنفيذ يحمل: "صفر subagents. نفّذ بنفسك تسلسليًا."** (نشأت من: مهمة قراءة صغيرة أطلقت ~50 وكيلًا بلا طلب، 2026-07-14) |
---

## سجل التنفيذ (دفعة إعادة إسناد السلسلة — 2026-07-12)

- **PHASE 1** — تعطيل البوابتين المخترَعتين G2/G3 (BL-02/03). build GREEN + جلسات.
- **PHASE 2** — نقل اعتماد العرض لـ TEC_APPROVER (BL-05) · إلغاء التوليد التلقائي (BL-06) · حارس أمر التصنيع: دور + رسمة + عقد (BL-01/08) · approvedById (BL-16) · زر إصدار الأمر في `/technical-office/[id]` (BL-32، قرار يوسف).
- **PHASE 3** — بوابة REVIEW على أمر التصنيع: 3 تأكيدات + حجب server-side (BL-07، D-09).
- **PHASE 4** — دورة كاملة من الشاشة بصفر بذرة: عميل→طلب TEC-PRJ-0005→إسناد→عرض→اعتماد TEC_APPROVER→عقد→رسمة معتمدة→إصدار أمر تصنيع→مراجعة REVIEW(3 تأكيدات+اعتماد)→READY. الأثر يثبت كل دور في موضعه.
- **مفتوح لقرار يوسف:** BL-18 (موافقة عميل السوشيال) · BL-31 (دفع السوشيال) · BL-33 (ملكية اختيار المصنع) · BL-20 (حذف أعمدة/enums الميتة، SCR).

*آخر تحديث: 2026-07-14 — D-31/D-32/D-33/D-34 · BL-63/78/91/92/93/96 مُغلقة · BL-94/95/97/98/99 مُسجَّلة.*

---

## سجل التنفيذ — موديول التحقيق BL-63 (SCR-017، 2026-07-13)

> schema مطبَّق سلفًا بيد يوسف (tag `schema-scr017a-done`، commit `0796a08`، migration 28). البناء app-layer فقط.

- **PHASE 1 — فتح التحقيق ✅ (2026-07-13):**
  - خدمة `src/lib/services/fault-investigations.ts`: `openFaultInvestigation` — `claimedFault` بخريطة BL-79 اليدوية (`BREAKAGE_REPLACEMENT→BREAKAGE · MFG_ERROR→FACTORY_ERROR · TEC_ERROR→TEC_ERROR · MEASUREMENT_ERROR→MEASUREMENT_ERROR`؛ `CLIENT_DELAY/OTHER` غير قابلَين للتحقيق عمدًا — لا يولّدان بديلًا أصلًا). `manufacturingOrderId` عبر `InstallationItem → InstallationOrder.manufacturingOrderId` (1:1، BL-80 granularity الأمر لا القطعة — مقبول بقرار يوسف).
  - action: `requireRole(["REVIEW","ADMIN"])` + zod + ActivityLog `INVESTIGATION_OPENED`.
  - تحقيق واحد لكل بند: فحص مسبق + قيد `@unique` على `installationItemId` (P2002 → `errors.investigationExists`).
  - نقطة الدخول (الدرس 2): شاشة `/investigations` جديدة (REVIEW/ADMIN) — قسم "بنود بلا تحقيق" بزر فتح + قائمة التحقيقات. مدخل قائمة جانبية `nav.investigations`. (إشعار REVIEW عند تسجيل البند قائم سلفًا في `installation-extras.ts`.)
  - **لا إشعار جديد عند الفتح** — غير منصوص عليه في D-25..29، والتزامًا بـ STD-05 لم يُخترع.
  - ✅ **مُثبَت بجلسة واجهة حقيقية (2026-07-13، طريقة BL-82: يوسف سجّل الدخول، الوكيل قاد الـpane):**
    - build GREEN داخل Docker · بلا مصادقة → 307 · فحوص خدمة سلبية (tsx داخل الحاوية): `CLIENT_DELAY`/`OTHER`→`itemNotInvestigable` · وهمي→`notFound` · صفر كتابة.
    - جلسة `dev-review-a` (REVIEW): الشاشة عرضت **4 بنود فقط** (CLIENT_DELAY/OTHER مستبعدان) والقائمة الجانبية بلا بنود ADMIN. زر "فتح تحقيق" على بند بدل كسر (Q-2026-00018) → الصف `cmrjdstmw0001mo3lof713bx3`: `claimedFault=BREAKAGE` (خريطة BL-79 صحيحة) · `manufacturingOrderId=cmri2pon9000jmo53mgjwf5uk` **مطابق للمشتق** من InstallationItem→InstallationOrder · `status=OPEN` · `verdictFault=NULL` · `openedBy=dev-review-a (REVIEW)` · ActivityLog `INVESTIGATION_OPENED` بنفس الفاعل.
    - منع التكرار: نقرات متكررة على نفس البند من تاب ثانٍ قديم → toast خطأ حرفي من الـDOM: **"يوجد تحقيق مفتوح على هذا البند بالفعل"**.
    - 🔧 **تصحيح لاحق (2026-07-13، PHASE 2):** جملة "القاعدة بقيت على صف واحد" في تقرير PHASE 1 كانت صحيحة لحظة فحص الـCOUNT لكنها **خاطئة لحظة الـcommit** — أثناء محاولات التقاط الـtoast، أعاد التاب "القديم" الجلب صامتًا (صفحة dynamic) فصار زرّه الأول بندًا آخر (خطأ مكتب فني)، ونقرة فتحته شرعيًا (تحقيق `cmrjed1db`، 15:48، dev-review-a). **قيد "تحقيق واحد لكل بند" لم يُخترق** (بندان مختلفان = تحقيقان، سلوك مقصود). الدرس: **عدّاد الدليل يُقرأ لحظة كتابة التقرير لا قبله** — وأي نقرة إضافية بعد آخر فحص تُلغيه.
    - ملاحظة جلسة: كوكي ADMIN قديم كان عالقًا في الـpane واكتُشف قبل أي نقرة (الاسم في الترويسة) — سُجّل الخروج ودخل يوسف بـREVIEW. الفاعل الصحيح مثبت بالقاعدة. (أنتجت STD-11/STD-12.)
- **PHASE 2 — شاشة الأثر ✅ (2026-07-13):** `/investigations/[id]` (REVIEW/ADMIN) تعرض الأضلاع الأربعة جنبًا إلى جنب: مقاسات المعاينة (ActivityLog `MEASUREMENTS_RECORDED` — نص، BL-81) · رسومات `TEC_APPROVED` فقط على نفس الطلب (المُلغاة/المُفرَج عنها مستبعدة) · اعتماد TEC_APPROVER (`approvedBy`+`approvedAt` من Drawing) · تأكيدات REVIEW الثلاث (`MFG_MATCH_CONFIRMED` — الفاعل والعنصر والوقت). + `evidenceNotes` (حفظ + ActivityLog `INVESTIGATION_EVIDENCE_SAVED`). **صفر استنتاج آلي للسبب (D-25) — عرض فقط.** ❓ سؤال مفتوح: هل تُقفل `evidenceNotes` بعد `JUDGED`؟ غير منصوص — لم يُخترع قفل، يُحسم مع PHASE 3.
  - ✅ **مُثبَت بجلسة واجهة (طريقة BL-82، هوية مُثبتة قبل كل جولة STD-11 — وأمسكت محاولتي دخول بهوية خاطئة قبل أي نقرة):**
    - **لا أمر قائم يحمل الأضلاع الأربعة مجتمعة** — الإثبات بتحقيقين متكاملين (بلا أي فبركة بيانات):
    - جولة `dev-lead-c` (INSTALLATIONS): `/investigations` و`/investigations/[id]` كلاهما **رفض وأعاد التوجيه** (فحص RBAC سلبي بجلسة حقيقية) · سجّل بند كسر جديد من الواجهة على أمر P5 (`cmrjf96860001mo3l80xgtrbp`) — **وصفه العربي مخزَّن سليمًا** (دليل BL-84: العطب القديم `????` إدخالٌ لا تخزين).
    - جولة `dev-review-a` (REVIEW): فتح تحقيق `cmrjfda64000dmo3lg1nzy5qm` على البند الجديد → شاشته عرضت **مقاسات حقيقية (300×250 + ملاحظة) · رسمة `p1.pdf` (و`RELEASED_TO_FACTORY` مستبعدة) · اعتماد TEC Approver بتاريخه** · تأكيدات فارغة بصدق (الأمر سابق لبوابة D-09). وشاشة تحقيق السوشيال `cmrjdstmw` عرضت **التأكيدات الثلاث الحقيقية** (طلب العميل/المعاينات/الرسم الهندسي — الفاعل والوقت) · مقاسات فارغة بصدق (سوشيال بلا معاينة).
    - `evidenceNotes`: كُتبت وحُفظت بنقرة حقيقية → **قُرئت بعد reload كامل** + صف القاعدة + ActivityLog بفاعل `dev-review-a` (127 حرفًا عربيًا سليمًا).
- **PHASE 3 — الحُكم ✅ (2026-07-13):** `judgeFaultInvestigation` — `requireRole(["ADMIN"])` (D-25) · `verdictFault`+`verdictNotes` (إلزامي، مزدوج: zod `.trim().min(1)` + فحص الخدمة) + `verdictById`+`verdictAt` + `status=JUDGED` · **`claimedFault` لا يُكتب في مسار الحكم إطلاقًا (D-27)** · قفل `evidenceNotes` بعد JUDGED server-side (D-30، `errors.investigationLocked`) · لا حكم ثانٍ (OPEN فقط + النموذج يختفي بعد الحكم) · إشعارات: REVIEW+INSTALLATIONS دائمًا + TECHNICAL_OFFICE لو الحكم TEC_ERROR/MEASUREMENT_ERROR · ActivityLog `INVESTIGATION_JUDGED` · قسم "تحقيقات بانتظار الحكم" في `/approvals` (مدخل للتفاصيل، لا بوابة ثانية — نمط BL-74). نموذج الحكم ظاهر لمن يرى الشاشة مع تنويه "للإدارة حصريًا" والحارس server-side (نمط BL-32: سبب صريح لا إخفاء).
  - ✅ **مُثبَت بجلسة واجهة (طريقة BL-82 · STD-11 قبل كل جولة — أمسكت جلستين خاطئتين قبل النقر):**
    - REVIEW (`dev-review-a`) ملأت نموذج الحكم ونقرت فعليًا → **"غير مصرح"** والقاعدة بلا تغيير (OPEN، لا حكم).
    - ADMIN (`dev-scr016`) حكم بلا تعليل → **"تعليل الحكم إلزامي"** والقاعدة بلا تغيير. ثم حكم `TEC_ERROR` على ادعاء `BREAKAGE` (تحقيق P5 `cmrjfda64`) → **`claimedFault=BREAKAGE` باقٍ** · الحكم بكل حقوله · `INVESTIGATION_JUDGED` مسجَّل.
    - الإشعارات وصلت فعلًا: **6 صفوف** — REVIEW (1) + INSTALLATIONS (2) + TECHNICAL_OFFICE (3، لأن الحكم TEC_ERROR).
    - قفل D-30: تعديل `evidenceNotes` بعد الحكم بنقرة حقيقية → **"التحقيق محكوم — ملاحظات الأثر مقفلة"**، الملاحظات كما هي وسجل الحفظ ظل واحدًا.
    - `/approvals`: التحقيق ظهر قبل الحكم (لقطة بأربعة تحقيقات) واختفى بعده (بقي 3).
    - توثيق: التحقيق الرابع `cmrjfklxr` (FACTORY_ERROR) فتحه `dev-review-a` 16:22 بتفاعل يوسف — دليل إضافي لخريطة BL-79 (MFG_ERROR→FACTORY_ERROR).
  - 🧹 ملاحظة واجهة (غير حاجبة): trigger الـSelect في نموذج الحكم يعرض القيمة الخام (`TEC_ERROR`) بدل التسمية المترجمة بعد الاختيار — يُصلح في جلسة تنظيف (نفس عائلة ملاحظة `factorMinimum`). → BL-87
- **PHASE 3.5 — سد ثقب الاعتماد (BL-88/STD-15) ✅ (2026-07-13):** حارس `reviewStatus === "APPROVED"` server-side في **أربعة مواضع كتابة** (المجموعة الكاملة في الريبو: contract.create واحد، payment.create واحد، manufacturingOrder.create اثنان):
  - `createContractCore` (`src/lib/services/contract-core.ts:47`) — يغطي `createContract` (مشروعات، بيد المبيعات) مركزيًا + العقد الآلي للسوشيال في `addPayment`.
  - `addPayment` (`lib/accounting/actions.ts:223`) — قبل الـ`$transaction` (لا عمل جزئي)، لكل الدفعات لا فرع السوشيال وحده.
  - `createManufacturingOrder` (`lib/manufacturing/actions.ts:75`) + `createReplacementOrder` (`fault-investigations.ts:240`) — دفاع عمق.
  - مفتاح خطأ **خاص** `errors.reviewNotApproved` (ar+en) — **لا يُعاد استخدام** `errors.quotationNotApproved` القائم لأنه مفهوم مختلف (`projects/actions.ts:170` يبوّب على `Quotation.status` لا `reviewStatus`؛ إعادة الاستخدام كانت ستنتج مفتاح JSON مكرَّرًا يدهس رسالة المشروعات — التقطه تدقيق ما بعد التعديل قبل الـcommit).
  - 🔎 **مسار مشروع للدفع قبل الاعتماد؟ لا** (تحقّق عدائي بوكيل مستقل + قراءتي): المُدخل الوحيد للدفع `getAccountingDashboard` مُرشَّح `reviewStatus:"APPROVED"`، والدفعة المقدمة (ACC-R02) = أول قسط **بعد** الاعتماد والتعاقد لا قبله. الاعتماد route-agnostic (`approveQuotationAction`، TEC_APPROVER/ADMIN) — السوشيال والمشروعات يمرّان به، فالحارس لا يكسر السوشيال.
  - ✅ **الدليل (استجابات خام، الأكشن/الخدمة مباشرة لا الشاشة — STD-15):** `createContractCore` على عرض `PENDING_REVIEW` → `errors.reviewNotApproved` (صفر كتابة) · `createReplacementOrder` على تحقيق لعرض `PENDING_REVIEW` → throw `errors.reviewNotApproved` (صفر كتابة) · `createContractCore` على عرض `APPROVED` (داخل transaction مُلغاة) → **PASS** (صفر أثر باقٍ). build GREEN.
  - ⚠️ **حدّ الدليل الصريح:** `addPayment` و`createManufacturingOrder` حارساهما **غير قابلين للتشغيل المستقل من الواجهة على عرض غير معتمد** — لأن كل مُدخلات الواجهة تُرشِّح/تعيد التوجيه للعروض غير المعتمدة (`contract/page.tsx:25` redirect · شاشة الحسابات filter). هذا **جوهر STD-15**: الترشيح لم يكن حارسًا، والآن الخادم هو الحارس. حراسهما نمط مطابق للمُثبَت خدميًا + build-green، ومسار **PASS** لهما يُختبر حيًّا في PHASE 4 (الدورة الصحيحة الكاملة).
- **PHASE 4 — الرسمة المصحَّحة + البديل ✅ (مُغلق، commit 8e72113):**
  - SUPERSEDED (BL-78): داخل `approveDrawingAction` (G1) — عند اعتماد رسمة، كل رسمة `TEC_APPROVED` أخرى على نفس الطلب → `SUPERSEDED` (transaction + ActivityLog). رسمة نافذة واحدة.
  - `createReplacementOrder(investigationId)` — `requireRole(["TEC_APPROVER","ADMIN"])` (D-29): يتطلب تحقيقًا `JUDGED` بلا بديل سابق (`replacementOrderId` null + `@unique`) · **نفس حراس الأمر الأصلي حرفيًا** (رسمة TEC_APPROVED + عقد + دفعة≥1 — لا استثناء D-29) · يُنشئ `parentOrderId=الأصلي · faultType=verdictFault · UNDER_REVIEW` مباشرة (D-16) + يكتب `replacementOrderId` على التحقيق في transaction واحدة + إشعار REVIEW.
  - نقطة الدخول (الدرس 2): زر "إصدار أمر البديل" في شاشة التحقيق (JUDGED بلا بديل) — **أُضيف TEC_APPROVER لعرض شاشتي `/investigations`** (قراءة + زر الإصدار فقط؛ الفتح REVIEW/ADMIN والحكم ADMIN كما هما) + بند القائمة.
  - ⚠️ **مكتشَف مسبقًا (فحص قراءة):** عرض P5 عليه عقد و**صفر دفعات** — حارس D-10 سيحجب البديل. **ليس كسر تصميم بل بيانات اختبار سابقة لبوابة الدفع.** خطة الدليل: إثبات الحجب أولًا (الحارس حي) → تسجيل دفعة حقيقية من واجهة ACCOUNTING → المرور. لا التفاف، لا كتابة مباشرة.
  - ❓ الأمر الأصلي بعد البديل: لا كود يلمسه — يُثبَت بالرصد قبل/بعد (حالته الآن READY).
  - ⚠️ ملحوظة أثناء البناء: خطأ ESLint "circular structure" يظهر في `next build` (لا يكسر الـbuild) — **سابق لهذا التعديل**، يُقيَّد للتنظيف لاحقًا.
- 🔴 **حارس W-06 (البوابة 3):** `REPLACEMENT_ELIGIBLE_FAULTS` مشتق من `CLAIM_MAP` — حكم `CUSTOMER_DELAY` يُرفض (`errors.faultNotEligibleForReplacement`). **مُثبَت تشغيليًا:** CUSTOMER_DELAY→رفض · BREAKAGE→عبر البوابة 3 (توقف عند البوابة 5) · صفر أثر (14→14 أمر، 0→0 سجل).

## المكتب الفني — البنود المتبقية (بعد إغلاق TO-01 → TO-31)

### مفتوح
| # | البند | النوع | ملاحظات |
|---|---|---|---|
| TO-27 | تجميل شاشة عرض السعر | واجهة | تفتح بـ6 أقسام منتجات فاضية ⇒ ملخص بـ6 أصفار، وسبب لبس «بيانات غير صالحة». المطلوب: قسم واحد + «أضف منتج» · تجاهل الأقسام الفاضية عند الحفظ · طيّ القسم المكتمل |
| TO-06 | طباعة ملف المشروع المجمّعة | ميزة | العرض المعتمد (`reviewStatus=APPROVED`) + رسوماته المعتمدة (`TEC_APPROVED`) + مقاساته في مستند واحد. قرار: العرض المعتمد النهائي فقط؛ لو أكثر من واحد يُؤخذ الأحدث `approvedAt` ورقمه في الترويسة. الفرق المبني حاليًا بين مسار السوشيال والمشروعات = مربع الرسمة وخانة جهة التصنيع فقط |
| TO-08 | داشبورد المكتب الفني | ميزة | **الهدف النهائي.** صار قابلًا للبناء بصدق: `costSnapshot` للربح · `submittedAt`+`leadDecidedAt` لزمن التسعير لكل مهندس · قواعد رؤية مستقرة لكل دور. `requireRole` على الأكشن التجميعي لا الصفحة · كل الأزمنة derived-on-read (لا scheduler في المشروع) |
| TO-46 | تابات «الرسوم والوثائق» بلا عدّاد والافتراضي ثابت | واجهة | تابات «الرسوم والوثائق» بلا عدّاد، والافتراضي `DRAWINGS` ثابت حتى لو فارغ — `TEC-PRJ-B1` يفتح على تاب فارغ وله **5 رسومات في تصنيف آخر**، فيُقرأ كـ«لا رسومات». **الحل:** عدّاد بجوار كل تاب + الافتراضي = أول تصنيف غير فارغ. **تغيير عرض بحت، بلا خدمة ولا استعلام** (الصفوف محمَّلة سلفًا في `drawings`). 🔴 **ليس انحدارًا** — رُصد كـ«تابات ميتة» وثبت العكس بالدليل: `onClick={() => setActiveCategory(cat)}` قائم (`tec-detail-client.tsx:512`)، وموجة B1 على هذا الملف `68` إضافة و**`0` حذف**، وكل الـhandlers الأربعة عشر متطابقة حرفيًا مع ما قبلها. السبب **بيانات**: 3 من 5 تصنيفات صفر صفوف في القاعدة كلها، و9 من 10 طلبات كل رسوماتها في `DRAWINGS` ⇒ التابات الأربعة الأخرى تعرض نفس رسالة الفراغ فتُقرأ كأنها لا تستجيب |
### TO-32 — شاشة تفاصيل عرض السعر (مفتوح)
1. 🔴 **زر «تعديل» مخفي عن المكتب الفني والتيم ليدر.**
   `quotation-detail.tsx:96` → `canEdit = [ADMIN, SALES_MANAGER, SALES_REP]`
   بينما `updateQuotation` محروس بـ`PRICING_ROLES` التي تضمّ TECHNICAL_OFFICE
   و TEC_LEAD — وTO-30 أضاف حارس مسار على نفس الدالة، أي أن التصميم يفترض
   استعمالهما لها. ⇒ **صلاحية بلا طريق (الحالة الخامسة في هذه الجلسة).**
   الإصلاح: ربط الواجهة بحارس السيرفر **بمصدر واحد** كما فُعل في
   `quotation-roles.ts` بـTO-31 — لا شرط ثانٍ.
2. **جدول البنود يعرض عمودين فقط** (المنتج · الإجمالي) بينما قالب الطباعة
   يعرض الكمية وسعر الوحدة والوصف. عرض ناقص لا بيانات ناقصة.
3. **ضبط محاذاة RTL** عبر الموديول — يشمل **BL-155** (خصائص CSS فيزيائية
   `ml-/mr-/pl-/pr-` في `users-client.tsx`).

**درس متكرر يستحق التسجيل:** تكرّرت خمس مرات في هذه الجلسة حالةُ «الصلاحية
مبنية والواجهة تخفيها» (TO-09 · TO-19 · TO-23-C · TO-26 · TO-32). القاعدة
المستخلَصة: **كل سحب أو منح صلاحية يقابله فحصٌ صريح لواجهته، ومصدر واحد
للشرط لا شرطان.**

### TO-33 🔴 — شاشة تعديل العرض تتجاوز محرك التسعير (تصعيد BL-151)
`/quotations/[id]/edit` تعرض حقولًا نصية فقط (وصف · كمية · إجمالي) — بلا نوع
منتج ولا مقاسات ولا معامل ولا خامات. كل تعديل يعيد بناء البنود من أرقام يدوية
⇒ **`costSnapshot` يُمسح إلى null**، فتُستبعد تلك العروض من حساب الهامش.

**لماذا صعد الآن:** كان الزر مخفيًا عن المكتب الفني فنادرًا ما يُستعمل؛ بعد
TO-32 صار ظاهرًا لهم ⇒ التآكل يصير روتينيًا.

**الأثر على TO-08:** مؤشر الربح سيفقد كل عرض عُدِّل مرة واحدة.

**خيارات (قرار مطلوب):**
(أ) شاشة التعديل تستعمل نفس محرك التسعير كشاشة الإنشاء — الأصح والأكبر.
(ب) الاحتفاظ بـ costSnapshot عند التعديل ما دام البند لم يتغيّر — أرخص وجزئي.
(ج) منع التعديل بعد الاعتماد المبدئي، وإجبار «عرض جديد» — أبسط وأقرب لدورة العمل.

**الأولوية: قبل TO-08 (الداشبورد)** — وإلا نبني مؤشرًا فوق بيانات تتآكل.

### ملاحظات مسجَّلة (بلا تنفيذ)
- **BL-153** 🟠 لو تغيّرت الخامة بين لحظة الحساب ولحظة الحفظ، تُحسب التكلفة من أسطر أقل ⇒ **هامش مبالغ فيه برقم يبدو سليمًا**. قرار مطلوب: `null` أم تحذير؟
- **BL-155** خصائص CSS فيزيائية في `users-client.tsx` — خرق RTL.
- **BL-151** `updateQuotation` يفقد `costSnapshot` (يُعاد بناء البنود بلا سياق وصفة). السلوك صحيح (`null` أصدق من رقم لا يخصّ السعر الجديد) لكنه فجوة معلَنة.
- **BL-152** `unitPrice` يصل من العميل بلا إعادة اشتقاق سيرفر-سايد.
- **BL-20** تنظيف حالات الرسمة الميتة — **يحتاج ترحيل 6 صفوف حية** لا مجرد إسقاط قيم enum.
- **TO-16** `Error: ENVIRONMENT_FALLBACK` في `(print)/statements/[id]` — غير متعقَّب.
- **سؤال معلَّق للمبيعات:** هل `SALES_REP` يجب أن يعلّم العرض «مُرسل»؟ الأداة كانت ظاهرة له والسيرفر يرفضها (زر مكسور سابق)، وأُخفيت في TO-31. القرار في نطاق المبيعات.

### بنود أمنية للنشر (إلزامية قبل الإنتاج)
1. `AUTH_SECRET` قيمة تطويرية معروفة ⇒ `openssl rand -base64 32`.
2. كلمة مرور القاعدة صريحة في `docker-compose.prod.yml` وفي تاريخ git ⇒ نقل + **تدوير** (القديمة محروقة).
3. `NEXTAUTH_URL` ⇒ `https://` عند النشر، وإلا تُكسر الجلسات (اسم الكوكي يتغيّر).
4. `TO-10` تصريح على مستوى السجل لجلب الملفات — الحماية اليوم «مُصادَق فقط».
5. أي reverse proxy مستقبلي: التأكد أن `/uploads` لا يُخدَم ساكنًا حوله.