# BACKLOG — السجل الوحيد للبنود المفتوحة

> أي متطلب/فجوة/قرار **يولد في محادثة** يُقيَّد هنا **فورًا**.
> الفجوات ماضاعتش لنقص أدوار — ضاعت لأن **مفيش ليستة**.
> ملفات التحليل = المتطلبات **الأصلية**. هذا الملف = المتطلبات **المكتشَفة**.
> أي تدقيق قادم يقرأ **الاثنين معًا**.

---

## ⛔ قاعدتان لا تُخترقان

### 1. قاعدة الإغلاق
**الـ migration ليست دليل إغلاق. الكود ليس دليل إغلاق.**
بند يُغلق **فقط** بـ: البوابة **مُفعَّلة** فعليًا (لا NULL) **+** الفرع **الشغّال** مُختبَر بجلسة حقيقية.

> اكتشفنا دورين ناقصين → عملنالهم عمودين → سبناهم NULL → **الدورين فضلوا ناقصين.**

### 2. قاعدة منع الاختراع (STD-05)
**الجهل يُكتب، لا يُصمَّم.**

- أي فجوة معلومة → **تُقيَّد هنا كسؤال مفتوح**. **ممنوع** تتحول لـ `nullable` / `config` / `enum` / `flag`.
- أي مصطلح من لغة الشركة **ممنوع يدخل تصميمًا قبل تعريفه**. (كلمة "المدير التنفيذي" غير المعرَّفة أنتجت بوابة كاملة وهمية.)
- **ممنوع إعادة تفسير حقل قائم** لسد فجوة. الحقل يعني ما يعنيه في الكود — لا ما نتمناه.
- **اختبار الاختراع:** حقل/بوابة مقترَحة **ولا يمكن الإشارة إلى سطر في مستند أو قول صريح من صاحب العمل** → **اختراع. قف واسأل.**
- "القيادة عندك" = رتِّب واحسم بين بدائل عليها دليل. **ليست إذنًا بملء الفراغ.**

---

## 📌 كتلة إلزامية في كل برومبت وكيل

```
BACKLOG DISCIPLINE (إلزامي):
1. اقرأ BACKLOG.md قبل البدء — قسم "قرارات محسومة" مرجع مُلزِم، لا اجتهادك.
2. أي متطلب/فجوة/تناقض يظهر أثناء العمل → قيّده في BACKLOG.md فورًا "مفتوح"
   (حتى لو خارج نطاق مهمتك — خصوصًا لو خارجها).
3. ممنوع الاختراع. لو احتجت قرارًا غير موجود في "قرارات محسومة" → قف واسأل.
   ممنوع تحويل الجهل إلى nullable/config/flag، وممنوع إعادة تفسير حقل قائم.
4. ممنوع "تم" بلا: بوابة مُفعَّلة + الفرع الشغّال مُختبَر.
5. عند الإغلاق: حدّث BACKLOG.md بالدليل (ملف/سطر). CLAUDE.md/AGENTS.md → يوسف فقط.
```

---

# ✅ قرارات محسومة — المرجع المُلزِم
### (صاحب العمل · 2026-07-12)

## السلسلة الحقيقية

```
1. INSPECTION_MANAGER: مقاسات + صور الموقع + رسم مقاسات — وبس.
   ❗ لا بوابة اعتماد. لا تحقّق على رسمة المكتب الفني.
2. مهندس المكتب الفني (TECHNICAL_OFFICE): رسم + تسعير
3. مدير المكتب الهندسي — TEC_APPROVER
      · يعتمد الرسم · يعتمد التسعير · يُصدر أمر التصنيع
4. عرض السعر → العميل → موافقة → التزام تعاقدي
5. REVIEW — قسم الجودة والمراجعة الفنية
      · مطابقة 3: طلب العميل + مقاسات/رسم المعاينات + رسم المكتب الهندسي
      · يمنع تضارب رسومات الإدارات
      · ❗ هو الذي يعتمد أمر التصنيع → بعده يتحوّل لـ PROCUREMENT
6. PROCUREMENT: فحص المخزون → تجهيز / طلب تصنيع من المصنع
7. تصنيع → استلام → تخزين → تسجيل التكاليف
8. تركيب → بنود إضافية → صور وتقرير → متابعة رضا العميل
```

## خريطة الأدوار الملزِمة

| الدور | الحقيقة |
|---|---|
| `ADMIN` (عمرو) | فواتير · خصومات · سياسات · كل الصلاحيات. **خارج سلسلة اعتماد الرسومات تمامًا.** |
| `TEC_APPROVER` | **المدير التنفيذي = مدير المكتب الهندسي.** يعتمد الرسم + التسعير + **يُصدر أمر التصنيع**. |
| `REVIEW` (محمد حسام) | **رقابة وجودة فقط.** يطابق الـ3 و**يعتمد أمر التصنيع**. **لا علاقة له بالتسعير ولا عروض الأسعار.** |
| `INSPECTION_MANAGER` (حسن بهاء) | مقاسات + صور + رسم مقاسات. **بلا بوابة اعتماد.** |
| `PROCUREMENT` (شكري) | ينفّذ الأمر **بعد** اعتماد REVIEW: مخزون · مصنع · تكاليف. |

## تصحيحات جوهرية

| # | التصحيح |
|---|---|
| **D-01** | **"المدير التنفيذي" ≠ ADMIN (الرئيس التنفيذي).** في لغة الشركة = **مدير المكتب الهندسي** (TEC_APPROVER). |
| **D-02** | **ADMIN خارج سلسلة اعتماد الرسومات** — بوابة CEO كانت **اختراعًا**. |
| **D-03** | **REVIEW لا علاقة له بالتسعير ولا عروض الأسعار.** |
| **D-04** | **REVIEW يعتمد أمر التصنيع** — بعد إصداره من المكتب الهندسي (TEC_APPROVER)، وقبل تحويله لـ PROCUREMENT. |
| **D-05** | **INSPECTION_MANAGER بلا بوابة اعتماد** — مقاسات وصور فقط. |
| **D-06** | **التسعير مساران:** (A) داخل القائمة → Price List · (B) **خارج القائمة → طلب تسعير من المصنع** (والإكسسوارات نفس المعاملة). |
| **D-07** | **النظام يتتبع رصيد الكميات** (هدف). **مرحليًا:** تكاليف على المشاريع فقط. |
| **D-08** | **مسار السوشيال: عرض السعر بعد موافقة العميل = العقد.** لا سجل `Contract` منفصل. |
| **D-09** | **النظام لا يقارن الرسومات آليًا** — المقارنة **دور REVIEW الفعلي**. النظام: يعرض الـ3 · يُجبر التأكيد الصريح · يسجّل الأثر · يحجب الاعتماد قبل اكتمال التأكيدات. |
| **D-10** | **الدفع شرط قبل التصنيع — في المسارين.** النسبة تختلف حسب الطلب. **لا حد أدنى للمقدم:** خطة الدفعات حرة لكل عقد. |
| **D-11** | **الضابط = فصل الأدوار + الأثر + الرؤية، لا رقم.** المندوب يتفاوض · الحسابات تسجّل. |
| **D-12** | **اختيار المصنع + التاريخ المتوقع = PROCUREMENT، لا REVIEW.** REVIEW يطابق ويعتمد فقط. (يحسم BL-33) |
| **D-13** | **موافقة عميل السوشيال = تحويل الدفعة نفسها.** لا حقل موافقة منفصل. المبيعات تتلقى صورة التحويل (واتساب)، لكن **الفعل النظامي المُلزِم = تسجيل الحسابات للدفعة.** |
| **D-14** | **عقد السوشيال يُنشأ تلقائيًا عند تسجيل الحسابات لأول دفعة على العرض.** المبيعات لا تُنشئه. |
| **D-24** | **إلغاء الفاتورة: ACCOUNTING تلغي — بسبب إلزامي مكتوب + إشعار فوري لـ ADMIN.** الضابط = الأثر والرؤية لا المنع (D-11). الإلغاء لا يُرسل شيئًا للعميل — يسحب. منعه يولّد مسارًا موازيًا خارج النظام، وذلك أخطر من الغلط نفسه. |
| **D-23** | **الخصم له مدخلان مشروعان، وكلاهما يمر بنفس السلسلة (D-19):** • عند إنشاء العرض (خصم قائم/موسمي). • على عرض قائم مُرسَل — العميل يعترض فيطلب SALES_REP خصمًا (هذا المدخل غير موصول بأي واجهة — `requestDiscountAction` يتيم). |
| **D-22** | **خريطة الاعتماد (مقفلة):** • `TEC_APPROVER` (المدير التنفيذي — موظف): الرسم · التسعير · إصدار أمر التصنيع. • `REVIEW` (الجودة): المطابقة الثلاثية · اعتماد أمر التصنيع. • `ADMIN` (CEO): أي خصم · الفاتورة قبل إصدارها للعميل. "اعتماد العرض النهائي" = اعتماد الخصم عليه، ليست بوابة منفصلة. |
| **D-21** | **ACCOUNTING تجهّز الفاتورة (مسودة). ADMIN وحده يصدرها — الإصدار هو الاعتماد.** بعده: الرقم يتجمّد وتذهب للعميل. |
| **D-20** | **SALES_MANAGER لا يعتمد خصمًا طلبه هو** (نفس مبدأ منع اعتماد الذات في الرسومات). قد يشغل الشخص دورين. |
| **D-19** | **سلسلة الخصم:** • SALES_REP يطلب نسبة — لا يفرضها. لا خصم بلا سجل. • SALES_MANAGER يعتمد ويمرّر — لا يرفض لتجاوز سقفه، ولا ينهي السلسلة. • **ADMIN يقرر النهائي على كل خصم — أي نسبة، في المسارين.** • `discountBasePct` (18) = **سقف تفاوض SALES_MANAGER**؛ فوقه → الطلب موسوم "طلب خصم استثنائي". • `discountMaxReqPct` (25) = **سقف صلب مطلق**: لا يُنشأ عرض بخصم أعلى؛ **يبقى كما هو** (`pricing/actions.ts:261`). • بعد قرار ADMIN → إشعار SALES_REP ليبلّغ العميل. |
| **D-18** | **مسار البديل: لا أمر تصنيع تلقائي — في كل الحالات الأربع.** INSTALLATIONS (المدير) يسجّل الطلب + السبب → يبلّغ REVIEW → REVIEW يفتح تحقيقًا (السبب + المتسبب: فني/مصنع) → بعد تعديل الغلط، REVIEW يطلب من PROCUREMENT تنفيذ أمر تصنيع جديد. |
| **D-17** | **الجدولة (SCHEDULED للمعاينة/التركيب) = صنف ثالث: عمل حقيقي بتنسيق خارجي.** تبقى يدوية. **D-16 لا يشملها** (ليست اعتمادًا ولا واقعة مادية مُثبَتة). |
| **D-16** | **الاعتماد ينقل تلقائيًا.** زر الاعتماد = انتقال فوري للمرحلة التالية + إشعار صاحب الدور التالي. **لا خطوة "أرسل للتالي" يدوية بعد أي اعتماد.** الاستثناء: **تسجيل واقعة مادية** (وصل من المصنع · تم التركيب) يبقى يدويًا — إثبات حدث في الواقع، لا قرار. أتمتته = بيانات كاذبة. |
| **D-15** | **فرق تنظيم ذاتي: كل إدارة تملك فعلها ولا تنوب عن غيرها.** المندوب لا يُنشئ عقدًا بلا مال — شغل الحسابات. **ADMIN لا يُستثنى من قواعد سلامة البيانات:** الاستثناء يخصّ القرار الإداري، لا صحة السجل. عقد سوشيال بلا دفعة = مستحقات كاذبة، لا صلاحية. |

---

# 🚨 P0 — أمني

| # | البند | الدليل | دليل الإغلاق |
|---|---|---|---|
| **BL-47** | 🔴 **لا مسار break-glass للأدمن.** المستخدم العادي ينساها → ADMIN يعيد ضبطها من `/users`. لكن **ADMIN نفسه = طريق مسدود** (لا استرجاع، لا seed لـ `admin@egyglass.net`). أثبتته الواقعة. **شدّته معلّقة على BL-42:** لو شاشة المستخدمين مكسورة → لا استرجاع لأحد. **جزئيًا مُخفَّف بـ BL-48 (سكريبت break-glass).** | يوسف |
| **BL-01** | ✅ **مُغلق (PHASE 2، 2026-07-12).** حارس `requireRole(["TEC_APPROVER","ADMIN"])` مُضاف — `lib/manufacturing/actions.ts:45`. مُثبت: SALES_REP → `notAuthorized` بجلسة حقيقية. | مُغلق |

---

# 🔴 سلسلة الاعتماد — أدوار خاطئة وبوابات مخترَعة

> **السلسلة مش مكسورة — مسنودة لأدوار غلط. الإصلاح حذف وإعادة إسناد، مش إعادة بناء.**

| # | البند | الحالة في الكود | المطلوب |
|---|---|---|---|
| **BL-02** | ✅ **مُغلق (PHASE 1).** G3 مُعطَّلة — `ceoApproveDrawingAction` يرجع `gateRemoved` (`technical-office/actions.ts`) + حقلا العتبة أُزيلا من شاشة الإعدادات وzod. العمود/الـenum ميتان حتى حذف SCR (BL-20). | مُغلق |
| **BL-03** | ✅ **مُغلق (PHASE 1).** G2 مُعطَّلة — `verifyDrawingAction` يرجع `gateRemoved` + أزرار UI أُزيلت. مُثبت: الرسمة تقف عند `TEC_APPROVED`، INSPECTION_MANAGER بلا بوابة. | مُغلق |
| **BL-04** | ✅ **قائمة وصحيحة + تُوسّعت (PHASE 2).** G1 `TEC_APPROVER`، وشمل اعتماد التسعير عبر `/review` (نُقل إليه). | مُغلق |
| **BL-05** | ✅ **مُغلق (PHASE 2).** اعتماد العرض → `QUOTATION_APPROVAL_ROLES=["TEC_APPROVER","ADMIN"]` (`lib/review/actions.ts:10`) + نقل شاشة `/review`. مُثبت: REVIEW → `notAuthorized`، TEC_APPROVER → success. | مُغلق |
| **BL-06** | ✅ **مُغلق (PHASE 2).** التوليد التلقائي حُذف من `approveQuotationAction` (`lib/review/actions.ts:143`). مُثبت: الاعتماد `mo_count=0`. | مُغلق |
| **BL-07** | ✅ **مُغلق (PHASE 3).** البوابة → `["REVIEW","ADMIN"]` + عرض الأضلاع الثلاثة + 3 تأكيدات صريحة + حجب server-side (`errors.matchIncomplete`) + ActivityLog لكل تأكيد. `mfg-review.ts` + `manufacturing/[id]/review-panel.tsx`. مُثبت بالجلسات (D-09). | مُغلق |
| **BL-08** | ✅ **مُغلق (PHASE 2).** `createManufacturingOrder` يرفض بلا رسمة `TEC_APPROVED` على الطلب (`errors.noApprovedDrawing`) — `manufacturing/actions.ts:45`. مُثبت بجلسة. | مُغلق |
| **BL-09** | **`NULL` = fail-open في كل إعداد حوكمي** | نمط عام | القاعدة: `NULL` = **الضابط مُفعَّل**، لا مُتخطّى (البوابتان الحوكميتان NULL أُزيلتا في PHASE 1؛ المبدأ قائم لأي إعداد قادم) |

---

# 🟠 مسارات عمل ناقصة

| # | البند | المصدر |
|---|---|---|
| **BL-57** | **عند نزول موديول المخازن (BL-21): "تجهيز من المخزون" = مسار مشروع بلا مصنع.** بوابة READY تتوسّع إلى: مصنع مُعيَّن **أو** تجهيز من المخزون. اليوم لا مخزون → المصنع إلزامي. | مرحلة 2 |
| **BL-58** | ❓ **اسم `IN_PRODUCTION` لم يعد دقيقًا** — يعني الآن "معتمد بانتظار توجيه PROCUREMENT". إعادة التسمية = تغيير enum = SCR. مؤجل. | يوسف (SCR) |
| **BL-55** | 🟠 **مسار إشعار التحويل غير مبني** — SALES ترفع صورة التحويل (واتساب) على ملف العميل + إشعار للحسابات. **ليس بلوكرًا:** ACCOUNTING تسجّل الدفعة والصورة تصل خارج النظام (D-13). دفعة منفصلة. | يوسف |
| **BL-65** | ✅ **مُغلق (2026-07-12، قرار يوسف: أي خصم>0=طلب).** `createQuotation`: `discountNeedsApproval = discountPct.gt(0)` (بدل `> base`) — **أي خصم يفتح DiscountRequest، صفر تطبيق مباشر**. الوسم "استثنائي" فوق `discountBasePct`، عادي تحته. السقف الصلب (25) باقٍ. مسار اليتيم `discount.ts` أُزيل سابقًا. مُثبت بالجلسات (10%/20%/0%). | مُغلق |
| **BL-66** | 🔴 **SALES_MANAGER يعتمد خصمًا طلبه هو** — لا assertNotSelf (`discount.ts`). يخالف D-20. | قيد التنفيذ (PHASE A) |
| **BL-67** | 🔴 **صفر إشعار بعد قرار الخصم** — SALES_REP لا يعلم بالنتيجة (`discount.ts:256`). | قيد التنفيذ (PHASE A) |
| **BL-68** | ❓ **`managerApprovalCeilingPct` عمود ميت** (صفر منطق يقرأه). اسمه يطابق "سقف المدير" لكن الوظيفة يحملها `discountBasePct` بمعنى مقلوب. يُوحَّد أو يُحذف — SCR. | يوسف (SCR) |
| **BL-71** | ✅ **مُغلق (PHASE C، D-23).** زر "طلب خصم" في شاشة تفاصيل العرض (`quotation-detail.tsx` — SALES_REP/SALES_MANAGER/ADMIN، على DRAFT/SENT بلا طلب معلّق) يستدعي `requestDiscountAction` القائم بلا منطق جديد. مُثبت: 15% → طلب+SALES_MANAGER، السلسلة كاملة حتى إشعار SALES_REP، 30% مرفوض، طلب ثانٍ مرفوض. | مُغلق |
| **BL-73** | ✅ **مُغلق (PHASE D، D-24).** `cancelInvoiceAction` (ACCOUNTING/ADMIN): سبب إلزامي server-side (`cancelReasonRequired`) + إشعار فوري لـ ADMIN (رقم+مبلغ+سبب) + `INVOICE_CANCELLED` + CANCELLED لا محذوفة. مُثبت. **رصد (recon): لا FK بين `Invoice` و`Payment`** (`Payment.invoiceId` غير موجود — يرتبط بالعرض) → **إلغاء الفاتورة لا ييتّم أي دفعة**. الاسترجاع مسار منفصل (BL-75). | مُغلق |
| **BL-75** | 🟠 **لا مسار استرجاع مبالغ.** `Payment` يسجّل الوارد فقط. أول استرجاع فعلي سيُسجَّل خارج النظام أو برقم خاطئ. صاحب العمل: "طبيعي يحصل استرجاع". دفعة منفصلة. | يوسف |
| **BL-77** | ❓ **لا رابط `Invoice` ↔ `Payment`.** الدفعة على العرض لا على الفاتورة. النظام لا يعرف أي فاتورة سُدِّدت. مقصود أم فجوة؟ | يوسف |
| **BL-76** | ❓ **حارس "طلب معلّق يمنع الثاني" لم يُثبَت مستقلًا** — حارس الحالة (`!DRAFT/SENT`) سبقه. النتيجة صحيحة، الآلية غير مؤكدة. يُتحقَّق لاحقًا. | يوسف |
| **BL-74** | ✅ **مُغلق (PHASE E).** شاشة `/approvals` لـ ADMIN فقط تجمع: طلبات الخصم PENDING (المُمرَّرة من SALES_MANAGER) + الفواتير DRAFT، بالأقدم أولًا + عمر الانتظار، صفر → "لا شيء بانتظارك". **تستدعي `decideDiscountAction`/`issueInvoiceAction` القائمين بنفس الحرّاس — مدخل لا مصدر حقيقة ثانٍ.** مُثبت: نفس أثر الشاشة الأصلية · غير ADMIN مرفوض (307) · العنصر يختفي بعد الاعتماد. | مُغلق |
| **BL-70** | ✅ **مُغلق (PHASE C).** لم يعد يتيمًا — `requestDiscountAction` موصول بزر "طلب خصم" (مدخل العرض القائم، D-23). المساران مشروعان الآن (D-23): createQuotation عند الإنشاء · requestDiscountAction على عرض قائم. كلاهما نفس السلسلة. | مُغلق |
| **BL-69** | 🔴 **`QuotationStatus` قائمة حرة بلا حارس تسلسل.** EXPIRED يمكن الرجوع منها. SALES_REP يرى القائمة والأكشن يرفضه (عدم تطابق UI/server). | يوسف |
| **BL-60** | 🔴 **[verified-in-code] مسار البديل (W-06) ينشئ أمر تصنيع مباشرة `IN_PRODUCTION` — يتخطّى بوابة REVIEW والحارس كله.** `installation-extras.ts:121` **كتابة مباشرة** `prisma.manufacturingOrder.create` (لا `createManufacturingOrder`) → يتخطّى: `requireRole(TEC_APPROVER)` · العقد · الدفعة · فحص الرسمة المعتمدة · UNDER_REVIEW/مطابقة REVIEW. يُطلقه **INSTALLATIONS** (قائد الفريق) بإضافة بند تركيب من نوع خطأ/كسر (`addInstallationItemAction`). يرث `factoryId` من الأصلي (قد يكون null). القاعدة: **3 أوامر بديلة، كلها IN_PRODUCTION**. البديل **أخطر حالة** (لازم يطابق الأصل وإلا خسارة خامات) ويتخطّى المطابقة. **✅ الباب مُقفَل (2026-07-12، D-18):** حُذف الإنشاء التلقائي؛ البند يُسجَّل + إشعار INSTALLATIONS/REVIEW + `REPLACEMENT_REQUESTED`. مسار البديل معطّل حتى بناء موديول التحقيق (BL-63). الأوامر الثلاثة القائمة: رُصدت، لم تُهاجَر. | مُغلق (الباب) |
| **BL-63** | 🔴 **موديول التحقيق غير مبني** — مسار البديل معطّل حتى بنائه (D-18: REVIEW يفتح تحقيقًا: سبب/متسبب فني أو مصنع → بعد التعديل يطلب من PROCUREMENT تنفيذ أمر جديد). **بلوكر UAT.** | يوسف |
| **BL-61** | ❓ **الفاتورة تصدر من ACCOUNTING بلا بوابة ADMIN** (`issueInvoice` — ACCOUNTING/ADMIN، لا اعتماد ADMIN منفصل) — وصاحب العمل يقول "ADMIN يعتمد الفواتير والخصم". الخصم له بوابة (`decideDiscount`)؛ الفاتورة لا. فجوة؟ | يوسف |
| **BL-62** | ❓ **`updateQuotationStatus` قائمة حالة حرة** (`pricing/actions.ts`) — `PRICING_ROLES` يضبط أي `QuotationStatus` بما فيها `APPROVED` بلا بوابة. هل `QuotationStatus` حاملة لأي قرار، أم عرض فقط؟ (منفصل عن `reviewStatus` الذي يحمل اعتماد التسعير.) | يوسف |
| **BL-59** | ✅ **مُغلق (PHASE B، D-16).** `createManufacturingOrder` يُنشئ `UNDER_REVIEW` مباشرة + إشعار REVIEW (`lib/manufacturing/actions.ts:94`). لا خطوة "أرسل للمراجعة" يدوية. `submitForReview` بقي لمسار REJECTED→إعادة الرفع. مُثبت بالجلسات. | مُغلق |
| **BL-64** | **[verified-in-code] `MfgStatus.PENDING` صارت حالة ميتة لأمر التصنيع** — بعد D-16 لا كاتب لها (كان `@default(PENDING)` عبر createManufacturingOrder فقط، والآن يحدد UNDER_REVIEW صراحةً). القرّاء (`submitForReview`, `executive`) يبقون. **حذف قيمة enum = SCR** (ممنوع schema هنا). لا تُحذف. | يوسف (SCR) |
| **BL-56** | 🔴 **لا نسخ احتياطي — لا للـ DB volume ولا لـ `egyglass_uploads`.** named volume ينجو من rebuild، **لا** من `docker volume rm` ولا فقد الـ host. **بلوكر UAT.** (يوسّع BL-40.) | يوسف (قبل UAT) |
| **BL-54** | 🟠 **`dev-scr016` = ADMIN ثانٍ نشط** — يُعطَّل مع كل حسابات dev قبل UAT (BL-38). | يوسف (قبل UAT) |
| **BL-49** | 🟠 **زر "حذف" في `/users` يُنفّذ تعطيلًا ناعمًا** (`deleteUser`: `deletedAt`+`isActive=false`). التسمية مضلِّلة — ADMIN لن يضغط "حذف" على موظف. **تُعاد التسمية إلى "تعطيل".** النموذج سليم. (دفعة تنظيف واجهة منفصلة.) | تنظيف UI |
| **BL-50** | 🟠 **حالة `isActive=false` + `deletedAt=null` بلا مسار واجهة.** إما تُمنع الحالة (توحيد التعطيل=حذف ناعم) أو يُضاف مسار استرداد لها. | تنظيف UI |
| **BL-51** | 🟠 **الشعار لا يظهر في كل الشاشات.** | تنظيف UI |
| **BL-52** | 🟠 **شاشة تفاصيل عرض السعر: قالب طباعة السوشيال** (بمساحة الرسم الهندسي) لا يظهر عند فتح عرض أفراد/سوشيال. يُتحقَّق: هل القالب مربوط بالمسار؟ | تنظيف UI |
| **BL-53** | 🟠 **لا زر رجوع في شاشة تفاصيل عرض السعر.** | تنظيف UI |
| **BL-10** | **تسعير خارج القائمة** — طلب تسعير من المصنع → التسعير على أساسه (+ الإكسسوارات) | D-06 |
| **BL-11** | **فحص توفر المخزون** قبل طلب التصنيع من المصنع | D-07 · الفلوشارت م2 |
| **BL-12** | **شاشة العينات** — طلب → المخازن تجهز وترسل → فيدباك للمبيعات | الفلوشارت م4 · SAL BRD-6 |
| **BL-13** | **طلب فحص (IR)** قبل التسليم النهائي — يبدأه المشروعات وبعده ينتهي دوره | الفلوشارت 4.5 · PRJ BRD-8 |
| **BL-14** | **تنبيهات التأخير** (قرب المدة / تجاوزها) لقسم المخازن | الفلوشارت م3 · PRC-R06 |
| **BL-15** | **`Drawing.REJECTED` قيمة enum ميتة** — لا يمكن رفض رسمة | تدقيق الكود |
| **BL-16** | ✅ **مُغلق (PHASE 2د).** `approvedById` يُكتب عند اعتماد العرض بيد `TEC_APPROVER` (`lib/review/actions.ts:105`) + المساران القديمان (`pricing/actions.ts:521`، `discount.ts:248`). مُثبت: الطباعة تُظهر "المدير التنفيذي — TEC Approver Test (DEV)" لا "—". | مُغلق |
| **BL-17** | **اعتماد تسعير المهندس من مديره** — "لو التسعير من مهندس، مدير المهندس يراجع ويعتمد" | الفلوشارت م3 |

---

# 🟡 قرارات معلّقة — **ممنوع الاختراع فيها**

| # | السؤال | القرار عند |
|---|---|---|
| **BL-18** | **تحقَّق (2026-07-12، grep كامل): لا يوجد أي حقل يسجّل موافقة العميل.** كتّاب `Quotation.status="APPROVED"` اثنان فقط وكلاهما داخلي: (1) `lib/pricing/actions.ts:501 updateQuotationStatus` — قائمة حالات عامة بيد `PRICING_ROLES` من شاشة تفاصيل العرض (`quotation-detail.tsx:115`)، (2) `src/lib/actions/discount.ts:247` — قرار خصم بيد SALES_MANAGER/ADMIN. تسمية APPROVED في الواجهة = "معتمد" (عامة). "موافقة العميل" تظهر في الكود **فقط** كعنوان مربع توقيع ورقي في قالب الطباعة (`(print)/quotations/[id]/print/page.tsx:376`) — ليست حقل بيانات. **الخلاصة: `APPROVED` = اعتماد داخلي، وموافقة عميل السوشيال غير مسجَّلة في النظام إطلاقًا. لم يُبنَ حارس التزام للسوشيال في إصلاح السلسلة (التزامًا بـ STD-05) — القرار عند يوسف: حقل جديد (SCR) أم آلية أخرى.** | **يوسف — مفتوح** |
| **BL-19** | **الفلوشارت قديم** — يضع المراجعة قبل إرسال عرض السعر، وصاحب العمل يقول بعد التعاقد. **يُحدَّث أو يُعلَّم "مرجع تاريخي".** | يوسف |
| **BL-20** | **مصير `RELEASED_TO_FACTORY` على الرسمة** — الإفراج صار خاصية **أمر التصنيع** بعد اعتماد REVIEW. تُحذف الحالة أم تُشتق؟ | يوسف (SCR) |
| **BL-32** | ✅ **مُغلق (2026-07-12، قرار يوسف: /technical-office/[id]).** زر "إصدار أمر تصنيع" في شاشة تفاصيل الطلب (بيت TEC_APPROVER) — `tec-detail-client.tsx` (TEC_APPROVER/ADMIN فقط، معطّل بسبب صريح: "الرسمة غير معتمدة"/"لا يوجد عقد"/"أمر التصنيع صادر بالفعل"). الحارس server-side كما هو. مُثبت بالجلسات (proofs 3/4/5 + حارس العقد). ~~[verified-in-code] createManufacturingOrder صار يتيمًا بلا مدخل~~ | مُغلق |
| **BL-33** | ✅ **مُنفَّذ ومُغلق (2026-07-12، D-12).** `approveOrder` (REVIEW) = مطابقة ثلاثية فقط، بلا مصنع/تاريخ → IN_PRODUCTION بـ factory/date=null. أكشن جديد `assignFactory` (PROCUREMENT) يعيّنهما (`mfg-review.ts` + `manufacturing/[id]/actions.ts`). **بوابة READY** (قرار ب): `updateMfgStatus` يرفض IN_PRODUCTION→READY بلا مصنع (`errors.factoryNotAssigned`). الحقول نُقلت في `review-panel.tsx` لـ PROCUREMENT. مُثبت بالجلسات: REVIEW يعتمد بلا مصنع✅ · READY بلا مصنع مرفوض✅ · REVIEW على assignFactory مرفوض✅ · PRC يعيّن✅ · READY بعده✅ · لا جمود. | مُغلق |
| **BL-31** | ✅ **محسوم بـ D-10 (2026-07-12).** الدفع شرط قبل التصنيع في المسارين، بنسبة متغيرة لكل طلب بلا حد أدنى. (كان ادعاء "100%" في commit `6f0908c` غير مؤكَّد — القرار النهائي: نسبة حرة لا رقم ثابت.) التنفيذ يحتاج BL-34 (Contract للسوشيال) + بوابة الدفع قبل التصنيع. | محسوم — بانتظار تنفيذ |
| **BL-34** | ✅ **مُحلّ app-layer (PHASE 1) — بلا schema.** السوشيال يحصل على `Contract` عام (النموذج عام أصلًا، لا حقول مشروعات إلزامية) يُنشأ آليًا عند أول دفعة → `totalValue` مجمّد → محرك الدفعات وحارس الالتزام يعملان. **`contractType` غير مطلوب** (المسار مشتق من `quotation.technicalRoute`، العقد 1:1). **BL-18 (حقل موافقة العميل) يبقى مفتوحًا** لكنه غير حاجب (D-13: الموافقة = الدفعة). | مُغلق (app-layer) |
| **BL-35** | ✅ **verified-in-code (2026-07-12).** `savePaymentPlan` (`lib/finance/actions.ts:14,34`) و`addPayment` (`lib/accounting/actions.ts:200,211`) كلاهما `ACCOUNTING/ADMIN`. **مسار كتابة وحيد على `Payment`، صفر `update`/`delete`/`raw`.** الخطر المفترض (SALES_REP يسجّل دفعة وهمية) **لا يتحقق.** | مُغلق |
| **BL-40** | 🟡 **تشخيص (2026-07-12، قراءة `docker-compose.yml`) — لا volume مخصّص لـ `public/uploads`، لكن bind mount `- .:/app` (السطر 51) يغطّي الجذر كاملًا** → محليًا الرسومات تُكتب على host (`./public/uploads`) وتنجو من rebuild/down-up. **الخطر الحقيقي في UAT/production:** إن لم يُكرَّر هذا الـ bind (نشر بصورة image فقط) → الرسومات في طبقة الحاوية العابرة = تُفقد. **التوصية المسجّلة:** named volume/bind مخصّص لـ `public/uploads` قبل UAT. | يوسف (قبل UAT) |
| **BL-42** | ✅ **محسوم (2026-07-12) — شاشة الأدمن سليمة.** النموذج: زر "حذف" = **تعطيل ناعم** (`deletedAt`+`isActive=false`) ↔ "إعادة تفعيل" (مشروط بـ `deletedAt`). العطل الظاهر (لا زر تفعيل) نتج عن **كتابة مباشرة سابقة للوكيل** (`isActive=false` بلا `deletedAt`) أنتجت حالة خارج النموذج — **نفس ما تحذّر منه STD-07.** BL-47 يهدأ تبعًا: ADMIN عامل يستطيع استعادة أي مستخدم من الشاشة. | مُغلق |
| **BL-45** | ✅ **مُغلق (PHASE 1، 2026-07-12).** `createContract` يرفض `SOCIAL_MEDIA` **لكل الأدوار بما فيها ADMIN** (`lib/contracts/actions.ts` → `errors.socialContractAutoOnly`). مُثبت بالدليل الخام: SALES_REP + ADMIN مرفوضان · مشروعات ينجح. المسار الوحيد لعقد السوشيال = `addPayment` عبر `createContractCore`. | مُغلق |
| **BL-43** | ✅ **مُغلق (PHASE 1، D-14).** عقد السوشيال يُنشأ آليًا في `addPayment` عند أول دفعة على عرض معتمد (`reviewStatus=APPROVED`) بلا عقد، **في transaction واحدة**، `createdById=المحاسب`، `totalValue` snapshot من العرض (`lib/accounting/actions.ts` + `src/lib/services/contract-core.ts`). مُثبت: `getContractBalances` يعمل فورًا · createdBy=ACCOUNTING. | مُغلق |
| **BL-44** | ✅ **مُغلق (PHASE 2).** حارس `createManufacturingOrder` موحّد: **عقد إلزامي للمسارين** (حُذف شرط `route===PROJECTS`) + **بوابة الدفع** (دفعة واحدة على الأقل، D-10 — لا عتبة). مُثبت بالدليل الخام: سوشيال بلا عقد→مرفوض · مشروعات بلا دفعة→مرفوض · النجاح في المسارين. | مُغلق |
| **BL-46** | ✅ **رُصد (2026-07-12): صفر** عرض `SOCIAL_MEDIA` عليه دفعات وبلا عقد (استعلام مباشر). لا بيانات يتيمة ستُحجب بعد توحيد الحارس (BL-44). **رصد فقط — لم أُهاجر.** | مُغلق (رصد) |
| **BL-48** | **سكريبت break-glass أُضيف** (`scripts/reset-password.mjs`). الحل الدائم = مسار إعادة تعيين داخل التطبيق (بعد UAT). السكريبت **لا يمنح قدرة جديدة** — من يملك الحاوية يملك القاعدة أصلًا — لكنه **يجعل الفعل مسجَّلًا** (`ActivityLog: PASSWORD_RESET_BREAKGLASS`) بدل كتابة صامتة. | ✅ مُضاف — الدائم بعد UAT |
| **BL-41** | ✅ **مُغلق (PHASE 2).** D-10 مطبّق — `createManufacturingOrder` يرفض بلا دفعة مسجّلة (`errors.noPaymentForManufacturing`). دفعة واحدة تكفي (لا عتبة). مُثبت بالجلسات. | مُغلق |
| **BL-36** | عمود **"نسبة المقدم"** في لوحة ADMIN — مشتق من `PaymentMilestone[0]`. **غير حاجب:** يُظهر النمط، وADMIN يضع سياسة بالبيانات لا بالتخمين. | يوسف |
| **BL-37** | 🔴 **حسابات UAT غير مُنشأة — REVIEW بلا حساب إطلاقًا.** (حساب dev-review-a للاختبار فقط.) **بلوكر UAT.** | يوسف |
| **BL-38** | **حسابات dev + بيانات دورة P6** تُعطَّل/تُنظَّف قبل UAT. | يوسف |
| **BL-39** | 🚨 **كلمة مرور `inspection@egyglass.com` غُيّرت أثناء الاختبار** — تُدوَّر قبل UAT. | يوسف |

---

# ⚪ مؤجل بقرار موثّق

| # | البند | ملاحظة |
|---|---|---|
| **BL-21** | **موديول المخازن (Inventory)** — النموذج المعتمد: **سجل حركات append-only** (`StockMovement`: IN/OUT/ADJUST + كمية + سبب + مرجع) · **الرصيد مشتق** (لا عمود رصيد قابل للتعديل) · **حجز عند إصدار أمر التصنيع** (المتاح = الموجود − المحجوز) | **ليس بلوكر UAT** — PROCUREMENT يعمل بدونه. SCR منفصل، مرحلة 2 |
| **BL-22** | آلية الملحق (Addendum) الكاملة | ما بعد UAT |
| **BL-23** | تعدد الأدوار (مثال: ACCOUNTING + HR لنفس الموظف) | تدقيق قراءة أولًا |
| **BL-24** | إرفاق رسمة بعرض السعر (رفع صورة مباشر) | لا ربط Drawing↔Quotation |
| **BL-25** | فصل قسمين فرعيين داخل `TECHNICAL_OFFICE` (مشروعات/سوشيال، R-03) | يحتاج حقل قسم فرعي |
| **BL-26** | شاشات المستخلصات/الفواتير (SCR-015) | schema مطبّق · التطبيق معلّق |
| **BL-27** | `getSettings()` مكرر — لا accessor مركزي (`lib/config.ts`) | دَين تقني |
| **BL-28** | تنظيف مراجع `NODE_ENV` القديمة (`AGENTS.md` · `CLAUDE.md` · `START-HERE.md`) | توثيق |
| **BL-29** | `calculateRecipe` غير مُهاجَر لـ Decimal (~1e-12) | تحسين |
| **BL-30** | قوائم أسعار لكل مُصنِّع | **ملغى كأولوية** |

---

## 🧭 بنود دائمة

| # | البند |
|---|---|
| **STD-01** | **بعد كل قرار:** تحديث `CLAUDE.md` + `AGENTS.md` — **يوسف فقط**، مرة واحدة بعد الحسم. |
| **STD-02** | **مصدر الحقيقة = الكود.** ليس تعليقات الـ schema (تعليق `ceoDrawingApprovalThreshold` قال `null = كل المشروعات`، والسلوك `null = تخطّي`). |
| **STD-03** | أي متطلب يولد في محادثة **يُقيَّد هنا في نفس اللحظة** — قبل ما يتحول لكود أو migration. |
| **STD-04** | **الفلوشارت قديم** — قسم "قرارات محسومة" **يعلو عليه**. |
| **STD-05** | **الجهل يُكتب، لا يُصمَّم.** (القاعدة الكاملة أعلى الملف.) |
| **STD-06** | **كل بند في هذا الملف يُوسَم `verified-in-code` (بملف/سطر) أو `unverified`.** ادعاء بلا سند سطري = `unverified` صراحةً — لا يُبنى عليه. (نشأت من رسالة commit وصفت تعديلات وقاعدة دفع لم تكن في الملف ولا في الكود.) |
| **DBW-01** | **كتابة مباشرة مأذونة (STD-07، إذن يوسف الصريح · 2026-07-12).** `UPDATE "users" SET "isActive"=true WHERE email LIKE 'dev-%' AND "deletedAt" IS NULL AND "isActive"=false;` — **السبب:** كتابة مباشرة سابقة للوكيل أنتجت حالة خارج النموذج (isActive=false بلا deletedAt) لا مسار واجهة يعيدها؛ التصحيح يتطلب كتابة واحدة. **الحسابات المتأثرة:** dev-rep-e · dev-tecm-e · dev-tec-a · dev-appr-b · dev-acc-d · dev-review-a · dev-lead-c · dev-tech-c · dev-prc-a (كل dev-* المعطّلة غير المحذوفة). **مستثنى صراحةً:** `inspection@egyglass.com` (يبقى معطّلًا — يخرق سياسة @egyglass.net). |
| **STD-10** | **نتعامل بالمناصب (roles) لا بالأشخاص.** الاسم = شاغل المنصب اليوم؛ المنصب هو الثابت. كل قرار في BACKLOG يُكتب بالدور، والاسم بين قوسين كإشارة. (جذر كارثة بوابة CEO: قُرئت "المدير التنفيذي" كشخص لا كمنصب.) **الاستثناء الوحيد: منع اعتماد الذات — يُبنى على الشخص لا المنصب.** ✅ **تحقّق 2026-07-12:** صفر منطق يستهدف شخصًا/userId صلبًا في الكود؛ كل مقارنات userId = ملكية/ذات ديناميكية؛ الأسماء في التعليقات فقط؛ `assertNotSelf` = الاستثناء المسموح. |
| **STD-09** | **ممنوع تخزين أي كلمة مرور في ملف نصي داخل شجرة العمل — حتى لحساب dev.** النمط ينتقل إلى UAT. الاعتماد يُمرَّر عبر stdin أو env، لا ملف. (الدرس 3: سر يدخل git = يبقى في التاريخ للأبد.) **تحقّق 2026-07-12:** ملفات كلمات dev كانت في scratchpad الجلسة (خارج الريبو) — **صفر منها دخل git**؛ `scr016-pw.txt` حُذف. |
| **STD-08** | **ممنوع تسليم الوكيل أي بيانات اعتماد حقيقية.** الوكيل يعمل بحسابات dev فقط. أي فعل يتطلب ADMIN حقيقيًا → **يوسف ينفّذه بنفسه.** (نشأت من: كلمة مرور `admin@egyglass.net` مجهولة، والوكيل لا يُسلَّم ولا يُنشئ اعتمادًا حقيقيًا.) |
| **STD-07** | **ممنوع الكتابة المباشرة على قاعدة البيانات كالتفاف على أكشن مكسور.** الأكشن المكسور = بند في BACKLOG، لا عقبة تُلَفّ. أي كتابة مباشرة تتطلب **إذن يوسف الصريح** وتُسجَّل في BACKLOG. (نشأت من واقعة: 4 حسابات عُطّلت بكتابة مباشرة بصفر `ActivityLog`، والالتفاف أخفى عطلًا محتملًا في شاشة الأدمن — BL-42.) |

---

## سجل التنفيذ (دفعة إعادة إسناد السلسلة — 2026-07-12)

- **PHASE 1** — تعطيل البوابتين المخترَعتين G2/G3 (BL-02/03). build GREEN + جلسات.
- **PHASE 2** — نقل اعتماد العرض لـ TEC_APPROVER (BL-05) · إلغاء التوليد التلقائي (BL-06) · حارس أمر التصنيع: دور + رسمة + عقد (BL-01/08) · approvedById (BL-16) · زر إصدار الأمر في `/technical-office/[id]` (BL-32، قرار يوسف).
- **PHASE 3** — بوابة REVIEW على أمر التصنيع: 3 تأكيدات + حجب server-side (BL-07، D-09).
- **PHASE 4** — دورة كاملة من الشاشة بصفر بذرة: عميل→طلب TEC-PRJ-0005→إسناد→عرض→اعتماد TEC_APPROVER→عقد→رسمة معتمدة→إصدار أمر تصنيع→مراجعة REVIEW(3 تأكيدات+اعتماد)→READY. الأثر يثبت كل دور في موضعه.
- **مفتوح لقرار يوسف:** BL-18 (موافقة عميل السوشيال) · BL-31 (دفع السوشيال) · BL-33 (ملكية اختيار المصنع) · BL-20 (حذف أعمدة/enums الميتة، SCR).

*آخر تحديث: 2026-07-12 — بعد إعادة إسناد السلسلة الحقيقية للأدوار (PHASE 1-4).*
